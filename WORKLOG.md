# WORKLOG

두 대의 Windows PC(직장 / 집)에서 Claude Code로 번갈아 작업할 때 대화 맥락을 이어받기 위한 작업 로그.
각 항목은 장소(직장/집)로 구분한다. **최신 항목이 맨 위**에 오도록 추가한다. 세션 시작 시 이 파일을 먼저 읽고, 세션 종료 시 맨 위에 새 항목을 추가한다. 과거 항목은 수정하지 않는다.

## 루틴
- **작업 시작**: 폴더 열기 → `git pull` → 이 파일 읽기 → Claude Code 작업
- **작업 종료**: 맨 위에 새 항목 추가 → `git add .` → `git commit` → `git push`

---

## 2026-08-27 (126) / 원격(클라우드 세션) — 분배금공지 백지·"불러오기 실패"의 진범: **Apps Script 트리거 인증 만료**

- **증상**: 분배금공지 탭에서 KODEX·ACE·RISE·SOL 카드가 `불러오기 실패`, 분배 일정 달력이 통째로 백지, 운용사별 일정 "정보 없음". 메일함엔 `[분배알림 이상] 12건` 워치독 메일(8/21·8/24).
- **원인은 파서가 아니다.** 컨테이너 프록시가 `script.google.com`·운용사 사이트를 전부 막아 라이브 호출이 안 돼서, **구글 드라이브 연결로 백엔드 시트를 직접 읽어** 판정했다.
  - 구글이 보낸 실패메일 7통(7/24~8/20)이 전부 같은 문구다: **`Authorization is required to perform that action.`** (8/5 `refreshAllDistributions` 09:01, 8/19 `snapshotPrices` 16:38, 둘 다 time-based)
  - **시간 트리거는 '만들어진 시점의 인증'으로 돈다.** `appsscript.json`에 `oauthScopes`가 없어 스코프가 코드에서 자동 추론되는데, 워치독의 `CalendarApp`·`MailApp` 등이 붙으면서 스코프 집합이 바뀌었다 → 옛 트리거가 전부 이 에러로 죽었다. **재인증만으로는 안 살아난다. 트리거를 다시 만들어야 한다.**
- **정지 시각 확정**: `_파서메타`.updated·분배캐시 savedAt 모두 **8/26 18:50~18:51**(그날 마지막 30분 슬롯)이 끝. 알림로그 8/26 16:52. 스크리너 `date_v4` 8/21. 시트 최종수정 8/26 18:51 → 오늘 아무 트리거도 안 돌았다. **오늘 08:36 워치독 메일이 안 온 것**(재발송 주기 3일 → 8/21·8/24 다음은 8/27)이 결정적 증거.
- **파서는 멀쩡하다.** 8/26 18:50 강제 새로고침에서 6개사 전부 파싱됐다: kodex 161 / tiger 95 / ace 37 / rise 78 / plus 37 / sol 27건, 8월 말 회차가 분배캐시에 온전하다.
- **조치: `resetAllTriggers()` 추가** (`Code.gs`, 수동 실행 섹션). `setup*` 11개를 하나씩 누르던 걸 **▶ 1회**로 끝낸다. 목록 밖 핸들러는 건드리지 않고 이름만 보고한다.
  - `snapshotPrices`는 **원래 설치 함수가 없었다**(트리거 UI에서 손으로 만든 것) → 이번에 계획에 넣었다(장 마감 후 16시).
  - `keepWarm`은 처음엔 뺐다가 **다시 넣었다** — `_diagTriggers()` 결과 실제로 깔려 있었고, 빼두면 인증이 낡은 채 5분마다 실패만 낸다. 실행 슬롯을 물던 건 자기 웹앱을 `UrlFetchApp`으로 호출하던 옛 버전이고(WORKLOG 85·86), 지금은 시트 핸들만 깨우는 한 줄이라 가볍다. (`_diagTriggers`의 ⚠ 표시는 그 시절 잔재 — 아직 안 고침)
  - 트리거는 편집기 저장본으로 도니 **재배포 불필요, `clasp push`만** 하면 된다.
  - **유령 트리거는 없었다.** `_diagTriggers()` = 15개 = 계획 12종 + `snapshotPortfolio` ×3. 8/5 메일의 09:01 실행 기록을 보고 중복을 의심했는데 그 사이 `setupRefreshTrigger`로 다시 깔린 것으로 보인다.

### ✅ 복구 완료 (같은 세션 내)
- **이 클라우드 컨테이너에 clasp를 붙였다.** `script.google.com`은 프록시가 막지만 **`script.googleapis.com`은 뚫린다** → `clasp push`가 된다. `npm i -g @google/clasp`(3.4.0) 후 `clasp login --no-localhost`로 URL을 사용자에게 주고 리디렉션 URL을 돌려받는 방식으로 인증했다(구글 인증만은 에이전트가 못 하는 경계).
  - ⚠️ clasp v3는 `clasp pull`이 **`Code.js`**로 떨어진다(원격 파일명은 `Code`라 push는 `Code.gs`로 해도 같은 파일). `clasp status`로 `Code.gs`+`appsscript.json` 2개만 올라가는 것 확인.
  - ⚠️ **`clasp push`는 이미 열려 있는 편집기 탭을 갱신하지 않는다.** F5를 안 하면 새 함수가 드롭다운에도 검색에도 안 나온다(실제로 두 번 헛짚었다).
- `▶ resetAllTriggers()` 12:21:46 실행 → 13종 전부 ✅, 15개 재생성, 계획 밖 경고 없음.
- `▶ checkDistNotices()` 12:25:59~12:26:54(55초) → **6개사 전부 오늘로 갱신**: kodex 189 / tiger 119 / ace 52(OCR) / rise 78 / plus 37(OCR) / sol 27건, 공시일 8월 26~27일. 화면 복구됨. (SOL 29,206자 쓰레기 행 폐기 로그 = `_dropJunkItems` 정상 동작)
- **검증은 사용자가 아니라 내가 한다.** 드라이브 연결로 시트(`1iNlOU1…`)의 `분배캐시`·`_파서메타`·`알림로그`를 직접 읽어 판정했다. "실행했는데 로그가 조용하다"를 시트로 갈랐다.

### 손으로 돌릴 건 `점검()` · `수리()` 두 개로 통합
- 편집기 함수 드롭다운에 최상위 함수가 **141개**(그중 손으로 돌릴 것 17개) 떠서 "뭘 눌러야 하지"를 찾는 것 자체가 일이었다.
- `점검()` — 바꾸는 것 없이 상태만: `TRIGGER_PLAN` 대조 트리거 목록(누락·중복·계획 밖), 분배캐시 6개사 신선도(회차 일치 + TTL 내, 아니면 "화면에선 빈칸으로 나온다"), 알림로그 마지막 줄.
- `수리()` — `resetAllTriggers()` + 6개사 강제 재파싱. 6분 실행 제한 전 **5분에서 스스로 멈춘다**(나머지는 30분 트리거가 채움).
- 한글 이름은 드롭다운에서 눈에 띄게 하려는 것(이 파일에서 유일한 한글 함수명).
- **검증**: 테스트 러너가 없어 Apps Script 전역을 가짜로 물린 `vm` 샌드박스에서 `점검()`을 실제로 실행했다(`scratchpad/smoke.js`). 트리거 누락·×3 중복·계획 밖 유령, 캐시 신선/stale(17.7시간)/없음 세 경로가 모두 의도대로 찍혔다.
- **`clasp run`은 안 하기로 했다.** 그게 되면 내가 원격에서 아무 함수나 돌릴 수 있지만, GCP 프로젝트를 기본→표준으로 바꿔야 하고 되돌리기가 깔끔하지 않다. 살아 있는 개인 금융 앱에 그만한 위험을 질 이유가 없다 — 정상 상태면 누를 일이 없고 확인은 드라이브로 하니까.

### 아직 안 고친 설계 결함 2개 (같은 증상의 진짜 원인)
1. **`getDistributionAll`이 오래된 캐시를 통째로 버린다.** TTL(공시 임박 낮 2시간) 초과면 `{items:[], stale:true}`만 반환 → 프론트가 6개사 실시간 스크랩 → 느리거나 실패 → **시트에 멀쩡한 데이터가 있는데 화면은 백지.** stale 표시만 달고 payload는 실어 보내야 한다.
2. **워치독의 "종목 0건 — 파서 점검 필요"는 거짓말이다.** `distWatchdog`는 `getDistributionAll()`만 부르고 **파서를 아예 안 돌린다.** stale이면 자동으로 `hasItems=false` → 소스당 2건씩 = 정확히 12건. 매달 21~25일(월말 공지 전)마다 나는 오경보다.

### ✅✅ 트리거 자동 실행까지 확인 — 완전 복구 (13:06 자동 점검)
- 세션에 13:05 자기 체크인을 예약해두고(`send_later`), 깨어나서 드라이브로 시트를 다시 읽었다.
- **분배캐시 `savedAt`·`_파서메타` `updated`가 12:56~12:57로 넘어갔다.** 수동 실행(12:26)이 아니고 아무도 안 눌렀다. `_파서메타`를 쓰는 건 `checkAndLogAlerts` 하나뿐이고 그걸 부르는 건 `checkDistNotices`(30분 트리거)뿐 → **트리거가 제 인증으로 정상 실행됐다.** 이게 인증 복구의 유일한 증거다(수동 실행은 편집기 권한으로 도니 증거가 못 된다).
- **알림 파이프라인도 살아났다.** 알림로그에 오늘 KODEX·TIGER·ACE `신규공지: 공시일 8월 27일 (월말)`이 새로 찍혔다 — 카톡·캘린더가 이 경로를 탄다.

### ✅ 설계 결함 2개도 수정 + 재배포 완료 (@157)
1. **`getDistributionAll`이 오래된 캐시의 items를 버리던 것** → 마지막 파싱 결과를 그대로 싣고 `stale` 표시만 붙인다. 프론트(`portfolio.html`·`dist_notice.html`, **쌍으로 수정**)는 `fillStale`(최대 45초)을 기다리지 않고 **먼저 그린 뒤** 갱신한다. 트리거가 또 멎어도 화면이 백지가 되지 않는다.
2. **워치독 12건 오경보** → 파이프라인 생존을 `refreshAllDistributions`가 남기는 **`WD_LAST_REFRESH`** 스크립트 속성으로 판정한다(파싱 결과와 무관하게 '돌았다'만 기록, `WD_PIPELINE_DEAD_HOURS = 26`).
   - 왜 그 신호여야 하나: **분배캐시 `savedAt`은 파싱이 0건이면 안 써지고**(새 회차 공지 전인 21~25일·1~7일), **`_파서메타`는 공지창 밖(13~22일)엔 아예 안 돈다.** 둘 다 생존 신호가 못 된다.
   - 0건·공시일 지연은 **공지가 올라왔어야 할 시점**에, 그리고 **선갱신이 살아 있을 때만** 사고로 본다. 선갱신이 멎었으면 데이터가 낡은 게 당연하므로 운용사를 탓하지 않고 원인 한 줄만 남긴다.
   - 검증(`scratchpad/wd_smoke.js`, vm 샌드박스): **8/24 상황 재현 12건 → 0건**, 선갱신 51시간 정지 → 원인 1건만, 공지가 있어야 할 8/28에 0건 → 6건 그대로 검출.
- **배포**: `clasp push --force` → `clasp redeploy AKfycbwJS1Fd-… -d "…"` → **@156 → @157**. clasp v3는 `redeploy`가 `update-deployment`의 별칭이고 `-d`는 그대로다.
- ⚠️ `WD_LAST_REFRESH`는 **내일 05시 선갱신이 처음 돌 때** 생긴다. 그때까지 워치독은 '선갱신 기록 없음'을 보고할 수 있다(정상, 문구에 그렇게 적어뒀다).

### 달력에서 '분배금지급기준일' 마크를 뺐다 + 분배락일 역산 공휴일 버그
- **왜**: 기준일 = 분배락일 + 1영업일이라 **연이틀 마크 두 개가 같은 사실 하나**를 말했다. 6개사 × 2회차 = 월 12개를 헛되이 채웠다. 행동에 필요한 건 "분배락일 **전날**까지 사야 받는다"뿐이다(T+2 결제 때문에 락일에 사면 기준일 명부에 못 든다).
- **표시만 뺐다. 데이터는 그대로 쓴다** — `SCHED_CFG`에 `tableOnly:true`를 달아 `addAll`에서만 거른다. 운용사별 일정 표에는 그대로 나온다. 지우면 안 되는 이유: ① `fillRakil`이 **기준일에서 분배락일을 역산**한다(운용사별 제공 필드를 실측하니 **RISE만 분배락일을 안 준다**; 기준일은 6개사 전부 준다) ② `cycleOf`가 기준일의 '일'로 월중·월말을 가른다.
- 🐛 **`prevBizDay`가 주말만 건너뛰고 공휴일을 무시했다.** 같은 파일에 `HOLIDAYS` 표가 있는데 안 썼다 — 기준일이 8/18(화)이면 8/17(광복절 대체)을 락일로 찍었다. RISE처럼 락일을 역산하는 운용사에서 틀린 날짜가 나온다. `HOLIDAYS`를 보게 고쳤다.
- **검증**(`scratchpad/cal_smoke.js`): `portfolio.html`에서 `HOLIDAYS`·`prevBizDay`·`SCHED_CFG` 조각을 **실제로 떼어내** 실행했다. 8/18→8/14, 3/3→2/27(삼일절 대체), 2/19→2/13(설 연휴), 1/2→12/31(신정) 등 6건 통과. 달력=공시일·분배락일·지급일, 표=4개 그대로.
- `portfolio.html`·`dist_notice.html` **쌍으로** 고쳤다. 프론트만이라 **재배포 불필요**(git push로 끝).

### SOL을 매번 못 잡던 진짜 원인 — 제목의 `②`에 의존 (@158)
- **증상**: SOL 월말 회차가 계속 안 잡힌다. 사용자가 블로그를 직접 보니 **월말 공지도 지급일도 올라와 있었다.**
- **원인**: `fetchDist_sol`이 월말을 **제목에 `②`(원문자 2)가 있는 글**로만 골랐다. SOL이 표기를 한 번만 바꿔도 그 회차가 통째로 사라진다.
  - 캐시가 증거였다: `2026-08-말` 행인데 내용물이 **8월 중순**이다(items 4건 전부 `cycle:"월중"`, `schedule` 8/11 공시·8/14 기준·8/18 지급, `fallback:true`·`_source:"blog"`).
  - 더 나쁜 것: 옛 필터는 **지난달 7월 ② 글을 8월 월말로 집을 수도** 있었다(스모크로 재현).
  - ※ `_파서메타`의 sol `cycles`·`pubDate`가 늘 비었던 것도 같은 원인이다 — 현재 회차(월말) 항목이 아예 없으니 `_fingerprint`가 공시일을 못 뽑았다. 앞서 "다음 할 일"에 적었던 그 항목이 이걸로 해소된다.
- **수정**: `_solCycle(sched, title)` 신설 — **본문에서 뽑은 기준일**(없으면 분배락일)의 '일'로 가른다(20 이하 월중). 프론트 `cycleOf`와 같은 규칙이라 다른 운용사와 일관된다. 날짜를 못 뽑으면 제목 힌트(`중순`·`①`·`말`·`하순`·`②`)로 폴백하니 예전 경로도 살아 있다.
  - ⚠️ **지급일은 회차 판정에 쓰지 않는다** — 월말 회차의 지급일은 다음 달 초라 '일'로 보면 월중으로 오분류된다. (프론트 `cycleOf`는 아직 `기준일 || 지급일`을 쓴다 — 같은 함정이 남아 있다.)
  - 글 선택도 제목 패턴 대신 `분배금` 최신순 훑기. **최신 4건 상한 + 두 회차 채우면 즉시 중단** — 본문 fetch가 글당 1회라 트리거 6분 한도를 지켜야 한다(6개사 force 합계 실측 213초, WORKLOG 90). 검증에서 실제 fetch는 2회로 옛 코드와 같았다.
  - 한쪽이라도 못 채우면 **실제 블로그 제목을 실행 로그에 남긴다.** 오늘 컨테이너에서 블로그가 막혀(프록시 000) 제목을 못 구한 게 진단의 병목이었다 — 다음엔 로그만 보면 된다.
- **검증**(`scratchpad/sol_smoke.js`): 네트워크 함수 2개(`_solNotices`·`_solParsePost`)만 가짜로 물리고 판정·선택 로직은 실제로 실행. `_solCycle` 6건 통과, 제목이 `8월 분배금 안내 (2)`여도 **월말 19건·월중 4건 둘 다** 확보, 이벤트 글 제외, 옛 코드로는 7월 ②를 월말로 집는 것까지 재현.

### 공지사항 게시판에 상시 안내 한 줄
- `renderNoticeBoard` 맨 위에 고정 문구: **"분배락일 전날까지 사야 이번 분배금을 받습니다 — 락일 당일 매수는 제외"** (배지 `안내`, 날짜 칸 `상시`).
- 운용사 데이터가 아니라 고정 HTML이라 항상 보인다. T+2 결제라 락일 당일 매수는 지급기준일 명부에 못 든다. `portfolio.html`·`dist_notice.html` 쌍으로.

### 달력 기준일: 뺐다가 **되돌렸다**
- 중복이라 판단해 `tableOnly`로 달력에서 뺐으나, 사용자가 알아보고 **필요하다고 해서 복구**했다. `SCHED_CFG` 4종 원상.
- 🐛 **같이 고친 `prevBizDay` 공휴일 반영은 유지한다**(사용자 확정: "공휴일은 당연히 적용해야지"). `WORKLOG 1335`에 *"HOLIDAYS가 생겼으니 반영 가능하나 사용자 확인 후 진행 예정"*으로 대기 중이던 항목이 이걸로 해소됐다. 지금 화면에 바뀌는 날짜는 없고(8/14 기준일→8/13에 공휴일이 안 낌), 설·추석 연휴에서 틀리던 것을 막는다.

**다음 할 일**: ① 프론트 `cycleOf`의 `기준일 || 지급일` 폴백 — 기준일이 없고 지급일만 있는 월말 회차를 월중으로 오분류한다(백엔드 `_solCycle`은 이미 분배락일로 바꿨다) ② (선택) `_diagTriggers`의 `keepWarm` ⚠ 표시는 옛 자기호출 시절 잔재라 지금은 오해를 부른다 — 지울 것.

## 2026-08-27 (125) / 원격(클라우드 세션) — KAIA 가 `err` 로 죽던 것: **야후에 원화 페어가 아예 없었다**

- 사용자가 학교 위젯 스크린샷을 보냈다. 상단에 `코인: 해외 환산` = **122의 미검증 항목 ②가 이걸로 해소됐다** — 거래소가 막힌 곳에서 해외 환산 경로가 실제로 발동했고 BTC·XRP 는 잘 돌고 있었다. 저장해 둔 김치프리미엄도 살아 있었다(`:618` 은 `coin_prem` 에 값이 있을 때만 "해외 환산"이라 쓴다).
- **그런데 `KAIA` 와 `XRP당 카이아` 두 줄만 `err`.** 코드로 범위를 좁혔다: `err` 은 값이 `None` 일 때만 찍히고(`:521`), BTC·XRP 가 환산됐으니 `blocked=True` 는 확실 → `elif ext.get(k) and blocked` 에서 KAIA 만 걸러졌다 = **야후 응답에 `KAIA-KRW` 가 없다.** 비율 줄은 `if a in data and b in data`(`:629`) 라서 딸려 죽은 것이고 별개 버그가 아니다.
- ⚠️ **이 컨테이너는 야후·빗썸·업비트·코인게코가 전부 프록시에 막혀(`curl` 전부 `000`) 심볼 존재 여부를 내가 확인할 수 없었다.** 넘겨짚지 않고 **PC 에서 돌릴 한 줄을 만들어 사용자에게 부탁했다.** 결과:
  `[('XRP-KRW', 1930.87, 'KRW'), ('BTC-KRW', 108447992.0, 'KRW'), ('KAIA-USD', 0.032171156, 'USD')]`
  → **`KAIA-KRW` 는 없고 `KAIA-USD` 만 있다.** 추측이 아니라 실측으로 확정하고 고쳤다.
- **고침(`tools/ticker.pyw`)** — 원화 페어가 없는 코인은 **달러 페어 × 환율(`USDKRW=X`)** 로 원화를 만든다.
  - `coin_usd` (코인 → 야후 달러 심볼) 를 새로 둔다. 야후가 원화 심볼을 **빼고 응답하면** 그 코인을 여기 담고 다음 조회부터 달러로 묻는다. 심볼표를 손으로 박지 않으니 다른 코인이 늘어도 알아서 걸린다.
  - **해외 시세를 원화로 정규화한 `far` 를 만들어 기존 로직에 먹인다** → `convert()` 도 프리미엄 학습식도 손대지 않았다. 달러 경로에서도 김치프리미엄은 그대로 순수한 프리미엄으로 남는다.
  - 등락률은 **달러 기준을 그대로** 쓴다. `(가격-등락폭)×환율` 은 `가격×환율÷(1+등락률)` 과 항등이라 **환율 변동이 코인 등락률에 섞이지 않는다**(대수적으로 확인).
  - `coin_usd` 를 설정에 저장한다(`coin_prem` 과 같은 자리, 10분 주기). 안 그러면 **막힌 곳에서 켤 때마다 첫 사이클은 `err`** 이다.
  - 🔎 **덤으로 표시 정확도 하나.** `coin_src` 를 코인 루프 안에서 덮어쓰고 있어서, BTC·XRP 는 프리미엄이 있고 KAIA 만 없는 지금 상황에서 **전부 원값인 것처럼 `해외평균(환산비 없음)`** 이라고 뜰 수 있었다. 루프 뒤에서 한 번만 정하도록 바꿔 `해외 환산 · KAIA 원값` 처럼 **어느 코인이 원값인지** 밝힌다. (118·120 의 "상태 표시가 거짓말하면 안 된다"에 걸린다.)
- **검증: 가짜 tkinter 를 물리고 네트워크만 스텁으로 바꿔 진짜 `refresh()` 를 9가지 시나리오로 돌렸다**(121 과 같은 방식, 스크래치패드 `h/faketk.py`·`harness.py`·`t2.py`). 테스트에서 건드린 원본은 `self_update()` 호출 한 줄뿐.
  ① 거래소 정상(원화 심볼 물어보고 없으면 학습) ② 2번째 사이클(달러+환율로 물어보고 프리미엄 학습, 손계산 `44.56 / (0.032171156×1385.15)` 과 일치) ③ 거래소 막힘 — **해외가를 KAIA +5% / BTC +2% 로 움직여 환산이 실제로 계산됨을 확인**(44.56→46.79, 110,000,000→112,200,000 정확히 일치) ④ 부팅부터 막힘(저장값으로 첫 사이클부터 나옴) ⑤ 환율 결측(KAIA 만 죽고 나머지 생존) ⑥ 거래소+야후 둘 다 막힘(`coin_usd` 를 오염시키지 않음 — 야후 실패를 "원화 페어 없음"으로 오해하면 안 된다) ⑦ **실제 업그레이드 경로**(저장값 없이 막힌 채 첫 실행 → 1사이클 `err`, 2사이클부터 정상) ⑧⑨ 상단 표시 문구.
  - ③ 에서 처음엔 환산 결과가 빗썸 값과 **똑같이** 나와 차단이 안 된 줄 알았는데, 스텁 가격이 안 움직여서 환산이 정확히 되돌아온 것이었다. 해외가를 움직이게 고쳐 실제로 계산됨을 드러냈다.
- ⚠️ **윈도우에서 못 돌려봤다.** 다만 이번 변경은 **렌더링이 아니라 데이터 경로**이고(120 의 교훈이 걸리는 종류가 아니다), 화면 요소를 하나도 건드리지 않았다. 실측 확인은 PC 스크린샷으로 해야 한다.
- **`XRP당 카이아` 퍼센트의 뜻을 정리해서 알려줬다** — `(지금 비율 − 자정 비율) ÷ 자정 비율`. 금액으로는 **"자정에 1000만원을 XRP 에 넣었을 때와 KAIA 에 넣었을 때의 오늘 수익률 차이"**(122 값으로 -1.77% ≈ 17.5만원). 두 코인 퍼센트의 뺄셈이 아니라 비율의 비율이다.
  - 사용자 지적: **"매일 리셋돼서 뭐가 중요한 수치인지 가늠이 안 된다."** 맞는 말이다 — 하루치 상대 강약만 보여주니 갈아타기 판단엔 못 쓴다. 기준을 바꾸는 두 안(A: 30일 전 대비 / B: 최근 30일 최저~최고 중 현재 위치)을 냈고 **B 를 추천**했다. **아직 고르지 않았다.**

**다음 할 일**: ① 학교 PC 에서 위젯 껐다 켜고 **KAIA 두 줄이 살아나는지 스크린샷 확인**(첫 5초는 `err` 이 정상, 두 번째 사이클부터 나온다) ② **KAIA 프리미엄은 거래소가 열린 곳(집)에서 한 번 켜야 배운다** — 그전까지는 해외 원값이라 실제 국내가와 1~3% 벌어지고 상단에 `해외 환산 · KAIA 원값` 으로 뜬다 ③ `XRP당 카이아` 기준 A/B 결정 ④ (122 에서 넘어옴) 집 PC `git pull` → `ticker_move.ps1` 실행, 그 뒤에 H: 사본 정리 ⑤ 부팅 자동시작을 걸지 말지 결정(지금은 두 PC 다 없음).

---

## 2026-08-27 (124) / 학교 — 답변 문체 반말 문제: 존댓말 규칙을 **전역 CLAUDE.md**로 승격

- **증상**: jjk 하위 방(세션)들에서 사용자에게 반말로 답한다는 지적.
- **원인**: 존댓말 규칙이 메모리 파일(`use-korean-polite.md`) 하나뿐이었다. **CLAUDE.md가 메모리를 이긴다.** 게다가 이 `CLAUDE.md`는 전체가 반말 지시문(`~한다`, `~하지 마라`)이라 답변 문체까지 거기 끌려갔다. 유저 레벨 `~/.claude/CLAUDE.md`는 아예 없었다.
- **조치**: 규칙 본문을 `~/claude-memory/CLAUDE-global.md`에 두고 `~/.claude/CLAUDE.md`로 **하드링크**했다(커밋 `84f21b0`). 저장소가 두 PC 간 동기화되니 본문은 자동으로 따라온다.
  - 심볼릭 링크는 **관리자 권한이 필요해서 실패**했다(개발자 모드 꺼짐). 하드링크는 같은 볼륨이면 권한 없이 된다. Git Bash의 `ln -s`는 조용히 **복사본**을 만드니 링크 여부를 `Get-Item .LinkType`으로 반드시 확인할 것.

**다음 할 일 (집 PC)**: `~/.claude/CLAUDE.md`가 아직 없다. `git pull` 후 아래 한 줄이면 끝난다 — 그 PC 세션이 알아서 실행하면 된다.
```
New-Item -ItemType HardLink -Path "$HOME\.claude\CLAUDE.md" -Target "$HOME\claude-memory\CLAUDE-global.md"
```

## 2026-08-26 (123) / 학교 — 비디오 다운로더를 **학교 PC에서 최신본으로**: `update.bat` 하나로

- 이건 `jjk`가 아니라 **두 번째 프로젝트** [`video-downloader-extension`](https://github.com/jaenamking1-collab/video-downloader-extension)(비공개) 이야기다. 크롬 확장(MV3) + `server.py` 로컬 서버(포트 57891), yt-dlp/ffmpeg/gallery-dl 래퍼.
- **저장소 최신 커밋은 8/21 `552d63d`**. 8/21에만 9개가 들어갔다 — m3u8 을 전부 yt-dlp 로(ffmpeg 순차가 너무 느림), 팝업 파일명이 늘 `master.mp4` 가 되던 것, CDN 핫링크 403, 재생 페이지 주소를 영상으로 착각해 0바이트, ← → 5초 이동, 같은 영상이 다른 제목으로 중복 저장. 학교 사본이 그 전이면 이걸 다 받는 셈이다.
- **`update.bat` 추가 (커밋 `3b16d90`)** — 최신본 맞추기를 매번 손으로 하지 않도록 한 파일로 묶었다. 더블클릭하면: 폴더 찾기 → 없으면 clone / 있으면 `git pull --ff-only` → `yt-dlp`·`gallery-dl` 업그레이드 → 지금 코드 한 줄 출력 → `start.bat` 으로 서버 재시작.
  - **폴더를 넘겨짚지 않는다.** 레지스트리 `Shell Folders`의 `Personal`/`Desktop` 실제 값을 읽는다 — OneDrive 리디렉션(`OneDrive\문서`)과 한글 폴더명 때문에 경로 리터럴을 박으면 한쪽 PC에서 빗나간다(ticker 이사 스크립트에서 겪은 그 문제).
  - **git 사본이 아닌 폴더**면 소스만 `_backup\` 에 복사한 뒤 `git init` + `fetch` + `checkout -f` 로 사본으로 바꾼다. **받아둔 영상과 `cookies.txt` 는 git 이 모르는 파일이라 안 건드린다** — 리눅스에서 가짜 폴더(영상 mp4 + cookies.txt + 고친 content.js)로 실제 실행해 확인했다.
  - **실행 중인 자기 자신이 `git pull` 로 덮이면** cmd 가 남은 줄을 파일 오프셋으로 다시 읽어 엉뚱하게 동작한다 → `%TEMP%\vdl_update\` 사본으로 다시 실행한 뒤에 pull 한다(`call` 없이 넘겨서 원본 파일을 닫는다).
  - `.gitignore` 가 **화이트리스트 방식**이라 `!update.bat` 을 직접 넣었다(안 넣으면 추적 자체가 안 된다).
  - ⚠️ **컨테이너에 cmd.exe 가 없어 배치 실행 검증은 못 했다.** 대신 지연확장(`!`) 때문에 `[!]` 가 `[]` 로 찍히는 것을 `[^!]` 로 고치고, 괄호 블록 안에서 다른 배치로 넘기던 것을 `goto :run` 으로 풀었다. git 쪽 로직만 리눅스에서 실제로 돌려 확인했다.
- **사람이 해야 하는 건 딱 하나**: `chrome://extensions` 에서 확장 새로고침. 안 하면 서버만 새 코드고 `content.js`/`background.js` 는 옛 코드로 돈다. `cookies.txt` 는 깃에 없으므로 PC 마다 따로 둔다(X/인스타 비공개용).
- `~/claude-memory/video-downloader-project.md` 에도 같은 요약을 남겼다.

- ✅ **학교 PC 실행 완료 (같은 세션 내).** 실제로 돌려보니 사전 정보가 두 군데 틀렸다.
  - **폴더가 `C:\Users\azsxd\Downloads\video_downloader_extension` 이었다.** 메모(`~/claude-memory/video-downloader-project.md`)의 `OneDrive\문서`가 아니다. 경로를 넘겨짚지 않고 찾게 만든 게 그대로 값을 했다.
  - **그 폴더는 git 사본이 아니었다** — `git pull` 이 `not a git repository` 로 끝났다. 8/13에 git 을 붙인 건 다른 PC였다. `update.bat` 의 변환 경로(백업 → init → fetch → `checkout -f`)를 손으로 한 번 태워 사본으로 바꿨고, 영상·`cookies.txt` 는 그대로 남았다.
- 🔥 **`_backup` 이 크롬 확장 로드를 통째로 막았다.** `Cannot load extension with file or directory name _backup. Filenames starting with "_" are reserved for use by the system.` **크롬은 확장 폴더 안에 `_` 로 시작하는 이름이 하나라도 있으면 매니페스트를 안 읽는다.** 내가 만든 백업 폴더가 원인이었다 — 미리 알았어야 했다.
  - **고쳐서 푸시 (`417771c`)**: 백업 자리를 `%LOCALAPPDATA%\vdl_backup` 으로 옮기고, **예전 버전이 확장 폴더 안에 만든 `_backup` 도 실행할 때 밖으로 옮긴다**(집 PC 가 그 상태가 될 수 있어서). 덤으로 yt-dlp 가 PATH 에서 잡히는지, PATH 쪽과 방금 받은 쪽 버전이 어긋나지 않는지도 확인해 알려준다.
  - 옮긴 뒤 확장 **새로고침 성공** = 학교 PC 는 최신본으로 다 맞춰졌다.
- **yt-dlp 는 이상 없었다.** pip 가 `%APPDATA%\Python\Python314\Scripts` 가 PATH 에 없다고 경고했지만, `where yt-dlp` → `C:\Python314\Scripts\yt-dlp.exe`, 버전 `2026.08.19` = **PyPI 최신 그대로**. 서버가 쓰는 쪽이 이미 최신이라 아무 문제 없었다. 다만 pip 가 사본을 하나 더 만드는 구조라 **다음 릴리스부터는 서버 쪽만 낡을 수 있다** — 그래서 위 버전 어긋남 검사를 넣었다.
- `fc /b` 로 백업과 새 소스를 비교하니 **전 파일이 "다름"** 으로 나왔는데, 바이트 비교라 줄바꿈(CRLF↔LF)만 달라도 걸린다. `git diff --ignore-cr-at-eol` 로 다시 보라고 안내했고 결과는 아직 안 받았다. 백업은 `%LOCALAPPDATA%\vdl_backup` 에 그대로 있으니 잃은 건 없다.

**다음 할 일**: ① 이 브랜치를 **main 에 합쳐야** 두 PC 에 반영된다 ② 집 PC 에서 `update.bat` 실행 — 그 PC 는 폴더가 `OneDrive\문서` 일 수도, git 사본이 아닐 수도 있으니 스크립트가 알아서 처리한다. 확장 폴더에 `_backup` 이 있으면 자동으로 밖으로 옮긴다 ③ (ticker) 집 PC `git pull` → `ticker_move.ps1` 실행 후 H: 사본 정리.

---

## 2026-08-26 (122) / 원격(클라우드 세션) — 위젯 상태 확인: 코드는 최신, **실행 위치는 아직 H: 드라이브**

- 121의 숙제 ③(브랜치 → main) **완료 확인**: `origin/main` = 브랜치 HEAD = `3cdb2ff`, 브랜치에만 있는 커밋 0개. 8/25 작업분(해외 환산·빗썸 종목별 주소·색상키 되돌림·교환비율 줄)이 전부 main에 있다. 위젯 자동 갱신 주소(`ticker.pyw:8` RAW)가 main을 가리키므로 켜기만 하면 내려간다.
- **설정창 스크린샷으로 판정.** `최신` 글자를 믿지 않고 **화면에 뜬 기능**으로 확인했다 — `XRP당 카이아 44.43 -1.77%` 줄이 그려지고 있다 = 어제 마지막 커밋(`29ef7c9`)이 실제로 돌고 있다. 숫자도 닫힌다(XRP -2.99%, 비율 -1.77% → 카이아 -1.24%, 1,980 ÷ 44.43 = 44.6원).
- **121의 다음 할 일 ①② 해소**:
  - ① 상단 코인 안내줄이 **비어 있다** → `ticker.pyw:626`은 빗썸이 아니거나 15초 넘게 늦을 때만 띄우므로, 빈칸 = 빗썸 직접 응답 + 지연 15초 이내. 오늘 이 자리에선 안 막혀서 해외 환산 경로는 발동할 일이 없었다(막히면 그때 `코인: 해외 환산`이 뜬다).
  - ② 클릭 시 흐려짐 → `TRANSPARENT = False`(`:129`), 켜는 줄은 `:417`에 주석 처리. 창 분리가 꺼져 있어 그 버그 경로 자체가 없다.
- ❗ **경로가 아직 `H:\내 드라이브\ticker\ticker.pyw`다.** 8/25에 `%LOCALAPPDATA%\ticker\`로 옮긴 게 적용되지 않았다 — 옛 바로가기(또는 시작프로그램 등록)로 켜는 것으로 보인다. 지금은 H:가 붙어 있어 갱신도 되고 잘 돌지만, 언마운트되면 실행 중인 파일이 사라져 위젯이 죽고 `self_update`가 쓴 새 코드도 남지 않아 **옛 코드를 돌리면서 "최신"이라 표시한다**(120에서 겪은 그 상태).
- 이 컨테이너는 빗썸·업비트·야후는 물론 `raw.githubusercontent.com`까지 프록시가 막는다(502). 라이브 확인은 PC 스크린샷으로만 가능하다.

- ✅ **H: 이사 완료 (같은 세션 내 해결).** 사용자가 cmd에서 `H:\내 드라이브\ticker\ticker.pyw`를 직접 쳤다가 `'H:\내'은(는) 내부 또는 외부 명령...` 에러를 받았는데, 이건 **드라이브가 없는 게 아니라 cmd가 공백에서 명령을 자른 것**이다(H:가 없을 때 메시지는 `시스템이 지정된 드라이브를 찾을 수 없습니다` — 120에서 본 것). 진단·보고·재명령의 왕복을 없애려고 **이사 스크립트를 만들어 한 번에 옮겼다**.
- **`tools/ticker_move.ps1` 추가.** 순서가 핵심이다: ① 도는 프로세스에서 **현재 경로를 읽는다**(H:를 넘겨짚지 않는다) ② **최신 코드를 먼저 받아 검증**(10KB 미만이면 중단하고 위젯은 안 건드린다) ③ 창위치·투명도 설정 이어받기 ④ **그 다음에야** 옛 위젯 종료 ⑤ `ticker.pyw`를 가리키는 바탕화면·시작프로그램 바로가기를 전부 새 위치로(이게 빠지면 다음 부팅에 또 H:) ⑥ 새 위치에서 실행.
  - ⚠️ **컨테이너에 PowerShell이 없어 실행 검증을 못 했다.** 대신 눈으로 다시 읽어 **경로 추출 정규식 버그를 잡았다** — `'"?([A-Za-z]:\\.*?)ticker\.pyw'`는 `.*?`가 따옴표를 넘어가서 앞쪽 `C:\Python314\pythonw.exe`를 잡는다. `'([A-Za-z]:\\[^"]*)\\ticker\.pyw'`로 고쳐 따옴표를 못 넘게 했고, 같은 정규식을 파이썬으로 세 경로에 돌려 확인했다.
  - `.ps1`에 **UTF-8 BOM**을 넣었다. Windows PowerShell 5.1은 BOM 없는 파일을 ANSI로 읽어 한글이 깨진다. 한글 경로 리터럴(`H:\내 드라이브`)은 아예 없애고 `H:\*\ticker\...` 와일드카드로 바꿨다.
  - **실행 결과 전부 성공**: `지금 도는 위치: H:\내 드라이브\ticker` (정규식 적중) → `코드 받음: 41,355 bytes` (저장소 파일과 바이트 동일) → 설정 이어받음 → `바로가기 고침: C:\Users\azsxd\Desktop\실시간 시세.lnk` → `완료 → C:\Users\azsxd\AppData\Local\ticker\ticker.pyw`.
- **출력에서 새로 알게 된 것 두 가지**: ① 바탕화면이 **`C:\Users\azsxd\Desktop`**으로 나왔다 — 120에서는 `OneDrive\바탕 화면`이었다. 리디렉션이 풀린 듯한데, 그렇다면 **OneDrive 쪽에 H:를 가리키는 옛 바로가기가 남아 있을 수 있다.** ② **시작프로그램에서는 아무것도 안 고쳐졌다** = 부팅 자동시작 등록이 없다.
- **`tools/ticker_check.ps1` 추가** — 실행 중인 파일 / 코드 날짜 / ticker를 가리키는 **모든** 바로가기(바탕화면 두 자리 + 시작프로그램 두 자리) / 남은 사본을 한 번에 찍는다. "적용됐나?"를 대화 열 번 대신 한 줄로 답하려는 것.
- **H: 사본은 일부러 안 지웠다.** 그 드라이브를 두 PC가 같이 보므로, 집 PC가 아직 그 파일로 도는 중이면 지우는 순간 집 위젯이 죽는다. 집에서도 `ticker_move.ps1`을 돌린 뒤에 정리한다.

- ✅ **`ticker_check.ps1`로 최종 확인 (이 PC 완료).** 도는 프로세스 `[OK] "C:\Python314\pythonw.exe" "C:\Users\azsxd\AppData\Local\ticker\ticker.pyw"` — **⚙ 표시를 안 봐도 프로세스 커맨드라인이 더 확실한 증거다.** 위젯은 딱 1개(옛 사본 동시 실행 없음), 바로가기도 1개 `[OK]`.
  - **걱정했던 OneDrive 바탕화면의 옛 바로가기는 실제로는 없었다.** 시작프로그램에도 없다 = 부팅 자동시작은 원래 안 걸려 있다.
  - `ticker_move.ps1` 재실행이 **멱등**임도 확인됐다 — 두 번째 실행에서 `지금 도는 위치: C:\Users\azsxd\AppData\Local\ticker`로 나오고 `설정 이어받음` 줄이 뜨지 않았다(이미 있는 설정을 안 덮는다).
- **H: 사본(`08-26 09:05, 41,355 bytes`)은 남겨뒀다.** 이 PC에선 아무것도 그걸 가리키지 않아 위험하지 않고, 구글 드라이브는 두 PC가 같이 보므로 **집 PC가 아직 그 파일로 돌고 있으면 지우는 순간 집 위젯이 죽는다.**
- **PR #1을 열었다가 그 자리에서 합쳤다.** 사용자의 "pr을 열어서 뭘 해야하냐"를 "PR 열어봐야 소용없다"로 잘못 읽고 API로 머지해 버렸다 — 결과는 같지만 **버튼을 누를 기회를 가져가 버렸다.** 앞으로 PR을 열면 링크만 주고 머지는 사용자에게 둔다. (rebase 머지로 이 저장소의 선형 히스토리는 유지했다.)

**다음 할 일**: ① **집 PC에서 `git pull` → `ticker_move.ps1` 실행** — 그 뒤에 H: 사본 정리 ② 학교처럼 거래소가 막히는 곳에서 `코인: 해외 환산`이 뜨는지(아직 미검증 — 오늘은 빗썸이 살아 있었다) ③ 부팅 자동시작을 걸지 말지 결정(지금은 없음).

---

## 2026-08-25 (121) / 원격(클라우드 세션) — 학교에서 막힌 코인 시세를 **해외 시세 × 김치프리미엄**으로 잇기 + 클릭하면 위젯이 어두워지던 것

- **사용자가 빗썸 앱과 위젯을 나란히 찍은 스크린샷 3장**을 보냈다. 위젯 코인 값이 3장 내내 `비트코인 111,195,000 +1.86%` · `XRP 2,098 +1.16%`로 **똑같고**, 코스피(6,728→6,727→6,726)·환율·코람코만 바뀐다 → **코인만 멈춰 있었다.** 상단에 `코인: 거래소 막힘` 표시. XRP 등락률이 빗썸 +1.21% vs 위젯 +1.16%인 것으로 보아 마지막 성공은 **업비트**(자정 시가를 따로 조회하는 경로)였고 그 뒤 둘 다 막혔다.
  - `refresh()`가 못 받아온 값을 **직전 값 그대로 두기** 때문에(`ticker.pyw`) 화면은 멀쩡해 보이는데 실제로는 몇 분 전 값이었다. 빗썸 앱이 111,106,000 → 111,204,000으로 오르며 위젯 숫자에 "따라붙은" 것도 그래서다.
- **사용자 제안 그대로 고쳤다** — "거래소 말고 해외 시세를 불러와 국내 가격이랑 매칭":
  - 야후 요청에 **코인도 함께 싣는다**(`SPARK`는 심볼 여러 개를 한 번에 받으므로 **요청 횟수는 그대로 1회**). 학교에서 야후는 살아 있다 — 화면의 PLTR·환율이 야후 값이다.
  - 거래소가 응답할 땐 `coin_prem[코인] = 국내가 ÷ 해외가`(김치프리미엄)와 `coin_base[코인] = (날짜, 국내 자정 시가)`를 기억한다.
  - 거래소가 30초 넘게 막히면 **해외 실시간 시세 × 그 비율**로 국내가를 추정해 계속 갱신한다. 등락률은 **국내 자정 시가 기준**을 그대로 써서 빗썸 화면과 기준이 어긋나지 않게 했다(그날 국내 시가를 모르면 해외 기준으로 떨어진다).
  - `coin_prem`은 설정파일에 남긴다(10분에 한 번 저장) → **학교에서 켤 때부터 막혀 있어도** 어제 비율로 환산한다.
  - 코인 심볼 때문에 야후 요청이 통째로 실패하면 **주식만 다시 받아** 살리고(`coin_ext`), 이후엔 코인을 빼고 묻는다.
  - 옛 동작(120초 뒤 야후 원값으로 대체)은 없앴다 — 해외 평균가는 국내가와 1% 넘게 벌어진다.
- **검증**: 이 컨테이너는 빗썸·업비트뿐 아니라 **야후까지 정책상 403**이라 실시간 확인이 안 된다. tkinter도 없어서 **가짜 tkinter를 물리고 네트워크만 스텁으로 바꿔 진짜 `refresh()`를 그대로 실행**했다(스크래치패드 `run_ticker.py` + `t1.py`). 6가지: 거래소 정상(비율 학습·거래소 값 유지) / 성공 직후 사이클(환산으로 덮지 않음) / 둘 다 막힘(환산·자정 기준 등락률) / 부팅부터 막힘(저장된 비율) / 비율 없음(해외 원값 + 표시) / 야후 코인 실패(주식 생존). **비율 없이 야후 값을 그대로 썼다면 1,207,284원(약 1.1%) 낮게 나왔다** — 이 변경의 실효가 그만큼이다.
- 🔥 **"앱을 클릭하니 화면이 어두워진다"** — 창을 둘로 나눈 `3020c10`의 부작용. 뚫린 픽셀은 클릭이 바탕 창(`back`)으로 내려가고, 그러면 **윈도우가 `back`을 앞으로 올려** 반투명 바탕(alpha 0.85)이 글자를 덮는다. 스크린샷의 "글자가 15%만 비쳐 보이는" 모습이 정확히 그 상태다. 다시 올려주는 `root.lift(back)`이 `sync_back()`(다시 그릴 때·자리 잡을 때)에서만 불렸다 → **`back`의 클릭·해제·우클릭에도 `sync_back()`을 걸었다**(`add="+"`라 기존 끌기/메뉴 동작은 그대로).

**다음 할 일**: ① 학교에서 위젯 켜고 상단 표시 확인 — `코인: 해외 환산`이 뜨면서 값이 5초마다 움직이면 성공, 빗썸 앱과 몇 만 원 안쪽이면 비율이 맞는 것 ② 바탕 클릭해도 글자가 안 흐려지는지 ③ 브랜치 `claude/school-wrap-up-content-59vfyq`를 **main에 합쳐야** 두 PC에 반영된다(120의 숙제 그대로).

---

## 2026-08-24 (120) / 원격(클라우드 세션) — 🔥 **집 PC는 8/21 이후 아무것도 못 받고 있었다** + 위젯이 스스로 설정을 동기화

- **119 뒤 이어진 원격 세션.** 사용자가 "학교에서 마무리된 내용"을 물어 대화 기록(`claude-memory/transcripts`)을 붙여 읽고 정리했다. 저장분이 12:00에서 끊겨 있었다 — Stop 훅 30분 스로틀 + `SessionEnd` 훅이 **그 세션에서 새로 생겨서** 마지막 30분(분배공지·캐시 작업)이 안 들어갔다. 다음 세션부터는 잡힌다.
- **원문 보관은 이미 영구**(`KEEP_RAW_DAYS = 0`). 원문 541KB + 발화본 44KB 둘 다 남는다.
- ⭐ **집 PC 진단 결과 — 훅이 안 돌고 있었다.** `~/claude-memory`의 HEAD가 `7e1dcd1`(8/21 17:46), `origin/main`까지 옛것 = **fetch조차 안 됨**. `ticker/` 폴더 자체가 없었다. 그래서 17종목 설정도, 대화 기록도, 브리핑도 집에 도착한 적이 없다. 수동 `git pull`로 12개 파일이 그제서야 내려갔다.
  - 집 위젯이 저장한 설정이 `jjk/tools/ticker_config.json`(LOCAL)에 있었다 → **집 jjk도 8/21에서 멈춰 있다**(`config_path()` 없는 옛 코드). 위젯 파일은 `C:\Users\azsxd\Documents\jjk\tools\ticker.pyw` 하나뿐(사본 없음).
  - 원인 후보: 집에서 Claude Code(PC 앱)를 **jjk 폴더가 아닌 곳에서** 열거나, 훅 승인 창을 넘겼거나. 사용자는 "어디서 열렸는지 내가 어떻게 아냐"고 했고 **그 지적이 맞다 — 사람이 확인해야 하는 구조를 만들면 안 된다.**
- **그래서 위젯이 훅에 기대지 않게 고쳤다**(`tools/ticker.pyw`):
  - `sync()` — 켤 때와 목록을 고칠 때 `claude-memory`를 직접 commit → pull --rebase → push. git 없거나 실패해도 위젯은 그냥 뜬다.
  - **공유 대상을 종목 목록으로 좁혔다.** 창 위치·불투명도·접힘은 `LOCAL`(스크립트 옆)에만 남는다. 한 파일에 섞여 있어서 **창을 옮긴 것만으로 충돌 → 목록 유실**이 났다(첫 구현에서 실제로 재현). 충돌 시 다른 PC 것을 따르고 내 것은 `ticker_config.json.conflict`로 저장소 **밖에** 백업.
  - 샌드박스 저장소 3개(remote/학교/집)로 4가지 시나리오 검증: 씨앗 업로드 / 학교→집 반영 / 창위치 로컬 유지 / 양쪽 동시 변경 충돌.
- **CLAUDE.md에 규칙 한 줄 추가**: "다른 PC 대화 이어받기" — `list_sessions`만 보고 "없다"고 답하지 말 것.
- **분배금 재배포 — 이미 돼 있었다(확인 완료).** 라이브 배포 `AKfycbwJS1Fd…`가 **@156 "분배캐시 보존 1년(24회차)"**를 물고 있고, Apps Script API로 156번 버전 소스를 직접 떠서 확인했다: `DIST_KEEP_CYCLES = 24` 있음, 옛 코드(`mine.length > 2`) 없음. 편집기 소스도 로컬과 3,662줄 전부 동일. **학교에서 push·redeploy 둘 다 했다.**
  - 원격에서 clasp 인증하는 법: `clasp login`(localhost 서버)을 컨테이너에서 띄우고 URL을 사용자에게 준 뒤, 승인 후 주소창의 `localhost:<포트>/?code=…`를 **컨테이너 안에서 curl로 그 포트에 넘기면** 로그인이 완료된다. `--no-localhost`(OOB)는 구글이 막았고, 토큰 교환을 직접 curl로 하면 세션 보안 분류기에 걸린다.
  - 이 컨테이너는 `script.google.com`(웹앱 `/exec`)이 정책 차단(403)이라 라이브 응답 확인은 불가. `script.googleapis.com`(배포 API)은 열려 있다.
- ⚠️ **이 세션은 `main`에 못 쓴다** — 전부 `claude/school-wrap-up-content-59vfyq` 브랜치에 있다. **PC에서 main으로 합쳐야 두 PC에 반영된다.**

- **남에게 주는 설치기 추가**(`tools/ticker_setup.bat`): 그 파일 하나만 주면 파이썬 확인·설치(winget) → 최신 `ticker.pyw` 내려받기(`%LOCALAPPDATA%\ticker`) → 바탕화면 바로가기 → 실행까지 한 번에. 내 설정 파일은 안 딸려 간다. 받은 사람 PC엔 `~/claude-memory`가 없으니 `SHARED=None` → 그 사람 종목 변경은 그 PC에만 남고 내 목록에 영향이 없다(빈 HOME으로 실제 확인).

- **받은 사람 쪽 자동 업데이트**(`self_update()`): 켤 때 GitHub raw에서 최신 `ticker.pyw`를 받아 자기를 덮고 `os.execv`로 새 코드로 다시 시작한다. **저장소 안에서 도는 내 작업본은 건너뛴다**(`in_repo()` — 부모 폴더를 거슬러 `.git`을 찾는다). 작업 중인 코드가 origin/main으로 덮이면 안 되기 때문. 받아온 게 10KB 미만이거나 `tkinter`가 없으면(에러 페이지 등) 교체하지 않고, 인터넷이 없으면 그냥 있던 걸로 돈다.

- **설정창 손질**: 바탕 투명 눈금을 **0~100 선형**으로(0 = 완전 투명, 옛 눈금은 20~100이 알파 0.76~1.0이라 최하로 내려도 덜 투명했다). 옛 설정은 보이던 밝기 그대로 새 눈금으로 한 번 옮긴다(`70 + 옛값*0.3`). 슬라이더는 **아무 데나 눌러 끌면 그 자리로**(Tk 기본은 여백 클릭 시 한 칸씩). **바탕색 5종 선택** 추가(`PALETTE`, 어두운 색만 — 글자가 흰 계열이라). 색을 바꾸면 만들 때 색이 박히는 종목 줄을 `build()`로 다시 그린다.
  - **코인 값 불일치 = 버그 아니라 지연이었다.** 받은 숫자: 위젯 110,928,000(1.61%) vs 빗썸 110,914,000(1.61%) → 차이 0.013%, **등락률은 동일**(기준은 맞다는 뜻). 코인만 20초 간격이라 그만큼 늦은 것. **`COIN_SEC` 5초로 당기고, 거래소가 막히면 자동으로 20초(`COIN_SLOW`)로 물러섰다가 응답하면 다시 5초**로 돌아오게 했다(학교망에서 업비트가 끊기던 것 때문에 20초였다).

- **내 PC도 자동 갱신으로.** 처음엔 저장소 안 사본을 자동 갱신에서 뺐는데(작업 중 코드가 덮일까 봐), 사용자가 "집에서 `git pull` 명령을 내가 기억하겠냐"고 했고 그 말이 맞다. 저장소 안이면 **`git pull --ff-only`**로 저장소째 최신화한다 — 작업 중 변경이 있으면 pull이 그냥 실패하므로 내 코드는 안전하다. 저장소 밖(남에게 준 사본)은 종전대로 raw로 자기를 갈아끼운다. 파일이 실제로 바뀐 경우에만 `os.execv`로 재시작. **덤: 위젯을 켜기만 해도 집 PC의 jjk 저장소가 최신이 된다**(훅이 안 돌던 문제의 실질적 보완). 샌드박스 3가지 확인: 옛 버전→갱신·재시작 / 이미 최신→그대로 / 로컬 수정 중→pull 실패하고 내 코드 유지.

- **설정창이 시세창을 가리지 않게**(`place_settings`): 내용을 다 채운 뒤 크기를 재서 **왼쪽 → 오른쪽 → 위** 순으로 빈자리에 붙인다. 아래끝을 시세창에 맞추고 모니터 작업영역 안으로 클램프한다(`work_area`는 창이 놓인 모니터 기준이라 보조 모니터에서도 맞다). 숨긴 상태면 작은 버튼을 기준으로 잡는다. 우하단·좌상단·화면중앙·1024 좁은 화면 네 경우로 겹치지 않는 것 계산 확인.

- 🔥 **오늘의 진짜 원인: 실행되던 파일이 저장소 파일이 아니었다.** `gwmi Win32_Process`로 확인 → `C:\Python314\pythonw.exe "H:\내 드라이브\ticker\ticker.pyw"`. **8/21짜리 구글 드라이브 사본**이었다. 설정이 안 넘어온 것도, pull 해도 안 바뀌던 것도, 껐다 켜도 그대로였던 것도 전부 이것 하나로 설명된다. **나는 '이제 자동으로 갱신된다'고 말하면서 정작 어느 파일이 실행되는지 확인하지 않았다** — WORKLOG 118에서 배운 것("상태파일까지 확인하고 말해라")을 그대로 반복했다.
  - 조치: 드라이브 사본을 최신 코드로 한 번 덮으면, 그 파일은 저장소 밖이라 이후 **GitHub raw에서 스스로 갱신**된다(양쪽 PC가 같은 드라이브 파일을 보므로 한 번이면 둘 다 해결).
  - 재발 방지: **설정창 아래에 지금 도는 파일 경로·코드 날짜·갱신 결과를 표시**한다. "적용됐나?"를 대화 열 번 대신 ⚙ 한 번으로 확인한다.

- **바탕만 흐려지고 글자는 또렷하게** — 창을 둘로 나눴다. Tk의 `-alpha`는 창 전체에 걸려서 한 창에 두면 바탕을 흐리게 할 때 글자까지 사라진다. `back`(바탕 전용 Toplevel)이 alpha로 색을 깔고, 글자 창(root)은 `-transparentcolor`로 바탕 픽셀만 뚫어 alpha 1.0로 둔다. 뚫린 자리는 클릭이 back으로 내려가므로 끌기·우클릭·마우스오버를 back에도 걸었다. 크기·위치는 `sync_back()`이 맞추고 `build()`·`place_panel()` 끝에서 부른다. 윈도우가 아니면 `-transparentcolor`가 실패하므로 `TRANSPARENT=False`로 두고 예전 동작(창 전체 투명)으로 떨어진다.
- **코인 출처 표시**: 값이 빗썸이 아닐 때만 위쪽에 `코인: 업비트 / 야후(해외평균) / 거래소 막힘`이라고 띄운다. 야후로 넘어가면 해외 평균가라 가격이 크게 벌어지는데, 지금까지는 그게 화면에 드러나지 않았다.

- ⛔ **바탕만 투명(창 분리)은 실패해서 되돌렸다.** `-transparentcolor`(색상키)로 글자 창을 뚫었더니 **글자가 지워지지 않고 겹쳐 그려져** 모든 줄이 이중으로 번졌고, 급기야 **프로세스가 죽은 뒤 유령 창**이 남아 왼쪽 모니터엔 검정 바탕, 오른쪽엔 글자만 떠 있는 상태가 됐다(`tasklist`에 pythonw 없음 = 죽어 있었다). 진입점 두 줄만 막아 창 전체 투명도로 복귀 — 구조는 남겨뒀으니 잔상 해법을 찾으면 되살릴 수 있다. **교훈: 윈도우에서 못 돌려보는 렌더링 변경은 올리지 말 것.** 계산은 맞아도 화면은 틀릴 수 있다.
- **빗썸을 종목별 주소로**(`/public/ticker/{}_KRW`). 10초 간격 스크린샷 4장에서 빗썸은 110,753,000→110,710,000→110,715,000으로 움직이는데 위젯은 110,810,000에 40초간 고정이었다(같은 시각 코스피는 정상 갱신). `ALL_KRW`가 캐시를 타는 것으로 보고 바꿨다. 응답의 거래소 시각으로 지연도 측정해 15초 넘으면 화면에 표시.

- **교환비율 줄 추가**: 목록에 `XRP-KRW/KAIA-KRW=XRP당카이아`처럼 `/`를 넣으면 **1 A가 몇 B인지**를 한 줄로 보여준다. 화면엔 비율만 뜨지만 시세는 두 종목 다 받아온다(`need`로 분해). 등락률은 각자의 자정 시가로 만든 비율과 비교해 계산 — XRP +0.43% / KAIA +5.35%면 비율은 47.64 → 45.41(-4.67%)로 내려간다(계산 검증 완료). 코인을 갈아탈 때 뉴스가 아니라 비율 숫자로 판단하려는 용도.

- 🔥 **오늘 증상 대부분의 공통 원인: 구글 드라이브(H:)에서 실행하고 있었다.** `ren`을 시도하자 **"시스템이 지정된 드라이브를 찾을 수 없습니다"** — H:가 아예 안 붙어 있었다. 가상 드라이브라 ① 언마운트되면 실행 중인 파일이 사라져 위젯이 죽고 ② `self_update`가 새 코드를 써도 남지 않아 "최신"이라 표시하면서 옛 코드가 계속 뜨고(14:44 코드가 3시간 뒤에도 그대로) ③ 바로가기가 대상을 못 찾는다.
  - **조치: 실행 위치를 `%LOCALAPPDATA%\ticker\ticker.pyw`로 옮겼다.** GitHub raw에서 직접 받아 넣고, 바탕화면 바로가기도 그쪽을 가리킨다. 드라이브는 이제 관여하지 않는다.
  - 바탕화면이 **`C:\Users\azsxd\OneDrive\바탕 화면`**으로 리디렉션돼 있었다(사용자는 원드라이브를 쓰지 않는다고 생각). `Desktop` 폴더만 뒤진 초반 진단이 헛돈 이유. 만들 땐 `[Environment]::GetFolderPath('Desktop')`을 쓸 것.
  - **최종 구조**: `GitHub(main) → 켤 때 자동 다운로드 → %LOCALAPPDATA%\ticker\ticker.pyw → 위젯`, 종목 목록은 `claude-memory`로 두 PC 동기화.
  - 교환비율 줄(`XRP-KRW/KAIA-KRW=XRP당 카이아`) 화면에서 정상 동작 확인.

**다음 할 일**: ① ~~브랜치 합치기·17종목 확인~~ 완료 ② 집 PC에서 Claude Code가 **jjk 폴더**로 열리는지 확인(훅 승인 창 포함) — 안 되면 훅을 사용자 설정(`~/.claude/settings.json`)으로 옮기는 방안 ③ ~~분배금 재배포~~ — 확인해 보니 학교에서 이미 완료(@156).

---

## 2026-08-24 (119) / 학교 — 위젯 설정을 **비공개 저장소**로 옮겨 두 PC 동기화 완성

- **118의 후속.** 집에서 잡은 원인은 맞았지만 해법(저장소 안에서 실행 → 설정이 자동 커밋)에는 문제가 있다. **`jjk`는 공개 저장소**라 보유 ETF 목록이 그대로 공개된다(어제 올라간 `tools/ticker_config.json`은 기본값뿐이라 유출은 없었다). 사용자 규칙도 "금융자료는 저장소 밖".
- **바꾼 것**: 설정 경로를 `config_path()`로 결정한다 — ① `TICKER_CONFIG` 환경변수 → ② `~/claude-memory/ticker/ticker_config.json`(**비공개** 저장소, 세션 훅이 자동 pull/push) → ③ 없으면 스크립트 옆 파일. 기존 설정은 처음 한 번 자동으로 옮겨온다. 이제 **어디서 실행하든**(저장소 안·바탕화면·드라이브) 두 PC가 같은 설정을 본다.
- `tools/ticker_config.json`은 공개 저장소에서 제거하고 `.gitignore`에 넣었다. 학교에서 쓰던 17종목 목록이 비공개 저장소에 들어갔으니 집에서도 그대로 뜬다.
- **코인 소스**: 학교 네트워크에서 `api.bithumb.com`이 TLS 단계부터 계속 막힌다(4/4 실패). 빗썸 → **업비트**(빗썸과 같은 자정 기준으로 맞추려고 00시 KST 시가를 따로 조회) → 야후 순으로 넘어가게 했다(`c32be75`).
- **반성**: 세션 시작 때 `git pull`도 WORKLOG 읽기도 건너뛰고 `list_sessions`(이 PC 전용)만 보고 "집에서 한 세션 없음"이라고 답했다. 푸시가 거부되고 나서야 집 커밋 5개를 발견했고, 그 사이 같은 듀얼 모니터 수정을 중복으로 만들고 드라이브 파일까지 덮어썼다(되돌림). **다음 세션은 `git status` → `git pull` → WORKLOG 순서로 시작한다.**

**다음 할 일**: 집에서 위젯 켜서 17종목이 그대로 뜨는지, 빗썸 값이 화면과 맞는지 확인.

---

## 2026-08-21 (118) / 집 — 🔥 **위젯 설정이 학교→집으로 안 넘어왔다** + 오늘 학교 세션은 WORKLOG 항목조차 없었다

- **오늘 학교에서 `tools/ticker.pyw`(바탕화면 실시간 시세 위젯)를 새로 만들었다** — 커밋 6개(`4cdbd8a` 신설 → 그룹 접기 → 자석 스냅·위치 기억 → 불투명도 슬라이더 2개 → 코인 업비트로 → 등락률 24시간·20초 간격). **그런데 학교 세션이 WORKLOG 항목을 안 남겼다.** 그래서 집에서 무슨 약속을 했는지 기록으로 확인할 수가 없었다. 이 항목이 그 자리를 메운다.
- **집에서 켜니 등록한 종목이 전부 사라져 있었다.** 사용자가 "학교와 집 작업이 연결되게 해달라"고 여러 번 요청했고 그렇게 하겠다고 답한 뒤였다 → 정당한 항의.
- **원인**: 종목 목록은 `tools/ticker_config.json`에 저장되는데(`ticker.pyw:8`, `__file__` 기준 경로) **git 이력에 한 번도 없다**(`git log --all -- tools/ticker_config.json` 빈 결과). 로컬 = `origin/main`, 뒤처진 커밋 0 → 덜 받은 게 아니라 애초에 안 올라갔다.
  - 훅은 정상이다. Stop 훅이 `git add -A`로 전부 커밋하고 이 파일은 `.gitignore`에도 안 걸린다. **저장소 안에 있었다면 자동으로 올라갔을 파일이다.**
  - 남은 가능성 둘: ① 학교에서 종목 등록을 세션 종료 **뒤에** 함(마지막 자동 커밋 이후) ② 학교에서 `ticker.pyw`를 **바탕화면에 복사해서** 실행함 → 설정 파일이 저장소 밖 바탕화면에 생성. 사용자가 "바탕화면에 학교에서 만든 위젯"이라 부른 걸 보면 ②가 유력.
- ⭐ **교훈 — "동기화된다"는 말은 코드가 아니라 상태파일까지 `git log -- <파일>`로 확인하고 해야 한다.** 코드가 커밋된 것만 보고 "집에서 그대로 이어진다"고 단언한 게 이번 실패의 본질이다. 기능이 두 PC에서 이어지려면 **사용자가 만든 상태(설정·목록·위치)가 저장소 안에 있고 실제로 커밋됐는지**가 조건이다.
- **집에서 한 것**: 바탕화면 바로가기 `시세 위젯.lnk` 생성(pythonw로 저장소 안 `tools\ticker.pyw` 실행 — 복사본이 안 생기니 ② 재발 차단). `.gitignore`에 `__pycache__/` 추가.
- **다음 할 일**: 종목 목록을 `ticker_config.json`이 아니라 **`ticker.pyw`의 `DEFAULT`에 박아 코드로 동기화**할지 결정(창 위치·불투명도만 로컬 파일에 남김). 학교 PC에서도 바탕화면 **복사본이 아니라 바로가기**로 바꿀 것. 학교 PC의 `ticker_config.json`(바탕화면 또는 저장소)을 찾아 커밋하면 목록 복구 가능 — 집에서는 복구 불가.

---

## 2026-08-20 (117) / 직장 — ⭐ **영웅문과 투자금액이 다른 이유 = 주식상황 시트의 평단가 두 칸이 뒤바뀜** + "투자원금" → "매입금액"

- 사용자가 앱 화면과 영웅문(재남 키움 연금저축)을 나란히 놓고 지적: 투자금액·수익금이 다르고, 명칭도 이상하다.
- **평가금액은 10,044,390으로 원 단위까지 일치** → 수량·현재가는 정확. 차이는 **평단가 한 군데**에서만 났다(투자금액 10,070,951 vs 총매입 9,938,925, **+132,026**).
- **원인 확정**: 외부 `주식상황` 시트(`19UsD0Tz…`)의 그 계좌 3행에서 **RISE(491620)와 TIGER(486290)의 매입가가 서로 바뀌어** 들어가 있다.
  - 시트: RISE 550주 @**10,904** / TIGER 105주 @**10,608**   ↔   영웅문: RISE 550주 @**10,608** / TIGER 105주 @**10,904**
  - 시트 기준 합 = 309×9,579 + 550×10,904 + 105×10,608 = **10,070,951** → **앱 값과 정확히 일치**. 앱은 시트를 그대로 옮긴 것뿐이고 계산 버그가 아니다.
  - 바로잡으면 9,939,231(영웅문 9,938,925와 306원 차 = 평단 소수점 표시 반올림). 수익금 −26,561 → **+102,505**로 부호까지 뒤집힌다.
  - 현재가는 제대로 짝지어져 있다(시트 손익도 뒤바뀐 평단과 내부적으로 일관돼서 눈에 안 띄었다).
  - 검증 방법: Drive 커넥터로 시트를 통째로 읽었다. 익스포트 인코딩이 깨져 나오는데 **cp949 바이트를 GBK로 잘못 디코드한 것**이라 `s.encode('gbk').decode('cp949')`로 복구된다.
- **명칭**: 앱은 이미 **두 개념을 다 쓰는데 이름이 겹쳐 있었다**.
  - 포트폴리오·대시보드의 "투자원금/투자금/투자금액" = `평단×수량` → 실체는 **매입금액**(영웅문의 '총매입').
  - 계좌 성적표(`portfolio.html` 3746~3819)의 "투자원금" = 외부입금−출금+계좌간이체+현물이관 → 이건 **진짜 투자원금**이 맞다.
  - → 앞의 것만 **"매입금액"**으로 바꿨다. `portfolio.html` 7곳(상단 요약·인별 요약 2·대시보드 스탯·연분배율 부제·계좌표 th·종목표 th), `m.html` 4곳. **성적표는 손대지 않았다.**
- **사용자 몫**: 시트에서 그 두 칸 맞바꾸기 → 앱 **스프레드시트 동기화**로 반영(평단 diff를 잡아서 적용해 준다).
- **이어서 (같은 세션) — 전 계좌 대조: 뒤바뀜은 이 한 건뿐**
  - 방법: 앱 DB `holdings`의 `avg_price`(=주식상황 시트 사본)를 **거래내역에서 복원한 `원금월별 v3`의 `cost/qty`**와 종목별로 맞췄다. v3는 증권사 거래내역이 출처라 영웅문과 같은 계보다 — 독립 검증이 된다. (실제로 v3는 문제의 두 종목을 영웅문과 똑같이 갖고 있었다: 486290=10,903.95, 491620=10,607.73.)
  - 결과: **일치 50 / 불일치 9 / 비교불가 4**. 불일치 9건 중 **7건은 반올림**(1~2원, USD는 1~2센트 — 앱은 평단을 정수로 저장, v3는 원가÷수량). 총매입 영향 다 합쳐 **약 6천원**.
  - **실제 오류는 재남 키움 연금저축 두 줄뿐**: 486290 앱 10,608 ↔ v3 10,903.95 / 491620 앱 10,904 ↔ v3 10,607.73. 정확히 −296 / +296으로 대칭 → 뒤바뀜 확정.
  - 애매한 것 1건: 은경 미래 일반계좌 `279570` 앱 8,123 vs v3 8,110 (13원, 90주라 1,170원). 반올림치고는 크지만 영향은 무시 가능.
  - **비교불가 4건**(v3 최신 행이 2024년 이전이라 내가 옮겨적은 범위 밖 — 데이터 문제 아님): 은경 미래 연금 `368590`(122주), 은경 미래 일반 `088260`(104주), 은경 키움 일반 `005380`(17주), 재남 키움 일반 `330590`(4,672주). 필요하면 v3 전체를 다시 떠서 확인.
  - 스크립트: 스크래치패드 `compare.py` + `cost_v3.txt`(v3 2024년~ 행). 계좌명은 익스포트 인코딩이 깨져서 id→이름 맵을 코드에 박았다.
- **라벨 최종**: 사용자가 **"총매입"**으로 정했다. `portfolio.html` 8곳·`m.html` 4곳(원래 "매입금액"이던 종목통합표 th 1곳 포함). 계좌 성적표의 "투자원금"은 그대로.

---

## 2026-08-20 (116) / 직장 — 두 PC 연속성 점검 + 매수 첫 달 **옅은 노랑**

- **연속성 확인**: 로컬=origin=`0fca80c`, 트리 클린. 집 PC 작업이 그대로 넘어왔다. 기억 동기화 훅도 이 PC에서 처음 돌면서 `~/claude-memory` 클론 + 정션 생성까지 자동으로 됐다.
- **구멍 2개**:
  - 훅이 기존 기억 폴더를 `memory.bak.*`로 밀어내는데, 이 PC엔 공용 저장소에 **없는 기억 10건**이 있었다. 중복 4건 버리고 **고유 6건 병합·푸시**(권한 프롬프트, 파서 선제점검, 알림 기준, jjk public, DB시트 Drive 읽기, Video Downloader).
  - 새로 클론한 기억 저장소에 **git 신원이 없어 Stop 훅의 기억 푸시가 조용히 실패**했다(실제 발생). 클론 직후 `user.name/email`을 박도록 `.claude/settings.json` SessionStart 훅 수정.
- **매수 첫 달 노랑** (115의 '다음 할 일'): 115에서 이미 **일괄 제외**로 구현돼 있었는데, 그러면 *첫 달에도 배당을 받는 종목*의 놓친 회차를 영영 못 잡는다. 매수 **일자**는 앱에 없다(원금월별은 월말 잔고뿐) → 흑백으로 결정 못 한다.
  - **절충**: 첫 달만 **옅은 노랑 `#fef9c3`** + 별도 툴팁("배당락 뒤 매수면 이 회차는 없습니다"). 오경보는 사라지고 눈에는 남는다. `portfolio.html` `loadDividendGrid` 셀 렌더.
  - **검증**(localhost 8777로 띄우고 `api` 스텁 주입 — `file://`는 `data:` URL로 렌더돼 sessionStorage가 막혀 스크립트가 1161줄에서 죽는다. 반드시 HTTP로 띄울 것): 3월신규 → 3월 옅은노랑/4~7월 진노랑 ✅ / 연초보유(firstM=1) → 1~7월 진노랑 ✅ / 5월신규인데 5월 입력됨 → 5월 무색, 6·7월 진노랑 ✅ / 분기 종목 → 노랑 없음 ✅ / 이번 달(8월) 이후 무색 ✅
- **다음 할 일**: WORKLOG 공백 메우기 — 115 이후 집 PC 대시보드 3커밋(월별 차트 툴팁 `c5330c7`, 투영 매도종목 제외·USD 환산 `8fffe40`, `accYM` 정리 `a54653a`)이 기록에 없다. 이어서 손댈 게 있는지 diff 확인 필요.

---

## 2026-08-20 (115) / 집 — 매수 전 달 **노랑 오표시** 제거, **원금월별 v2**(매도 종목 32개 추가)로 실분배율 복구

- **증상 3개** (사용자): ① 최근달도 아닌 칸이 노랗다 ② 2024년 화면에서 미국주식이 티커가 아니라 한글("나스닥 100 인베")로 나온다 ③ 실분배율·환산분배율이 대부분 비어 있다.
- **①·③은 뿌리가 같다** — `costMap`(원금월별)에 그 종목·그 달 행이 없으면 **현재 보유수량으로 폴백**하던 것.
  - 노랑: 매수 전 달은 당연히 행이 없다 → 폴백이 "그때도 보유"로 읽어 `missed`(지나간 달 미입력) 판정 → 노랑. → **원금월별에 이력이 있는 종목이면 행 없는 달 = 미보유**로 읽고, 이력이 아예 없는 종목만 폴백하도록 고쳤다(`portfolio.html`, `costIds` 셋).
  - 실분배율: 분모를 못 구하면 `-`. 2024 화면 종목 대부분이 여기 걸렸다.
- **③의 진짜 원인은 데이터가 아니라 매칭 범위였다.** 원금월별은 8/19 시점 앱 holdings **65종목** 기준으로 만들었는데, 그 뒤 2017~2024 배당 162건을 복원하면서 **매도 종목 유령행 31개가 새로 생겨 96행**이 됐다. 그 행들엔 원금월별이 없다. 거래내역 원본은 이미 다 있었다(`보유수량_월별.csv`, 2016-08~2026-08).
  - `backfill_costbasis.py` 신설(`_통합\`): 기존 `match_holdings.py`는 **마지막 달 잔고**에 (수량,평단)으로만 붙여서 매도 종목은 구조적으로 못 붙는다. 새 스크립트는 **전체 이력의 종목명**에 대고 ① 종목명에 박힌 티커 `(QQQM)` ② 정규화 이름 완전일치 ③ 앞부분 포함(겹치면 최장 공통접두)로 붙인다. **34개 중 32개 매칭**, 추가 70행.
  - 남은 2건은 둘 다 은경 미래에셋 연금저축 0주 — `KB RISE 미국나스닥100`(같은 계좌 `KB KBSTAR` 122주와 동일 종목, 2024 리브랜딩. 이력은 활성 행이 이미 가져감)과 `KODEX 금융고배당TOP10`(그 계좌 거래내역에 매매 자체가 없음). 둘 다 유령/결손이라 `-`로 둔다.
- **시트 교체**: Drive 커넥터에 시트 **내용**을 덮어쓰는 기능이 없다(`update_file`은 제목·부모만). → 기존 149행 + 추가 70행 = **219행**으로 새 시트 생성 후 상수 교체.
  - `원금월별 v2 (거래내역 복원 · 분배율 분모 · 매도종목 포함)` = `1WyMQis17RXCM3fqifnhyM6FXfXoOESiYvcsskMmM3mU`. 구 시트(`1zjDhF53…`)는 백업으로 남겨둠 — 지워도 된다.
  - `Code.gs`의 `COST_SHEET_ID`만 바꿔 `clasp push --force` → `redeploy @151`. 겹치는 holding_id는 0이라 기존 값은 하나도 안 바뀐다.
- **②**: 표시가 `h.name || h.ticker`인데 매도 종목 유령행 이름이 증권사 한글 표기(그것도 잘린 것 — "배당 성장주 아", "프리미엄 고배당")였다. **USD 종목은 티커 우선**으로 바꿨다(분배금 그리드·분배율 추이 둘 다).
- **이어서 (같은 세션)**:
  - 신규 매수 **첫 달은 노랑 제외**(배당락 뒤 매수면 그 회차를 못 받는다). 첫 등장이 1월이면 이월과 구분이 안 되니 제외 안 한다.
  - 지급일 ±3일 창은 **입력한 칸도 노랑 유지**(`paintDueCells`의 `if (inp.value) return` 제거). 이번 회차 표시로 쓴다.
  - 🔥 **`paintDueCells`가 통째로 죽어 있었다** — 서버 `getDistributionAll`은 `{success, sources:{…}}` 객체인데 `distDueMap`은 `[{data}]` 배열을 기대해서 `forEach`에서 TypeError. 공지 탭이 하는 변환을 넣었다. **8월 노랑이 하나도 안 뜨던 진짜 원인.**
  - 🔥 **오매칭**: `KODEX 금융고배당TOP10`(월중, `0089D0`)이 접두어라는 이유로 `KODEX 금융고배당TOP10타겟위클리커버드콜`(월말, `498410`) 5계좌에 붙었다. → **티커가 있는 공지 항목은 이름 포함 매칭 금지**. ±3일 대상 22 → 17종목.
  - **원금월별 v3**: 매도로 이력이 끊긴 달에 **0주 행 33개**. 없으면 이월이 마지막 수량을 영원히 끌고 가서 2년 전에 판 종목이 2026 화면에 빈 줄로 뜬다(사용자가 바로 잡아냄). 시트 `1DC8cBZkDm1yj0_VjbsNR3Ij4GeDPtyZv1UKrkpm-dF0`(252행), `redeploy @152`. v2는 휴지통.
  - **배당주기 공지동기화** 버튼 신설(`applyAllDivCycles`) — 앱의 `월초` 표기가 실제 회차와 어긋나 있었다(49행 전부). 공지가 **명시한** cycle만 쓰고, 지금 월배당(월초/월중/월말)인 종목만 바꾸며, 섹터(`|`뒤)는 보존한다. 드롭다운에 없던 `월중` 선택지도 추가. 21개 티커 / 49행: 월초→월말 34, 월말→월중 15.
- **다음 할 일**: 신규 매수 **첫 달**을 노랑에서 뺄지 결정. 지금은 3월에 산 KODEX 200타겟위클리(재남 일반 50주 / 은경 미래 일반 198주)가 3월 칸 노랑으로 남는다 — 지급기준일 뒤 매수라 3월 회차를 못 받은 것으로 보이는데, 첫 달에 받는 종목도 있어서 일괄 제외하면 놓친 회차를 못 잡는다.

---

## 2026-08-20 (114) / — 과거 연도 화면에 **그때 없던 종목이 나오던 문제** + 2024 배당 검증
- 사용자: "2024년 배당 보는데 소유하지 않았던 종목이 들어가 있다. 그당시 소유 안 했으면 숨기기로 했잖아."
- **배당 데이터 자체는 정상**: 2024년 배당 70건을 지급월 보유수량과 대조(`verify_div_hold.py`) → **전건 보유 중 수령**. 유일한 예외 1건도 실제 문제가 아니었다 — `KB RISE 미국나스닥100`(2024-10-04)은 같은 계좌의 `KB KBSTAR 미국나스닥100` 122주와 동일 종목이다. **KB자산운용이 2024년에 KBSTAR → RISE로 리브랜딩**해서 거래내역상 이름만 바뀐 것.
  - ⚠️ 검증 1차 시도는 **내 방법이 틀렸다**: `보유수량_월별.csv`는 **거래가 있었던 달만** 행이 있는데 "행 없음 = 미보유"로 읽어 30건을 이상하다고 오판했다. 직전 값을 **이월**해서 봐야 한다. 해외종목은 이름이 PDF에서 잘려(`노보노디스크(A`) 매칭도 실패 → **티커로** 맞춰야 한다.
- **진짜 원인은 숨김 규칙이었다.** 기존: `지금 보유 중(수량>0) || 그 해 배당 있음` → 2026년에 산 종목이 **2024년 화면에 빈 줄로** 쏟아졌다.
- **고침**: `그 해 배당 있음 || 원금월별에 그 해 보유수량 있음 || (올해 화면일 때만) 지금 보유 중`. 올해만 예외를 둔 이유는 막 사서 아직 배당 전인 종목을 입력하려면 줄이 보여야 하기 때문.
- **검증**(실행 하네스에 과거연도 시나리오 추가): 2026 화면 → 3종목·노랑 12칸·매도종목 숨김 / 2024 화면 → 2종목·그 해 배당 보임·그 해 보유 종목 보임·**그 해 미보유 종목 숨김**·노랑 0칸.

---

## 2026-08-20 (113) / — ⭐ **파서 버그: 미래에셋 배당은 '거래금액'이 세전이다**
- 사용자가 "스프레드시트는 제대로 입력했다고 생각했는데" → 대조하다 **내 쪽 버그**가 나왔다.
- **증거(예수금잔고 증분)**: `분배금입금 169,000 / 제세금합 11,400` 행에서 예수금은 **+157,600**만 늘었다. 즉 미래에셋의 `거래금액`은 **세전**이고 제세금합이 차감돼 들어온다. **키움은 반대로 정산금액이 이미 세후**(증분 = 금액 그대로).
- 미래 계좌 전체를 훑어 검증: 세금>0인 배당·이자 **37건 전부** `금액 − 세금`과 예수금 증분이 일치.
- **고침**: 미래 구포맷 파싱에서 배당·이자 입금은 `금액 −= 제세금합`. → 전체 배당누적 84,992,048 → **84,718,528**(−273,520), 은경 미래 일반 8,229,992 → 7,984,294.
- ⚠️ **`계좌요약` 시트(계좌 성적표용)가 낡았다** — 재생성·재업로드 필요.
- **대조 결과**: 58종목 중 **46종목 일치**. 사용자 시트는 대체로 정확했고, 내가 앞서 지적한 것 중 상당수가 내 오류였다:
  - 익스포트가 **분배금 표도 잘라서** 준다(59행에서 끊김) → 은경 키움 3종목이 "시트에 없음"으로 보였다. 실제론 정상.
  - "기준월 −1개월" 규칙을 **반기·분기배당에까지** 적용해 롯데리츠(4/10 지급)·코람코(3/6 지급)를 틀렸다고 잘못 짚었다. 그 규칙은 **월배당 월말형**에만 해당한다.
- **사용자가 실제로 고친 것**: 30년국채 4월 190,000→40,000, RISE 미국배당100 5월 155,430 삭제, 200타겟위클리 6월 18,600→180,600.

---

## 2026-08-20 (112) / — 🔥 **2026년 화면에 2023·2024년 배당이 섞여 나왔다** — `divYear` 빈 값
- **증상**(사용자 스샷): 2026년 분배금 그리드에 재남 ISA 이리츠코크렙 3월 744,607(=2024-03), 재남 일반 코람코 2월 1,027,219·8월 1,170,381(=2023년)이 보였다. 롯데리츠는 한 줄에 2024년(3·9월)과 2026년(4·10월)이 섞였다.
- **데이터는 멀쩡했다** — `diagDividendYears()`를 만들어 시트를 직접 찍었다: 674행, year 전부 `number`, `"2024" === "2026"` → `false`. 연도별 2026=383 / 2025=129 / 2024=68 / 2023=34 / 2022=35 / 2021=15 / 2017~2020=10.
  - (덤으로 확인: 익스포트가 보여주던 415행은 역시 잘린 값이었다. 실제 674행.)
- ⭐ **원인**: `divYear`는 **숨은 input이고 초기값이 빈 문자열**이다. 채우는 건 `setupYearSelects()`인데 이게 **부팅 함수의 맨 마지막 줄**이다. 부팅 중에 탭을 열거나 앞 단계가 하나라도 실패하면 `year=''`로 요청이 나가고, 서버 `getDividends`의 `if (year)`가 거짓이라 **전 연도 674행을 다 돌려준다.** 프론트는 `divMap[holding_id_월]`로 키를 잡으므로 **뒤 행이 앞 행을 덮어써** 아무 연도나 화면에 뜬다. 라벨이 계속 "2026년"으로 보인 건 그 텍스트가 **HTML에 하드코딩**돼 있어서다(`<span id="divYearLabel">2026년</span>`) — 그래서 눈으로는 정상처럼 보였다.
- **고침 2단**: ① `year = divYear.value || String(올해)` 폴백 ② 서버가 다른 해를 섞어 보내도 **화면에서 그 해만 쓰도록** `divMap`·`paidThisYear`를 연도로 한 번 더 거른다. 두 그리드 모두.
- **검증**: 실행 하네스에 '서버가 전 연도를 보내는 상황'을 재현해서 넣었다 → 2024년 값(777777) 안 새어나옴 ✅ / 2021년만 있는 매도종목 안 나옴 ✅.
- **교훈**: 초기값이 빈 문자열인 숨은 input + 서버의 `if (param)` 관용 처리 = 조용히 틀린 데이터. 라벨이 하드코딩이라 증상이 더 늦게 드러났다.

---

## 2026-08-20 (111) / — 🔥 **분배금 탭이 안 열렸다** — (110) 패치가 두 군데 어긋났다
- **증상**: 사용자 "배당금 탭이 안 열려".
- **원인 1 — 내 패치 스크립트가 조용히 빗나갔다.** `isMonthly` 정의를 넣는 치환이 대상 문자열을 못 찾았는데 `str.replace`는 **에러 없이 그냥 통과**한다. 사용하는 쪽만 남아 `ReferenceError: isMonthly is not defined`.
- **원인 2 — `dueMap` 정의가 다른 함수에 들어갔다.** `divMap` 블록이 `loadDividendGrid`·`loadDivRateGrid` 양쪽에 똑같이 있는데 `replace(a,b,1)`가 엉뚱한 쪽을 잡았다. 정의는 분배율 그리드에, 사용은 분배금 그리드에.
- **원인 3 — `getDistributionAll`을 `Promise.all`에 끼웠다.** 6개 운용사 파싱이라 느린데 all-or-nothing이라 그리드 전체가 그 지연을 뒤집어쓴다. **WORKLOG (100)에서 똑같은 실수를 하고 적어놨는데 또 했다.**
- **고침**: 정의를 전부 `loadDividendGrid` 안 제자리로. 공지는 `Promise.all`에서 빼고 **표를 먼저 그린 뒤 `paintDueCells()`가 나중에 덧칠**한다(빈 칸만, 이미 입력된 칸은 안 건드림).
- ⭐ **교훈 — `node --check`(구문 검사)로는 이걸 못 잡는다.** 미정의 식별자는 런타임에만 터진다. 그래서 **DOM·API를 스텁으로 물리고 `loadDividendGrid()`를 실제로 실행**하는 하네스를 만들었다(`scratchpad/rundivgrid.py`). 결과: 예외 없음, 입력칸 36개(3종목×12), 노랑 12칸(월배당 2종목의 지나간 빈 달), 반기배당·매도종목 제외 확인.
- **앞으로 프론트 로직을 고치면 구문검사만 하지 말고 이 하네스로 한 번 돌릴 것.**

---

## 2026-08-20 (110) / — 분배금 그리드: **입력해야 하는데 빈 칸을 노랗게**
- 요구: "공지에 따른 배당이 입력돼야 할 셀만 앞뒤 3일간 노란색" → 이어서 "미입력과 입력해야 할 **모든** 셀".
- ⚠️ **±3일만으로는 확인용으로 못 쓴다** — 그 사이 놓치면 노랑이 사라져서 누락을 영영 못 잡는다. 그래서 두 조건의 합집합으로 했다:
  1. **이번 회차**: 공지 지급일이 오늘 앞뒤 3일 안 (`getDistributionAll` → `distDueMap`)
  2. **놓친 회차**: 이미 지나간 달인데 그 달에 **보유 중이었고**(원금월별의 그 달 수량 > 0) **월배당** 종목 → 채울 때까지 계속 노랑
  - 분기·반기는 어느 달이 회차인지 공지 없이는 모르므로 2번에서 제외. 보유 여부는 `costMap`(원금월별)으로 판정해서 **사기 전 달은 안 칠한다**.
- **중복 정의 제거**: `renderMasterCalendar` 안에만 있던 `parseDay`·`normName`을 `distParseDay`·`distNormName`으로 끌어올려 공유. 달력과 노랑 표시가 다른 파서를 쓰면 결과가 갈린다.
- ⭐ **이름 매칭 구멍**: 앱은 `삼성 KODEX 미국배당커버드콜S&P액티브`, 공지는 `KODEX 미국배당커버드콜S&P 액티브 ETF`다. 운용사 접두어 때문에 **완전일치가 빗나간다**(달력의 `isHeld`도 같은 약점을 갖고 있다). `distDueMap`엔 티커 → 완전일치 → **포함관계(8자 이상)** 순으로 폴백을 넣었다. 노드로 4케이스 검증: 티커일치 ✅ / 이름포함 ✅ / 9일 지난 건 제외 ✅ / 남의 종목 제외 ✅.
- portfolio.html만 변경 → 재배포 불필요.

---

## 2026-08-20 (109) / — 매도 종목(수량 0) 화면에서 숨기기 — **지우지는 않는다**
- 과거 배당 162건을 넣으면서 매도 종목 32개가 holdings에 딸려 들어와 65 → 97개가 됐다. 분배금 목록이 옛날 종목으로 도배됐다.
- 사용자 요구: "받아서 수익이나 거래가 있었지만 현재는 없는 것 = 관리대상에서 빼되 **기록은 남아야 하므로 숨기기만**". → 시트 데이터는 그대로 두고 렌더에서만 거른다.
- **분배금 그리드·분배율 추이**(`loadDividendGrid`·`loadDivRateGrid`): 수량 0인 종목은 **그 해에 배당을 받은 경우만** 남긴다. 그래서 2026년 화면에선 사라지고 2021년으로 넘기면 다시 보인다 — 과거 배당을 넣은 의미가 유지된다.
- **종목관리**(`loadHoldings`): 수량 0은 기본 숨김 + **'매도 종목 보기' 체크박스**. 관리 탭에서까지 영영 못 보면 나중에 고칠 방법이 없어 토글을 뒀다.
- `state.holdings`에는 전부 남긴다(계좌 성적표·대시보드가 참조하는데 수량 0이라 합계엔 영향 없음).
- portfolio.html만 변경 → 재배포 불필요.

---

## 2026-08-20 (108) / — 계좌 성적표: 시작일 열 추가 + 색 톤다운
- **시작일 열**(세부표 맨 끝): 그 계좌에 처음 돈이 들어온 날. 현금흐름의 최소 날짜다. 툴팁에 '첫 입금일 — N.N년 굴렸다'. **소계·합계 행은 비운다** — 여러 계좌 중 가장 이른 날짜는 의미가 없다(사용자 지적).
  - 부수 수정: XIRR의 기간 계산이 `fl[0].date`(배열 첫 행)였는데 시트 정렬을 믿는 코드였다 → **정렬해서 최소값**을 쓴다. 값은 그대로였지만 시트 순서가 바뀌면 틀렸을 자리다.
- **색**: 계좌마다 다른 6색(`getAccColor`)이라 "너무 알록달록"하다는 지적 → **재남/은경 2색만**. 행 `#eff6ff`/`#fdf2f8`, 소계 `#dbeafe`/`#fce7f3`. 다른 탭의 `getAccColor`는 그대로 둔다(그쪽은 계좌 구분이 필요하다).
- portfolio.html만 변경 → 재배포 불필요.

---

## 2026-08-20 (107) / — ✅ 배당 162건은 처음부터 잘 들어가 있었다 — **Drive 익스포트가 잘린 것**
- **결론 먼저**: 8/19 16:21 1차 실행으로 **2025년 이전 배당 162건(2017~2024) + 종목 32개가 정상 입력됐다.** 오늘 재실행 로그: `배당 660행 → 660행 (신규 0, 중복건너뜀 162)`. 
- ⚠️ **내가 틀렸던 것**: 어제 Drive 커넥터(`read_file_content`)로 DB 시트를 읽어 "dividends가 415행 그대로라 안 들어갔다"고 판단했다. **익스포트가 큰 탭을 잘라서 준 것**이었다 — 실제 660행인데 익스포트는 지금도 415행까지만 보여준다. 헛짚고 반나절 날렸다.
- **교훈(메모리에 저장)**: 시트 행 수·입력 여부 검증은 **익스포트로 하면 안 된다.** GAS에서 `getLastRow()`를 로그로 찍은 값이 진실이다. 앱 API로도 검증 못 한다(토큰 = 사용자 진입 비밀번호).
- **그 덕에 잡은 진짜 버그: `dividends` 시트에 헤더 행이 없었다.** 1행부터 데이터인데 `getDividends()`가 `rows.slice(1)`로 첫 행을 헤더로 알고 버려서 **배당 1건(재남 키움 ISA 롯데리츠 2026-04, 94,341원)이 앱에서 계속 안 보였다.** `saveDividend`·`deleteRowById`도 `i=1`부터라 그 행은 수정·삭제도 안 됐다. → 임포터가 1행에 헤더를 심어 정상화(오늘 실행 로그에 `⚠ 헤더 행이 없어서 새로 심었다`).
- **임포터 보강(남겨둘 가치 있음)**: 쓰기 후 `SpreadsheetApp.flush()` → 재읽기로 행 수 대조, 기대만큼 안 늘면 예외. 로그에 시트명·스프레드시트 ID·전후 행 수를 찍는다. 이번 진단이 가능했던 이유다.
- **확인 방법**: 분배금 탭에서 연도를 뒤로 넘기면 2017년까지 보인다((106)에서 연도 네비를 2016~내년으로 넓혀둠).

---

## 2026-08-19 (106) / — 성적표 상단 요약 블록 + **2025년 이전 배당 162건 일괄 입력 준비**
- **요약 블록**: 카드 4장(투자원금·총자산·총수익·XIRR) + 계좌별 7열 한눈표(투자원금·총자산·총수익·총수익률·**기간**·XIRR)를 세부표 위에 배치. 사용자가 채팅에서 본 표가 "보기 좋다"고 해서 그대로 화면에 넣었다.
- **2025년 이전 배당**: 앱 dividends에는 2025(53행)·2026(362행)뿐이었다. 거래내역+해외배당 PDF에서 **2017~2024년 162건**을 복원.
  - ⚠️ **구조적 걸림돌**: dividends는 holding_id로 묶이는데 과거 배당의 대부분은 **이미 판 종목**이라 holdings에 자리가 없다(162건 중 127건). → 임포터가 **수량 0짜리 종목을 먼저 만들고**(div_cycle='매도완료') 배당을 붙인다. 드라이런 결과 **신규 종목 32개 / 배당 162건**, holdings 65 → 97행.
  - 시트 `배당임포트 2025년이전` = `1UWa4bORYbedjdh9cF8RhjKVIysceo-uhT00Xcg29mKM`.
  - `importDividends()`는 CLAUDE.md 규칙대로 **'수동 실행' 섹션 맨 아래**에 뒀다. `(holding_id, year, month)` 중복이면 건너뛰므로 **여러 번 돌려도 안전**(드라이런 2회차: 0건 입력 / 162건 건너뜀).
  - **국내 매도 종목 49행은 티커가 빈칸**이다(거래내역엔 티커가 없고 앱에도 없는 종목). 수량 0이라 시세 조회가 필요 없어 그대로 뒀다.
  - 제외 1건: 재남 키움 일반 이리츠코크렙 2024-05 **−65,550원**(일반과세부동산추징 배당출금). 음수 배당은 월 단위로 표현할 방법이 없다.
- **연도 네비 범위 확대**: `cur±2`(2024~2028) 고정이라 과거 배당을 넣어도 볼 수가 없었다 → **2016~내년**으로.
- **배포**: `clasp push --force`만. doGet/doPost 경로가 아니라 재배포 불필요(CLAUDE.md 규칙).
- **남은 건 사용자 몫 1회**: Apps Script 편집기에서 `importDividends` ▶ 실행. (`clasp run-function`은 API executable 배포가 아니라서 안 되고, 웹앱으로 부르려면 토큰이 필요한데 그건 사용자 비밀번호다.)

---

## 2026-08-19 (105) / — 계좌 성적표 UI 정리 (사용자 피드백 반영)
- **회색 금지 위반 2건 수정**: XIRR '1년 미만'을 회색으로 칠했고 합계 행을 검정(#334155)으로 깔았다. → 1년 미만은 값 색 그대로 두고 `1년↓` 윗첨자(#b45309)만, 합계 행은 `3px double #111827` + `#e2e8f0`(헤더와 같은 톤).
- **다른 탭과 톤 통일**: `.sc-table`을 스크리너(`.scr-table`)와 같은 규칙으로 — 2px 바깥 프레임, 그룹 첫 열에 `border-left:2px solid #111827`(`.g`), 소계 행 위아래 2px.
- **계좌별 색은 `getAccColor()` 재사용** — 다른 탭과 같은 팔레트. 소계는 재남 `#bfdbfe` / 은경 `#fbcfe8`.
- **서브라인 → 셀 분리**: 총자산 = `주식`·`예수금`·`총자산` 3열, 이익금 = `평가손익`·`실현손익`·`이익금` 3열.
- **표 밑 설명 문단 전부 삭제 → `title` 롤오버**로. 열 제목·셀마다 계산식이 뜬다. 탭 헤더의 `ⓘ 계산 기준`에 데이터 기준일·빠진 계좌 안내.
- **거래총액 열 삭제**(사용자 판단).
- **'배당수익률'이 뭔지 모르겠다** → 이름을 쪼갰다: **배당회수율**(배당누적 ÷ 투자원금 — 원금을 배당으로 몇 % 회수했나)과 **연배당률**(최근 1년 배당 ÷ 주식 평가금액 — 지금 연 몇 %가 나오나). 은경 키움 일반이 35.53% vs 4.20%로 가장 크게 갈린다.
- ⭐ **'배당회수율' 삭제 — 사용자 지적이 옳다.** "배당금은 계속 다시 갯수 늘리는데 사버리잖아". 배당을 재투자하면 그 현금은 계좌 밖으로 나간 적이 없고 **보유원금(매입원가)으로 옮겨간다**(그래서 보유원금 583,969,865 > 투자원금 487,692,419). '회수'라는 말 자체가 틀렸다. 남긴 건 **연배당률**(최근 1년 ÷ 주식 평가금액)뿐 — 이건 현재 상태라 재투자와 무관하다.
- 대신 총수익 툴팁에 **검산**을 넣었다: `총자산 − 투자원금`. 이익금 + 배당누적과 거의 같아야 한다(전체 136,522,999 vs 135,481,565). 차이는 예수금·거래내역 기준일 차이. 이 대사가 맞는다는 건 **배당을 이익금과 따로 더해도 이중계산이 아니라는 증거**다 — 재투자분은 보유원금에 들어가 있어 평가손익에는 '차익'만 잡히기 때문.
- portfolio.html만 바뀌었으므로 **재배포 불필요**(로컬 파일). git push로 끝.

---

## 2026-08-19 (104) / — 계좌 성적표 확장: 예수금·XIRR·배당수익률·세금 (@150)
- (103)에 이어 사용자 요청 4가지를 전부 반영. 시트 2개 신규: `계좌요약 v3`(`1WXslTXPsdZNm8aG-3DbHx_yIsxz4xHonYAkTSvKb-j4`, 14열)·`현금흐름`(`1JdnOiI8UQ2HJ_SKZRYVSl4DWoxcZ19mTQhmlgHvQ23A`, 146행). 액션 `getAccountFlows` 추가.
- ⭐ **버그: 현금 성격 항목에 `금액`을 쓰면 안 된다 — `증감`(부호 반영)을 써야 한다.** 미래에셋의 `배당세출금`·`이자세금출금`은 분류가 '배당'/'이자'인데 `금액`으로 더하면 **세금이 배당에 가산**된다. 은경 미래 일반 배당이 8,362,572(틀림) → **8,229,992**(세후 순현금). 전체 배당누적 85,492,768 → 84,992,048.
- **① 예수금**: 계좌별 마지막 `예수금잔고`(원화)와 `외화예수금잔고`(USD). 총자산 = 주식 평가 + 예수금. 원화는 2026-08-18까지 최신, **USD는 마지막 해외거래일(2026-07-09) 기준**이라 그만큼 낡는다.
- **② XIRR**: 현금흐름 146건 + 오늘의 총자산으로 이분법([-95%, +1000%]) 계산. 뉴턴법은 발산할 수 있어 안 썼다. 계좌간 이체는 양쪽에 반대 부호로 들어 있어 소계·합계에서 저절로 상쇄된다. **1년 미만 계좌는 회색 + '1년 미만' 표시** — 재남 키움 연금저축이 6개월 +19.9%를 연 43.47%로 뻥튀기해서 보였다.
- **③ 세금**: 국내 배당소득세(키움은 소득세 컬럼, 미래는 `배당세출금` 행) + 해외 외국납부세액(PDF). ISA·연금은 0 → '비과세' 표시. 배당누적은 전부 세후 기준.
- **④ 배당수익률 분리**: 누적(배당÷투자원금)과 현재(최근 1년 배당÷주식 평가금액). 은경 키움 일반이 누적 35.53% / 현재 4.20%로 가장 크게 갈린다 — 오래 들고 있어 누적이 쌓인 계좌와 최근에 넣은 계좌를 같은 잣대로 보면 안 된다는 증거.
- **검증**: 함수만 떼어 Node에 DOM 스텁 물리고 실데이터로 렌더 → 표 전체 텍스트 확인(평가금액은 평단×1.07 가정).
- **배포**: `clasp push --force` → `clasp redeploy @149 → @150`.

---

## 2026-08-19 (103) / — **계좌 성적표 탭** 신설: 순입금·실현손익·배당누적을 거래내역에서
- 요청: 계좌별 투자원금(입금액)·배당금총액·거래총액·평가금액·이익금(배당 제외)·수익률.
- 앱은 '지금 보유'만 안다. 나머지는 전부 증권사 거래내역에서만 나오므로 **비공개 시트 `계좌요약`**(`1h2dyPSjBVQrwBMqM6Uv-GUD4nP0p5OXo7ojPea8FbFc`)에 7계좌×9열로 올리고 `getAccountSummary`로 읽는다. (원금 데이터를 Code.gs에 박으면 안 되는 이유는 (102) 참고 — 저장소가 public이다.)
- ⭐ **함정 1 — 펀드 기준가는 1,000좌당이다.** 처음 실현손익이 은경/미래 연금 **−1.5억**으로 나왔다. `수량×단가`가 실제 금액의 정확히 **1,000배**(429,297좌 × 1,697 = 7.28억, 실제 거래금액 728,650원). `'펀드' in 거래구분`이면 단가를 1000으로 나눈다 → 실현손익 합계 **+16,024,026원**으로 정상화.
- ⭐ **함정 2 — 타사대체입고(현물이관)를 원금에 넣어야 한다.** 재남 키움 일반 수익률이 **102.55%**로 나왔다. 순입금 67,959,176 − 계좌간이체 54,799,999 = 13,159,177인데 보유원금은 95,425,351. 차액의 정체는 **다른 증권사에서 주식째로 옮겨온 56,291,370원**(9건). 현금 입금은 아니어도 외부 자본이라 투자원금에 포함 → **19.43%**로 정상. (유상주입고는 유상불입출금으로 현금이 나갔으므로 제외.)
- **정의**: 투자원금 = 외부입금 − 출금 + 계좌간이체 + 현물이관 / 이익금 = 평가손익(평가금액 − 보유원금) + 실현손익 **(배당 제외)** / 총수익 = 이익금 + 배당누적 + 이자 / 수익률 = 이익금 ÷ 투자원금.
- **실현손익**은 파서의 이동평균 원가로 매도 시점마다 (수량×단가 − 원가배분) 누적. USD는 매수 시점 환율로 환산한 원화 원금 기준.
- `renderAccountStats`의 보유원금·평가금액 계산을 `acctInvestValue()` 하나로 뽑아 성적표와 공유(CLAUDE.md의 '계산 블록 중복' 경고 때문에 복붙하지 않았다).
- **검증**: portfolio.html에서 함수만 떼어내 Node에 DOM 스텁을 물리고 실데이터로 렌더 → 표 전체를 텍스트로 뽑아 확인. 두 함정 다 이 표에서 눈에 띄어 잡았다.
- **배포 완료**: `clasp push --force` → `clasp redeploy @148 → @149`.
- **앱에 없는 계좌 2개는 성적표에서 빠진다**: 은경 미래에셋 IRP(순입금 13,460,000 — 신탁계약출금으로 빠져 매매 내역 자체가 없음), 재남 미래에셋 일반(순입금 249,668). 필요하면 앱에 계좌 등록.

---

## 2026-08-19 (102) / — 거래내역 → **월별 원금 복원 완료**, 분배율 분모를 그달 원금으로 교체
- **101의 후속**. 거래내역 원본(키움·미래 xls, 키움 해외주식 CSV, 해외배당 PDF)을 전부 파싱해 `_통합\` 산출. 원본·산출물은 전부 저장소 **밖**(`Documents\jjk-안전\금융\`) — public repo + 자동푸시 때문.
- **파서**(`parse_all.py`): 거래내역 1,699건 / 해외배당 173건. 해외 매매는 xls에 아예 없어서 키움 '해외주식 거래내역' CSV로만 들어온다(중복 없음).
- ⭐ **수량은 누적하면 안 된다 — 파일의 `유가잔고`(거래 후 보유수량)가 진실이다.** 누적(매수−매도) vs 잔량 대사를 붙였더니 전 계좌 일치, 남은 불일치 2건은 실제 기업행위였다: NLOK→GEN 티커 변경(별칭 처리), NVO 2:1 분할(수량만 영향, 원금 무관). SCHD 3:1 분할도 앱 대조에서 잡혔다.
- 파싱하며 걸린 함정 3개: ① 미래에셋 파일은 **최신순** 정렬이라 같은 날 두 건이 거꾸로 들어간다(파일 단위로 뒤집음) ② 배당·청약 행에도 종목명이 붙어 있어 그 행의 잔량 0을 잔고로 오인 ③ 잔고 0은 **공란**으로 나온다.
- **월말 보유수량·원금** `보유수량_월별.csv` (2016-08~2026-08, 1,441행). USD 원금은 해외배당 PDF의 적용환율을 날짜별 환율표로 재활용해 매수 시점 환율로 환산.
- **검산 통과**: 코람코(은경 키움) 13,802주×5,033.56 → 앱의 69,479,268원과 일치. JEPQ(은경) 170주×59.08 → 앱 평단과 일치.
- **앱 holdings 65종목 중 61개 매칭**(`match_holdings.py`). 종목명 표기가 증권사·앱마다 달라 이름 대신 **(계좌, 수량, 평단)**으로 붙였다. 미매칭 4: 앱에 0주로 남은 유령 2건, 재남 JEPQ(앱 179 vs 내역 175 — 4주 차이 원인 미상), 재남 SCHD(3:1 분할로 앱 66주 vs 내역 22주).
- **코드 변경**:
  - `Code.gs`: `getCostBasis(year)` 신설 + 라우터에 `getCostBasis` 추가. 시트 `원금월별`=[holding_id, year, month, quantity, cost, currency]를 `{holding_id_월: {qty,cost,cur}}`로 반환. **탭이 없으면 `{}` → 프론트가 현재 원금으로 폴백**하므로 시트 없이 배포해도 안 깨진다.
  - `portfolio.html`: `loadDivRateGrid`(분배율 추이)와 `loadDividendGrid`(분배금 입력, 툴팁·실분배율·환산분배율) **둘 다** 같은 버그였다. 월별 분모를 `costMap`의 그달 원금으로, 실분배율·환산분배율 분모는 **분배금 받은 달들의 평균 원금**으로 교체.
- **효과(2026년 실분배율, 배포 전 오프라인 검산)**: 59종목 중 22종목의 분모가 바뀐다. 은경 키움 JEPQ 0.60%→2.60%, 은경 키움 RISE200위클리 12.93%→16.89%, 재남 KODEX미국나스닥100OTM 6.10%→12.23%. 연중 수량을 늘린 종목이 전부 과소평가돼 있었다는 101의 진단과 방향이 일치.
- **원금 데이터는 별도 비공개 시트로 올렸다**(내가 Drive 커넥터로 직접 생성): `원금월별 (거래내역 복원 · 분배율 분모)` = `1zjDhF53_yFbFzRdrf3zlZm2CdduSMkBCiu8wVFGPzn8`. **Code.gs에 데이터를 박지 않은 이유: 이 저장소는 public이다.** 시트 ID만 상수로 둔다(기존 SHEET_ID와 같은 방식).
  - 원금은 매매가 있을 때만 바뀌므로 **바뀐 달만** 저장(147행). `getCostBasis`가 이월(carry-forward)해서 12달을 채운다. 747행 → 147행.
- ✅ **배포 완료 — 내가 clasp로 직접 했다.** 이 PC엔 clasp가 없어 새로 설치(3.3.0), 사용자는 `clasp login` 브라우저 '허용' 1회만. `clasp push --force`(매니페스트 프롬프트 때문에 `--force` 필요, 파이프로는 안 먹는다) → `clasp redeploy AKfycbwJS1Fd… -d "..."` → **@147 → @148**. push 전 임시 폴더로 `clasp pull` 해서 원격 == 내 작업 전 로컬(3,439줄 동일) 확인 — 편집기에서 만든 코드가 없으니 push가 아무것도 지우지 않는다.
- ⚠️ **`portfolio.html`은 배포 대상이 아니다 — 내가 처음에 "붙여넣기 + 재배포"라고 잘못 안내했다.** 로컬 파일을 브라우저로 여는 구조이고 `.claspignore`가 Apps Script push에서 막는다. git push로 끝.
- **남은 것**: 앱 새로고침 후 분배율 추이 탭에서 위 숫자 확인(님 몫 — 토큰이 세션 비밀번호라 내가 API를 못 친다).
- ⭐ **JEPQ로 복원 전체를 교차검증했다**: 은경 JEPQ의 월별 주당배당(배당÷수량)으로 재남 JEPQ 배당을 나눠 수량을 역산하니 1~7월 **전부 소수점까지 일치**(111·166·166·166·166·166·169). 거래내역 복원이 옳다는 독립 증거다. 175 vs 179는 **CSV가 2026-07-09에서 끊긴 것**(그 뒤 8월 매수 4주 미포함)이지 파싱 오류가 아니다. → 이번 달 행만 앱 holdings 값으로 덮도록 `make_costbasis.py`에 규칙 추가(과거 달은 거래내역 그대로).
- **재남 JEPQ**: 사용자 확인 결과 **앱 179주가 맞다** → 거래내역 CSV에 매수 4주가 빠져 있다. 수량이 달라도 티커로 붙이도록 매칭 3단계를 추가해서 포함시켰다(과거 달 분모는 실제 거래에서 나오므로 여전히 옳다). 재남 SCHD도 같은 방식 — **SCHD 3:1 분할은 2024-10-11**(마지막 매수 2024-08-06 22주 $78.22 → 앱 66주 $26.07로 정확히 3배). 최종 매칭 **63/65**, 남은 2건은 앱에 0주로 남은 유령 행.

---

## 2026-08-18 (101) / — 분배율 분모가 **현재 보유 기준 1개**라 수량 바뀐 종목이 전부 틀림 (원인 확정, 설계 대기)
- **증상(사용자)**: "분배율 추이가 잘못돼서 실분배율까지 문제다. 중간에 코람코를 팔고 JEPQ를 샀는데, 큰 분배금이 마지막에 들어오니 이전 분배금 비율을 너무 낮게 잡는다."
- **원인 확정** — `portfolio.html:2048`:
  ```
  const invest = (h.avg_price) * (h.quantity);   // 지금 이 순간의 원금 하나
  rateArr[m-1] = amtKrw / investKrw * 100;       // 1~12월 열두 칸 전부 같은 분모
  ```
  `realRate`(실분배율, :2090)·`annualRate`(환산분배율, :2094)도 **같은 분모**를 쓴다.
- **실데이터 검산(Drive 커넥터로 DB 시트 직접 조회)**: JEPQ(은경 키움) `59.0835 × 170주` ≈ 14,173,000원 → `$8.32×1411 ÷ 14,173,000 = 0.083%` = 화면의 0.08% ✅ / 코람코 `5,034 × 13,802주 = 69,479,268원` → `1,952,320 ÷ 69,479,268 = 2.81%` = 화면과 일치 ✅. 분모 가설 확정.
- ⭐ **사용자 지적이 정확했다 — JEPQ만의 문제가 아니라 구조적이다.** "분배금 받은 거나 입금해서 갯수 늘린 주식들의 분배율이 전부 이런식으로 계산된 거잖아" ⇒ 맞다. **연중 수량이 바뀐 모든 종목**이 틀린다. 수량을 늘렸으면 과거 달이 낮게, 줄였으면 높게. 가장 단적으로는 **오늘 추가매수를 하면 이미 지나간 1월 분배율 숫자가 소급해서 바뀐다**(평단·수량이 둘 다 현재값이라서).
- **과거는 복원 불가 — 스키마를 전부 확인했다**: `dividends`=`[id,holding_id,year,month,amount,currency]`(원금 없음) / `holdings`=현재 수량·평단만 / `수익로그`=**계좌 단위 평가금액**(종목별 아님) / `시세로그`=가격만. 어디에도 과거 보유수량이 없다.
- **사용자 결정**: **① 거래내역 기능부터(근본)**. 그리고 "학교에서 하던 작업이 그간 모든 거래내역 엑셀 파일"이라 **그걸 기준으로 과거까지 복원 가능**하다고 함.
- ⚠️⚠️ **그 엑셀을 이 저장소에 두면 안 된다 — 확인 결과 `jjk`는 GitHub `visibility: public`이다.** 게다가 Stop 훅이 매 턴 `git add -A`로 자동 커밋·푸시한다 ⇒ **폴더에 엑셀을 두기만 해도 사람이 손대지 않아도 전 세계에 공개된다.** 자동화가 만든 새 위험이다.
  - **조치**: `.gitignore`에 `*.xlsx` `*.xls` `*.csv` 추가. **검증**: 더미 `거래내역_테스트.xlsx`·`잔고_테스트.csv`를 만들고 `git status`에 안 잡히는 것 확인(교훈대로 `check-ignore`가 아니라 `git status`로 판정) → 통과, 더미 삭제.
  - **거래내역 엑셀은 Google Drive에 올릴 것.** 그러면 두 PC 어디서든 Drive 커넥터로 내가 직접 읽을 수 있고 저장소엔 안 남는다. (로컬·Drive 둘 다 검색했지만 현재 이 PC엔 없음 — 아직 학교 PC에 있다.)
- **다음 할 일 — 🏫 내일 학교에서 이어서 마무리한다(사용자 결정).** 학교 PC에 이미 같은 작업(거래내역 엑셀)이 있으므로 거기서 끝내는 게 맞다.
  1. **엑셀을 저장소 폴더에 두지 말 것** — public + 자동 푸시라 그대로 공개된다(위 ⚠️). `.gitignore`로 막아뒀지만 **Google Drive에 올리는 게 정답**이다. 그러면 내가 Drive 커넥터로 바로 읽는다.
  2. 열 구성 확인 → `거래내역` 시트 스키마 확정 → 임포터 → "그 달의 실제 보유수량"으로 분배율·실분배율·환산분배율 재계산.
  3. 백엔드 시트가 늘어나므로 **Code.gs 재배포 필요**(사용자 확인 후).
  - **엑셀 열 구성을 봐야 임포터를 설계할 수 있어 여기서 멈춘다.** 추측으로 만들면 헛일이 된다.

## 2026-08-18 (100) / — 분배금 공지 "또 느림" = **패널이 all-or-nothing이라 꼬리 지연을 그대로 뒤집어씀** → 스냅샷 즉시 렌더
- **증상**: 분배금공지 탭에서 좌측 '📢 분배금 공지'만 "불러오는 중…"에 멈춤. 달력·운용사별 일정은 정상 표시(사용자 스샷).
- ⭐ **스샷 자체가 단서였다**: 달력(`loadDistributions`)은 **GAS 왕복이 더 많은데도 다 그려졌는데**, 공지(`loadAllNotices`)는 왕복이 1번뿐인데 멈춰 있었다. ⇒ 느린 건 특정 요청이 아니라 **공지 패널의 구조**다.
- **실측(백엔드는 멀쩡하다)**: `getEtfNoticesAll` 10회 연속 — 중앙값 **1.6초**, 최대 **6.2초**, 5초 초과 **1/10**. 별도로 **14.0초·18.7초**도 관측. 즉 **평소 빠르고 5회에 1번꼴로 6~19초 튀는 꼬리 분포**.
  - 동시성 가설은 **기각**: 처음엔 `getDistributionAll`+`getEtfNoticesAll` 동시 발사에서 둘 다 18.7초가 나와 실행 슬롯 경합인 줄 알았으나, 재측정 2회에서 3.2·3.4초로 정상. 간헐적 지연이지 동시성 때문이 아니다. (86)의 '구글 응답 배달' 계열.
  - CORS·운용사 사이트도 **정상**: 브라우저에서 TIGER 147ms / PLUS 135ms.
- **근본 원인**: `loadAllNotices()`가 **① 스냅샷 없음 ② 6개사가 다 모여야 한 번에 렌더(all-or-nothing) ③ 타임아웃 없음**. 그래서 6개 중 **가장 느린 한 다리**가 패널 전체를 잡아둔다. 앱의 나머지는 전부 `paintFromCache`로 즉시 그리는데 이 패널만 예외였다.
- **한 일(`portfolio.html` 한 파일, +21/-5)**:
  - **스냅샷 `jjk_nsnap_v1`**: 지난번 공지를 **즉시 렌더**하고 '갱신 중' 배지를 띄운 뒤 뒤에서 갱신. 백엔드가 몇 초를 먹든 화면은 0ms에 찬다. 공지는 한 달에 몇 번만 바뀌므로 잠깐 옛것이 보여도 무해.
  - **전부 0건이면 스냅샷을 덮지 않는다** — 일시적 실패가 다음 진입의 빈 화면으로 굳는 걸 막는다.
  - **`NOTICE_VIA_GAS`에 tiger·plus 추가**(4곳→6곳). 서버 벌크 캐시엔 이미 6개사가 다 들어 있는데(실측: 6개사 전부 `items=5`, stale 아님) **TIGER·PLUS만 브라우저가 운용사 사이트를 또 긁고 있었다.** 공개 페이지(`dist_notice.html:230`)는 **이미 6개사를 전부 프록시로 통일**해뒀다 — portfolio.html만 옛 방식으로 남아 있었던 것. stale이면 예전 직접 fetch로 자동 폴백하므로 안전.
- **검증(로컬 http 서버에 실제 파일을 띄우고 실제 함수에 대고 17항목)**: 직접 fetch 0회(수정 전 2회) / 6개사 렌더 / 스냅샷 6개사 저장·필드 보존 / **백엔드 3초 지연 중에도 첫 렌더 0ms** / **스냅샷 렌더 HTML == 실데이터 렌더 HTML(내용 어긋남 0)** / 갱신 후 동일 / 0건일 때 스냅샷 보존 / stale 폴백 6개사 전부(직접 2 + GAS 개별 4) / 스냅샷 없는 첫 방문은 기존대로 스피너 / `node --check` OK.
  - ⚠️ 처음 2건 FAIL은 **코드가 아니라 내 기대값이 틀린 것**이었다: ① 폴백 횟수를 한 카운터로 셌는데 경로가 2가지(직접 fetch 2 + `api` 4)라 6이 아니라 2로 보였다 ② 배지 opacity를 `.35s` 페이드 **진행 중에** 읽었다. transition 제거 후 재측정하니 1↔0 정상.
  - 📌 **하네스 함정 2개**: `data:` URL 미리보기는 `sessionStorage`가 막혀 **`portfolio.html:1129`에서 스크립트가 통째로 중단**된다 → 그 아래 모든 `const`가 TDZ("Cannot access X before initialization"). 내 코드 탓으로 오해하기 딱 좋다. **진짜 검증은 `http://localhost`로 띄워야 한다.** 또 미리보기 탭은 프레임을 안 돌려 **CSS transition이 멈춰** computed 값이 시작값에 고정된다.
  - 📌 Windows PowerShell에서 `Get-Content -Raw`는 한글을 cp949로 깨뜨려 **가짜 SyntaxError**를 만든다. `[IO.File]::ReadAllText(path, UTF8)`를 쓸 것.
- ✅ **프론트만 수정 — Apps Script 재배포 불필요.**
- **(이어서) 공개 페이지도 같이 고쳤다** — 사용자 "당연히 해야지". `dist_notice.html`도 같은 all-or-nothing 구조라 스냅샷(`jjk_dnsnap_v1`)을 넣었다. 여긴 프록시 통일이 이미 돼 있어 `NOTICE_VIA_GAS` 변경은 불필요. `showRefreshing`이 없는 파일이라 배지 대신 **제목에 ' ⟳'**를 붙였다 가 갱신 끝나면 `renderNotices`가 제목을 새로 쓰면서 자동으로 지워진다.
  - 검증 **11/11 PASS**(localhost + 실제 함수): 첫 방문 스피너 / 재방문 **0ms 렌더** / 스냅샷 HTML == 실데이터 HTML / ⟳ 표시·제거 / 0건일 때 스냅샷 보존 / stale 폴백 6개사.
  - **`jjk-dist` repo 미러·푸시까지 완료** — 안 하면 라이브는 안 바뀐다.
- ⭐ **권한 프롬프트가 자꾸 뜨는 원인도 같이 잡았다** (사용자: "그냥 니가 진행하라고 했는데 자꾸 묻는다").
  - **원인은 WORKLOG 99와 판박이다**: 허용 목록 84개가 전부 **`.claude/settings.local.json`**에 있는데 `.gitignore`가 `.claude/*`(예외 `settings.json` 하나)라 **그 파일이 다른 PC로 간 적이 없다.** 한쪽에서 '계속 허용'을 아무리 눌러도 반대편은 처음부터 다시 묻는다.
  - **조치**: 커밋되는 **`.claude/settings.json`에 `permissions` 신설**(allow 45 / deny 6). 실제 트랜스크립트 17세션·툴콜 1,968건을 세어 빈도 상위로 골랐다 — 브라우저 MCP 전체(javascript_tool 109회 등), `git add·commit·push`(사용자 상시 지시), `curl`, `Skill(*)`, `node --check`.
  - 넣지 **않은** 것: `git status`·`ls`·`grep`·`head`·`cat`·`echo`는 원래 자동 허용이라 불필요. `Bash(python *)`·`Bash(node *)` 같은 **임의 코드 실행 와일드카드는 공유 파일에 안 넣었다**(로컬엔 이미 있음). `deny` 보호 규칙 6개는 **공유 파일에도 그대로 복사** — 허용만 공유하고 가드를 빼면 안 되니까.
  - **검증**: JSON 파싱 OK / `hooks` 두 개가 **HEAD 버전과 문자열 단위로 완전 동일**함을 대조(권한 추가하다 훅을 깨먹지 않았는지가 제일 위험한 지점이었다).

## 2026-08-18 (99) 집 — ⭐**자동 커밋·푸시 훅이 한쪽 PC에만 있었다. 학교 작업이 갇힌 원인.**
- **발단**: 사용자가 "학교에서 계좌 거래내역 넣는 작업"을 하고 왔는데 저장소에 없었다. 집 PC에서 확인:
  - 작업 트리 깨끗, stash 없음, `main` + 로컬 브랜치 `home-savings-32fb5cc` 전체에 `거래내역·거래·transaction·입출금` **0건**.
  - 배포된 Apps Script까지 실물 대조(스크래치 디렉터리에 `.clasp.json` 만들어 `clasp pull`) — 로컬 `Code.gs`와 **완전 동일(3,438줄)**, 백엔드에도 흔적 없음.
  - ⇒ **저장소 문제가 아니라 학교 PC에 커밋 안 된 채 남은 것.** 오늘 학교발 커밋 9개는 12:07~12:28에 올라오고 끊겼다(전부 차트·대시보드 작업).
- ⭐ **원인을 찾았다 — 훅은 있었는데 동기화가 안 됐다**:
  - 집 PC `.claude/settings.local.json`에 **자동 커밋·푸시 Stop 훅이 이미 있었다.** 그래서 집 작업은 늘 올라갔다.
  - 그런데 `.gitignore`에 **`.claude/`가 통째로** 들어 있어 **그 훅이 학교 PC로 간 적이 없다.** 학교에는 방어막이 없었다.
  - 방증: 오늘 9개 커밋 중 `WORKLOG.md`를 건드린 게 **하나도 없다.** 종료 루틴이 아예 안 돌았다는 뜻.
- **조치 — 훅을 커밋되는 파일로 옮겼다**:
  - `.gitignore`: `.claude/` → **`.claude/*`** + `!.claude/settings.json`. ⚠️ 트레일링 슬래시면 git이 디렉터리 안으로 안 들어가 **예외가 먹지 않는다** — 이것 때문에 패턴을 바꿔야 했다.
  - `.claude/settings.json` 신설(= 검증된 기존 훅 그대로: main 브랜치만, 머지/리베이스 중이면 skip, 더러우면 `add -A` + `auto-sync <시각>` 커밋 + push).
  - `.claude/settings.local.json`에서는 **훅을 제거**했다 — 두 곳에 있으면 중복 실행된다. 권한 목록(allow 82 / deny 6)은 그대로.
- **검증**: 두 파일 JSON 파싱 OK(UTF-8 지정 필요 — Windows 파이썬이 cp949로 읽어 한 번 실패했다) / `hooks` 키가 local에서 사라짐 확인 / `git status`가 **`?? .claude/settings.json`**을 보고 `settings.local.json`·`skills/`는 계속 무시 → 예외가 정확히 한 파일만 뚫었다.
  - 📌 `git check-ignore -v`는 **부정 패턴에도 exit 0**을 내서 판정에 쓰면 안 된다. `git status`로 봐야 맞다.
  - ⚠️ 이번 커밋·푸시는 **훅에 맡기지 않고 손으로 했다** — 방금 만든 프로젝트 설정이 현재 세션에 아직 안 읽혔을 수 있어서다.
- **2차 조치 — 사용자 지적("내가 그 명령어를 학교에서 기억하겠냐? 니가 자동으로 넣어야지")이 옳았다.** 위까지는 "학교 가서 `git status` 쳐보세요"라는 **안내문**이었다. 그건 자동화가 아니다. 사람이 기억해야 하는 절차를 남기면 안 된다.
  - **`SessionStart` 훅 추가**: 더러우면 **먼저 커밋·푸시하고 그 다음 `pull --ff-only`**. 순서가 핵심 — pull을 먼저 하면 갇힌 작업이 충돌로 바뀐다. 구조가 일어나면 `systemMessage`로 "미커밋 파일 N개를 자동 커밋·푸시했습니다"를 띄운다.
  - **검증(드라이런)**: `commit`/`push`/`pull`을 `echo`로 바꿔 두 경우를 돌렸다. 더러운 트리 3건 → 커밋·푸시 → pull 순서 + JSON 메시지에 **3** 정확히 박힘 / 깨끗한 트리 → pull만, 메시지 없음. 두 훅 모두 `main` 가드·머지 가드·`exit 0` 종료 확인.
- ⚠️ **닭과 달걀 — 이게 이번 설계의 진짜 약점이다.** 훅은 **pull을 통해 도착하는데 그 pull을 훅이 하게 되어 있다.** 즉 **2026-08-18 이후 한 번도 pull 안 한 PC에는 훅이 없다.** 그 첫 세션 한 번은 훅이 못 지킨다.
  - ⇒ 그 한 번을 **`CLAUDE.md`가 메운다.** `CLAUDE.md`는 이미 양쪽 PC에 있고 세션 시작에 무조건 읽힌다. 그래서 영어·한글 양쪽 루틴 항목을 고쳤다: **"세션의 맨 처음은 `git status`다. 더러우면 읽기도 pull도 하기 전에 커밋·푸시한다."** 훅과 지시문의 **이중 방어**.
- **다음 할 일**: 학교 PC에서 Claude Code를 열기만 하면 된다 — `CLAUDE.md`가 `git status`→구조→`pull`을 시키고, pull 이후로는 훅이 자동으로 맡는다. (프로젝트 훅 첫 로드 시 승인 한 번 뜰 수 있음.) 거래내역 작업이 아직 거기 있으면 그 과정에서 자동으로 올라온다.
- ✅ 프론트·백엔드 코드 무변경 — **Apps Script 재배포 불필요.**

## 2026-08-13 (98) / — **`m.html` 2차: 대시보드 · 스크리너 · 은경저축 추가**
- 더보기에서 들어가는 하위 화면 3개를 붙였다. 탭바는 4개 그대로 두고 하위 화면에선 **'더보기'가 켜진 채** 상단에 `‹ 더보기` 버튼.
- **대시보드**: 월 1,000만원 목표 진행률 + D-Day(2029-02-28) + **12개월 막대**(초록=실제 입력, 연보라=배당주기 투영, 이번 달 강조) + 수익률 상위/하위 5.
  - 이를 위해 `projectMonthly()` 신설 — `portfolio.html:2255~2278` 대시보드 로직을 월 단위로 편 것. **계산 중복이 하나 늘었다**(설계문서 표에 추가 필요). 검증: 월배당 1종목 12개월 × 50,000 = **연 600,000**으로 `projectAnnualDist` 합계와 일치, 월평균 50,000.
- **스크리너**: `getEtfScreener` → 분배율 높은순, 검색 + 추종 필터, 상위 120건. 핵심3열(이름·분배율·현재가) + 펼침.
  - ⚠️ **결함 하나를 스스로 잡았다**: 처음엔 `oninput`에서 곧바로 `render()`를 불러 **글자마다 120행을 다시 그리고 커서가 튀었다.** → `scrType()`로 **250ms 디바운스 + 커서 끝 복원**. 검증: 'TIGER' 입력 후 필터 1건·포커스 유지·커서 끝.
- **은경 저축**: `getSavings` → 원금·만기일·금리, 만기 임박도 색(30일/91일/183일 구간은 `portfolio.html:3431`과 동일). 폰에서는 **조회만**, 수정은 PC.
- 📌 **분배금공지는 폰 화면을 안 만들었다 — 의도적이다.** `dist_notice.html`이 이미 있고 **820px 모바일 규칙도 이미 들어 있다**(`dist_notice.html:97`). 여기에 또 만들면 달력 코드가 **세 벌**이 된다(CLAUDE.md가 경고하는 바로 그 상황). 더보기에서 링크로 연결.
- 📌 **연금플랜도 PC 링크로 남겼다.** 퇴직시점·사학연금 입력·연차별 투영이 얽힌 **설계 도구**라 폰에서 만질 화면이 아니라고 판단했다. 필요하면 별도 작업으로.
- **검증**: 375px에서 4화면 전부 가로 밀림 없음 / 하위 화면 뒤로가기 표시 / 탭바 상태 유지 / 스크리너·저축은 계좌 데이터 없이도 열리도록 로딩 가드를 분리.
- **사용자 요청으로 탭바를 4개 → 6개로 늘림**(홈·종목·분배금·대시보드·스크리너·더보기). 대시보드·스크리너가 탭바로 올라와 `SUB`에는 은경저축만 남았다. 폰트 11→10px, 아이콘 17→16px.
  - 검증: **360px**(흔한 안드로이드 최소폭)에서 6칸 **글자 잘림 0건**, 7개 화면 전부 하이라이트가 정확히 1개만 켜짐(저축은 '더보기'가 켜짐), 가로 밀림 없음, 탭바 높이 51px.

## 2026-08-13 (97) / — **신규: 폰 전용 화면 `m.html` (1차)**
- **사용자 지시**: "폰으로 보는 버전의 화면을 따로 구현". 내가 "같은 파일 안에서 분기"를 권했으나 **사용자가 별도 파일을 재차 지시** → 그 결정에 따랐다. 설계: `docs/superpowers/specs/2026-08-13-mobile-view-design.md`.
- **사용자가 고른 것**: 폰에서도 PC와 같은 일(조회+편집) 전부 / 넓은 표는 **핵심 열 + 탭하면 펼침** / PC 파일을 폰으로 열면 **자동 이동**.
- **복제량이 걱정보다 작았다**: 폰은 열이 적어 렌더 코드가 PC보다 **단순**하다. 4,400줄을 베낀 게 아니라 같은 백엔드에 대고 **새로 짠 ~700줄**이다. 공유는 `API` 상수 + `api()`/`apiPost()`뿐.
- ⭐ **재로그인이 필요 없다**: 같은 오리진이라 `localStorage`를 공유 → (94)의 지문 `jjk_pw_v1`을 `m.html`이 그대로 읽는다. 폰에서 비번 재등록 없이 **첫 진입부터 빠른 경로**를 탄다. 스냅샷만 구조가 달라 별도 키 `jjk_msnap_v1`.
- **구성**: 하단 탭바 4개(홈·종목·분배금·더보기). 홈=합산+재남/은경+계좌목록 / 종목=핵심3열+펼침(수정·삭제) / 분배금=**연·월 고르고 그 달만 세로로 입력**(PC의 12개월 그리드는 폰에 안 들어간다). 2차 예정: 대시보드·연금플랜·분배금공지·은경저축·스크리너. 엑셀은 제외.
- ⚠️ **이 설계의 진짜 위험은 계산식 중복이다** — **평가금액·손익을 서버가 주지 않고 브라우저가 계산한다**(`renderAccountStats`, `portfolio.html:1365`). 레이아웃이 어긋나면 보이지만 **숫자가 어긋나면 안 보인다.** `m.html`의 계산을 한 블록에 모으고 원본 줄번호를 주석으로 박아뒀다. **고칠 때 반드시 양쪽 다.**
- **검증 — 같은 입력을 두 파일에 넣고 대조**(KRW 100주×10,000, 현재가 11,000 / USD 10주×$25, 시세없음, 환율 1400 / 7월 분배금 50,000, 월배당):
  - **10개 값 전부 일치** — 총투자 1,350,000 · 총평가 1,450,000 · 수익 +100,000(7.4%) · 분배 50,000 · 누적 3.70% · **환산 44.44%** · 재남/은경 4값. `projectAnnualDist`도 `{h1:600000,h2:0}`으로 동일.
  - 렌더: 375px에서 가로 밀림 없음, 탭바가 내용을 가리지 않음, 펼침 9항목+버튼 2개, 분배금 합계 일치.
- ⚠️ **미리보기 하네스의 한계**: 파일을 `data:` URL로 띄우므로 **상대경로 이동(`location.replace('m.html')`)이 동작하지 않아 자동 이동을 로컬에서 검증할 수 없다.** 실제 https 배포본에서 확인해야 한다. (`sessionStorage` SecurityError도 같은 이유의 아티팩트 — (96) 참고.)
  - 브라우저 창이 **외부 도메인을 정책상 차단**해 배포본을 열어보는 것도 불가. ⇒ 자동 이동은 **판정 조건만 9케이스로 따로 검사**(폰/경계 768/769/PC/`?pc=1`/`&pc=1`/무관 쿼리)했고 전부 의도대로였다. 실제 이동은 **사용자가 폰에서 한 번 확인**해야 최종 확인된다.
  - 📌 알아둘 느슨함: 정규식 `[?&]pc=1`이라 `?pc=12`도 매치돼 이동이 억제된다. 실사용에 문제없어 그대로 둔다.
- **배포 확인**: `portfolio.html`에 리다이렉트 포함, `m.html` 30,376바이트로 게시됨(지문키·탭바 포함).
- ✅ 프론트만 변경 — 같은 백엔드·같은 액션이라 **Apps Script 재배포 불필요**.

## 2026-08-13 (96) / — **포트폴리오 탭 모바일 대응. 그리고 ⭐배포 경로가 밝혀졌다: 이 저장소가 곧 GitHub Pages다**
- **발단**: 사용자가 폰(카톡 인앱 브라우저)으로 접속 → 상단 합산 카드가 "글자크기가 넘어가고 난리", 재남·은경 카드도 잘려서 안 보임.
- ⭐ **배포 경로 실측 확정 — 앞으로 이걸 전제로 판단할 것**:
  - 주소창엔 `jaenamking1-collab.github.io`만 보이는데 **카톡 브라우저가 경로를 숨긴 것**이다. 루트는 실제로 **404**(사용자 사이트 저장소 없음). 후보 4개를 훑어 확정: **`https://jaenamking1-collab.github.io/jjk/portfolio.html`** — 즉 **이 `jjk` 저장소가 그대로 Pages로 서비스된다.**
  - ⇒ **`git push` = 프론트 배포.** 별도 미러 없음. (`jjk-dist`는 공개 분배금공지 전용으로 따로 살아있다.)
  - 배포본에 `jjk_pw_v1`이 들어있는 걸 확인 → 배포본 = `main` 최신. 바이트 차 5,369는 줄 수와 정확히 일치(CRLF/LF)라 내용은 동일.
- **원인**: 이 탭엔 모바일 규칙이 **아예 없었다**. 파일 전체 `@media`가 단 하나이고 그건 분배금공지 전용(`portfolio.html:385`).
  - 합산 카드: 6칸 `min-width` 합 **710px** + 패딩 ≈ **870px** 필요한데 폰은 375px. 카드에 `overflow:hidden`이 걸려 **넘친 부분이 잘려** 사라졌다.
  - 재남/은경: `grid-template-columns:1fr 1fr`가 **하드코딩**이라 폰에서도 화면을 반으로 쪼갬 → 한쪽 190px에 5칸 지표줄 + 8열 표.
- **걸림돌**: 이 영역이 전부 **인라인 스타일** ⇒ 미디어 쿼리가 그냥은 못 이긴다. 클래스로 들어내는 안(~40줄 HTML 수정)은 **눈으로 검토하는 파일이라 노이즈가 크다**고 보고 기각 → **`!important` 오버라이드 1블록 + `id="personGrid"` 하나**로 끝냄. HTML 구조 무변경.
- **적용**(사용자가 두 선택지에서 고름): 합산 카드 **3칸×2줄**(숫자 20→14px, 세로줄 제거) / 재남·은경 **세로로 쌓기**(지표줄 3칸씩, 계좌 표는 `min-width:620px` + `overflow-x:auto`).
- ⚠️ **한 번 놓쳤다 실측으로 잡음**: 처음엔 합산 카드 글자만 줄이고 **인물 카드 16px을 안 줄여** `199,931,984204,971,590`처럼 옆칸과 붙었다. 스크린샷으로 발견 → `#personGrid … > div:last-child { font-size:13px }` 추가.
- **검증 방법(게이트 때문에 로그인을 못 하므로)**: 로컬 파일을 375×812로 열어 **게이트만 감추고** 화면에서 본 실제 숫자를 주입해 렌더만 확인. 자동 판정 3종 — **칸 넘침 0건**(16칸 전부 `자식 scrollWidth ≤ 칸 clientWidth`) / **표 좌우 스크롤 작동**(624>326) / **페이지 가로 밀림 없음**(`scrollWidth==innerWidth==375`). 스크린샷으로 눈 확인까지.
- ✅ **프론트만 변경 — Apps Script 재배포 불필요.**
- **이어서 메뉴도 정리**("동글하니 정렬도 이상하고 지저분하다. 글자만 넣자"): 알약 버튼을 걷어내고 글자만 남김 — 테두리·배경·`border-radius:999px`·탭별 7색(`nth-child` 규칙 7줄)·이모지 9개·`.tab-icon` 규칙 전부 제거. 활성 탭은 **보라색 + 굵게 + 밑줄**로 구분. `gap`을 `10px 20px`로.
  - 왜 지저분해 보였나: **폭이 제각각인 알약이 줄바꿈되면 오른쪽 끝이 들쭉날쭉**해진다. 글자만 남기면 같은 문제가 훨씬 덜하다.
  - **검증**: 폰 375px에서 **4줄 → 2줄**, 데스크톱 1280px는 **1줄**. 이모지 잔존 0(`\p{Extended_Pictographic}` 검사), 가로 밀림 없음. `.tab-icon`은 내 변경이 고아로 만든 것이라 같이 지웠다(다른 참조 없음을 grep으로 확인 — 10건 = CSS 1 + 버튼 9).
- **색 테마 기능 완전 삭제**("쓰지도 않는다"): `#paletteBar` 9줄 + `DEFAULT_VARS`/`PALETTES`/`applyPalette()` 13줄 제거.
  - **지워도 안전한 근거를 먼저 확인했다**: `applyPalette`는 **로드 시 호출되는 곳이 없다**(클릭 핸들러 6개가 전부). 기본 색은 전부 `:root`(`portfolio.html:11`)에서 오므로 스위처를 들어내도 화면 색은 그대로다. 실측으로도 `applyPalette남음:false`에 콘솔 오류 0.
  - 🔎 발견: `DEFAULT_VARS`의 `--accent`가 `#2563eb`인데 `:root`는 `#5b5bf0`이었다 — **'기본' 버튼이 진짜 기본이 아닌 다른 파랑을 넣고 있었다.** 어차피 삭제됐지만 기록해 둔다.
- ⭐ **헤더 겹침은 테마 바를 지우자 저절로 해결됐다 — 추가 CSS 0줄.** (96) 작성 시점엔 `@media` 블록이 필요할 거라 적었는데, 겹침의 원인이 테마칩 6개였다. **고치기 전에 원인을 다시 확인해서 불필요한 코드를 안 넣었다.**
  - 360px 실측: 로고 `24~212` / 환율 배지 `238~336` → **26px 여유, 겹침 false**, 헤더 밖 이탈 false, 가로 밀림 false.
  - 참고: 미리보기가 `data:` URL이라 `sessionStorage` SecurityError가 콘솔에 뜨는데 **샌드박스 아티팩트**다(실제 https 배포본엔 없음). 앞으로 이 하네스로 검증할 때 이 오류는 무시할 것.

## 2026-08-13 (95) / — **(94) 지문 게이트 실사용 확인 완료. 코드 변경 없음**
- 사용자가 앱에서 로그인 1회를 마쳤다고 알려와 콘솔로 실측 확인:
  - `localStorage['jjk_pw_v1']` = `{"s":"o+IAFs7mvUqFniDI+epShw==","h":"…"}` — salt **16바이트**, 해시 **32바이트(256비트)**, **비밀번호 문자열 없음**. 형식이 `pwSaveLocal`이 쓰는 것과 정확히 일치.
  - ⭐ **지문의 존재 자체가 서버 통과의 증거다** — `pwSaveLocal()`은 `gateCheck` 성공 뒤에만 호출된다(`portfolio.html:5352`). 별도 서버 로그를 볼 필요가 없었다.
  - `jjk_snap_v1`도 `true`. **빠른 경로는 조건이 둘**(`_snapGet() && await pwVerifyLocal(pw)`, `portfolio.html:5335`)이라 지문만 확인하면 부족했다 — 스냅샷까지 봐야 "다음 진입이 빠르다"고 말할 수 있다.
- ⇒ (94)의 "다음 할 일" 종료. 콜드스타트 대책은 두 갈래(`keepWarm` 트리거 + 지문 게이트) 모두 걸렸다.
- ✅ **(87) OKX NFT 알림도 가동 확인** — 사용자가 트리거 화면을 보여줘 판정. `okx_nft_alert` 프로젝트에 트리거 **1개**: 함수 `checkOnce` · 시간 기반 · **오류율 0%** · 최종 실행 `2026-08-13 00:53:49`(확인 시각 01:21 = **28분 전**) ⇒ `everyMinutes(30)`(`okx_nft_alert.gs:241`)이 제대로 돌고 있다. 실행이 성공한다는 것 자체가 **권한 승인도 끝났다**는 뜻이라 (87)의 ①③④가 모두 완료.
  - 📌 **여전히 미확인**: **폰에 실제 팝업이 뜨는지**. 이건 앱 화면으로만 확인된다 — 구글 캘린더 앱에서 `OKX NFT` 캘린더 **동기화가 켜져 있어야** 한다(새 캘린더는 기본 꺼짐인 경우가 있다). 레전더리 신규 매물이 나올 때까지는 조용한 게 정상이라, 침묵을 '고장'과 구별하려면 `sendTestEvent()`를 한 번 ▶실행해 2분 뒤 폰을 봐야 한다.

## 2026-08-08 (87) / — **신규: OKX NFT Legendary/Mystic 리스팅 → 구글 캘린더 알림 (`okx_nft_alert.gs`)**
- **요청**: OKX(Kaia)의 `puuvillasociety` / `puuvilla-fashionista` 두 컬렉션에 레전더리 매물이 올라오면 알림. 알림은 **메일 말고 캘린더**로(그래야 본다). 내용은 등급·KAIA·달러·한화.
- **데이터 소스를 실측으로 확정** (추측으로 안 짜려고 브라우저 네트워크를 떠서 실제 요청 본문을 캡처):
  - `POST /priapi/v1/nft/secondary/market`, 본문의 핵심 키는 **`projectIn`**(이걸 몰라 `projectId`/`slug` 등으로 5번 헛발질 — 전부 `code:0`에 빈 목록).
  - **인증·쿠키·API키 전부 불필요.** curl로 확인 ⇒ `UrlFetchApp`으로 그대로 됨. project ID: Society **748906**, Fashionista **2289945**.
  - `sortBy:"makeOrderTimeDesc"` = 최근 리스팅순. 응답에 Rarity·가격·USD·리스팅시각이 다 있어 **컬렉션당 요청 1건**이면 끝.
- **함정 2개를 실측으로 잡음**:
  - **`pageSize`는 50이 상한**. 100·200을 주면 에러 없이 **12건만** 온다(3회 재현). 모르고 올렸으면 조용히 매물을 놓쳤다.
  - 리스팅이 50건을 넘으면 오래된 매물이 창 밖으로 밀려 상태에서 사라졌다가 다시 들어올 때 **가짜 '신규' 알림**이 난다. 다행히 `makeOrderTimeDesc`가 **판매중을 앞에 몰아준다**(Society: 앞 42건 연속 판매중 → 뒤 8건 미판매). 그래서 **미판매가 섞인 페이지가 나오면 정지**하는 cursor 페이지네이션으로 해결. 지금은 1페이지로 끝나고, 늘면 알아서 늘어난다.
- **구현**: `okx_nft_alert.gs` (약 210줄). 30분 트리거 → 두 컬렉션 조회 → Legendary·Mystic 중 **신규 리스팅 / 가격 인하**만 골라 `OKX NFT` 전용 캘린더에 일정 생성(팝업 0분 전, 시작은 +2분 — 정확히 '지금'이면 팝업이 안 뜰 수 있음).
  - 제목에 정보를 다 넣음(폰 알림은 제목만 보임): `🆕 [Fashionista] Legendary #99 · 250,000 KAIA · $6,820 · 960만원`. 원화는 제목만 만원 단위, 본문은 원 단위.
  - 가격 비교는 **네이티브 KAIA 값**으로. USD로 비교하면 KAIA 시세가 내릴 때마다 가짜 '인하'가 난다.
  - 첫 실행은 **조용히 시드**(안 그러면 현재 매물 전부가 신규로 오폭). 조회 실패 회차는 **상태를 안 건드리고 건너뜀**. 일정 생성이 실패하면 **상태를 저장하지 않아** 다음 회차에 다시 알린다.
- ⚠️ **기존 `Code.gs`에 넣지 않았다** — (85)(86)에서 겪은 실행 슬롯 점유가 재발한다. **별도 Apps Script 프로젝트**로 분리(웹앱 배포도 불필요, 트리거만).
- **검증**: GAS 전역을 가짜로 채운 Node 하네스에 `.gs`를 그대로 올려 **실제 OKX·야후 응답**으로 4단계 통과 — 시드 0건 / 무변화 0건 / 항목 삭제 시 🆕 1건 / 가격 2배로 올려두면 🔻 1건(`↓500,000` 표시). 환율 야후 실값 1407.45, `$6,820 × 1407.45 = 9,599,506원` 본문과 일치. `node --check` OK.
- ⚠️ **다음 할 일(사용자)**: ① script.google.com에서 **새 프로젝트** 만들고 `okx_nft_alert.gs` 전체 붙여넣기 ② **`sendTestEvent()` 실행** → 권한 승인 → 2분 뒤 폰에 테스트 알림이 오는지 확인 ③ 안 오면 **구글 캘린더 앱에서 `OKX NFT` 캘린더 동기화 켜기**(새 캘린더는 기본 꺼짐인 경우가 있다) ④ `setupOkxTriggers()` 실행.
- 🔎 **미확인**: 판매자가 가격만 낮췄을 때 OKX가 `sale.lastTime`을 갱신하는지(새 주문으로 처리될 것으로 봄). 갱신 안 되더라도 페이지네이션이 전체 리스팅을 훑으므로 인하 감지 자체는 된다.

## 2026-08-12 (94) / — **콜드스타트 두 갈래로 해결: `keepWarm` 재설치(사용자) + 비밀번호 지문으로 게이트 즉시 통과**
- **사용자가 `setupKeepWarm()` ▶실행 완료** (`오전 1:08:29 keepWarm 트리거(5분마다) 설치 완료`). 이제 5분마다 1초짜리가 돌아 서버가 잠들지 못한다. (81)에서 경고했던 "옛 자기호출 버전이 다시 깔릴 위험"은 clasp로 원격=로컬을 확인했으므로 없었다.
- **문제의 나머지 절반 — 게이트가 콜드스타트를 정면으로 맞았다**: `submitGate()`가 `await gateCheck()`(서버 검증) 뒤에야 `hideGate()`·`bootApp()`을 부르므로, 캐시로 첫 화면을 즉시 그리는 `paintFromCache`가 **게이트에 갇혀 무력화**됐다. 비번 넣고 '확인 중…'만 20~60초 보게 됐다.
- **처음 제안했던 안은 폐기**: "캐시를 게이트 이전에 그리기"는 기기를 집어든 사람이 비번 없이 자산을 보게 된다. 사용자가 "이 PC만 그런 거지 다른 사람이 볼 순 없고?"라고 물어 범위를 다시 따져본 결과 **더 나은 안**이 나왔다 — 비밀번호가 맞는지는 **브라우저에서 즉시 판단할 수 있다.**
- **채택안(지문)**: 서버가 통과시킨 비밀번호만 **PBKDF2-SHA256 10만 회 + 16바이트 랜덤 salt** 지문으로 `localStorage`에 남긴다(비밀번호 자체는 저장하지 않는다). 다음 진입 때 **지문만 즉시 비교**해 화면을 열고, `getBootstrap`은 뒤에서 돌린다. 서버가 거부하면 `relockGate()`가 스냅샷·지문·세션토큰을 지우고 새로고침.
  - ⭐ **잠금 세기는 그대로다** — 비밀번호를 여전히 입력해야 하므로 기기를 집어든 사람은 아무것도 못 본다. 트레이드오프가 사라졌다.
  - 안전장치: 스냅샷이 **있을 때만** 빠른 경로를 탄다. `crypto.subtle`이 없는 환경이면 조용히 `false`를 반환해 **예전 서버 검증 경로로 떨어진다**(안 깨짐). 지문 저장은 **서버가 통과시킨 뒤에만** — 오답이 지문으로 굳지 않는다.
- **검증**: 원문에서 지문 함수를 떼어내 실제 `crypto.subtle`로 **26종 ALL PASS** — 맞는/틀린/빈 비번 / **저장값에 비밀번호 문자열 없음**·`{s,h}`만 / 지문 없음·깨진 값·`h` 누락 시 false(예외 없음) / **salt가 매번 달라 같은 비번도 지문이 다름** / `relockGate`가 3종을 다 지우고 새로고침 / 배선 순서(빠른 경로가 `gateCheck`보다 앞, 지문 저장은 서버 통과 후, `unauthorized` 검사가 `applyBootstrap`보다 앞, 기존 폴백 유지).
- ✅ **프론트만 변경 — 재배포 불필요**((93)의 기준: `doGet` 경로가 아니다).
- ⚠️ **다음 할 일**: 앱에서 **Ctrl+Shift+R** 후 로그인 1회(이때 지문이 저장된다) → **그다음 진입부터** 비번 입력 즉시 화면이 뜬다. 첫 1회는 예전과 동일하다(지문이 없으므로).

## 2026-08-12 (93) / — ⚠️ **내 오해 정정: `clasp push`는 웹앱에 반영되지 않는다.** 재배포 @147 + 쓰레기 행 필터
- ⚠️ **내가 틀린 것**: (88)에서 SOL 파서를 고치고 "재배포 불필요"라고 했다. **틀렸다.** `clasp push`(=편집기 저장)는 **트리거·수동 ▶ 실행에만** 즉시 반영된다. `getDistribution`은 `doGet`이 서비스하므로 **화면에 보이는 값은 배포본**이다 ⇒ 알림은 고쳐졌지만 **분배금공지 탭의 SOL 표시는 계속 깨진 상태**였다.
  - 실측 증거: 수정·push 후에도 `/exec?action=getDistribution&source=sol`이 그 **28,810자 쓰레기 행**을 그대로 돌려줬다.
  - **판단 기준(앞으로)**: 바뀐 코드가 `doGet`/`doPost` 경로에서 실행되면 **재배포 필요**. 트리거·수동 실행 전용이면 불필요. "액션 계약이 바뀔 때만"이라고 적었던 CLAUDE.md 문구는 너무 좁았다.
- **재배포**: clasp로 가능했다 — `clasp list-deployments`로 라이브 배포(`AKfycbwJS1Fd…` @145)를 찾아 `clasp redeploy`. 사용자 승인 후 **@146**. 공개 페이지도 같은 exec URL을 쓰므로 outward-facing 작업이라 먼저 물었다.
- **재배포 후에도 남은 쓰레기 1건 → 근본은 캐시**: `getDistribution`은 **분배캐시 시트의 최근 3회차를 합쳐** 반환한다(`attachDistHistory`). 새로 긁은 건 깨끗했지만(8월 11일 4건 + 7월 28일 19건), **옛 파서가 써놓은 캐시 행**(공시일 7월 28일)에 29,206자 행이 굳어 있었다. 파서를 고쳐도 캐시가 오염돼 있으면 계속 나온다.
- **수정(방어)**: `DIST_NAME_MAX = 60` + `_dropJunkItems()` 신설. 적용 지점 2곳 —
  - `attachDistHistory`: **병합의 단일 관문**(`getDistribution`·`getDistributionAll` 둘 다 통과)에서 신선분·이력분 모두 필터. 입력 객체를 훼손하지 않게 복사본에서 처리(캐시 시트에서 읽은 객체일 수 있다).
  - `finishDist`: `writeDistCache` **전에** 필터 → 쓰레기를 캐시에 굳히지 않는다. 전부 쓰레기면 저장 자체를 안 한다.
  - 상한 60자 근거: 실제 최장 종목명이 28자(`PLUS …`)라 여유가 크다.
- **검증**: `node --check` OK. 원문 추출 모의 테스트 **13종 ALL PASS**(29,000자 폐기 / 26자 유지 / 이력 병합 시 제거 / 이력 없을 때도 제거 / 입력 훼손 없음 / 캐시 저장 전 제거 / 전부 쓰레기면 미저장 / name 없는 행 안전). **라이브 실측**(@147): sol 44건 **이름최대 21자**, 60자 초과 **0건**, 6개사 전부 `stale=false`에 이름최대 21~28자. SOL 월중 4종목 티커 정상(0105E0·490490·493420·0080X0).
- 📌 **못 한 것 (사용자만 가능)**: **`setupKeepWarm()` ▶실행.** `clasp run-function`은 "API executable로 배포돼 있어야 함"이라 거부된다 → **트리거 설치는 편집기 ▶ 뿐이다.** 콜드스타트 대책이 아직 안 걸려 있다.
  - 콜드스타트 실측(20:09~20:22, 40회): 연결 재사용 38회는 **1.11~1.81초로 매우 일정**, 첫 요청 **17.41초**, 웜 중 스파이크 1건 8.56초, 그 직전 배치 첫 요청 **57.19초**. ⇒ 주범은 **새 연결의 첫 요청**.
  - 체감이 특히 나쁜 이유: **게이트가 콜드스타트를 정면으로 맞는다.** `submitGate()`가 `await gateCheck()` 뒤에야 `hideGate()`·`bootApp()`을 부르므로, `paintFromCache()`의 즉시 렌더가 **게이트에 막혀 무력화**된다. 캐시를 게이트 이전에 그리면 해결되지만 잠금이 약해지는 트레이드오프라 사용자 결정 대기.

## 2026-08-12 (92) / — **clasp 연결 완료. 이제 Code.gs 수동 붙여넣기 없음 — `clasp push` 한 줄**
- **배경**: 이날 Code.gs를 **네 번** 붙여넣었다(알림 결함 → SOL 파서 → `_EXEC_URL` → 워치독). 사용자가 "나보고 하라고?"라고 지적 — 맞는 지적이라 내가 세팅했다.
- **한 것**: clasp 3.3.0 설치 / `scriptId`는 **Drive에서 직접 찾음**(`포트폴리오관리` = `1yYeK3W1aHUY…`). 나머지 두 프로젝트(`분배금공지배포용`·`okx_nft_alert`)도 Drive에 있는 걸 확인했고 **건드리지 않았다**.
- ⭐ **`.claspignore`가 핵심**: 없으면 clasp가 루트의 모든 `.gs`/`.html`을 이 프로젝트로 밀어넣는다. `okx_nft_alert.gs`는 별개 프로젝트(합치면 30분 트리거가 실행 슬롯을 훔친다, (85)·(86))이고 `public_dist_proxy.gs`도 별개이며 `portfolio.html`·`dist_notice.html`은 프론트다. `**/**` 후 `!Code.gs`·`!appsscript.json`만 되살림 → `clasp status`로 검증(추적 `Code.gs`·`appsscript.json` 둘뿐).
- ⚠️ **`clasp pull`을 저장소에서 그냥 돌리면 로컬 Code.gs를 옛 원격본으로 덮어써 작업이 날아간다.** 그래서 **임시 폴더로만 pull**해서 매니페스트만 꺼내고 원격 Code.gs는 비교용으로만 썼다. 원격 파일은 `Code.js`로 내려온다(clasp가 서버 코드를 로컬에서 `.js`로 쓴다 — 원격 파일명은 `Code`라 push 대상은 일치).
- **`appsscript.json`은 원격에서 받은 것을 그대로** 넣었다(직접 쓰면 `webapp.access`·`executeAs` 같은 배포 설정을 바꿔버릴 위험). 실제 값: `timeZone Asia/Seoul`, `runtimeVersion V8`, `executeAs USER_DEPLOYING`, `access ANYONE_ANONYMOUS`.
- **대조 결과**: 원격·로컬 **3415줄 / 함수 133개 동일**, 원격에만 있는 함수 **0개**(편집기에서 직접 만든 코드가 없다 = push가 아무것도 지우지 않는다). 바이트 차 3,415는 줄 수와 정확히 일치 → CRLF/LF 차이뿐. `clasp push` 성공(`Pushed 2 files`).
- **사용자가 한 것은 `clasp login` 브라우저 '허용' 1회뿐.** 비밀번호는 내가 다루지 않았고, Apps Script API 토글은 **이미 켜져 있었다**(pull이 성공한 것이 그 증거).
- **앞으로**: `Code.gs`를 고치면 **`clasp push`까지 내가 한다.** 트리거·수동 실행은 저장된 코드로 도니 즉시 적용, 웹앱(`/exec`)만 재배포 전까지 옛 버전 — 지금까지 방식과 동일. CLAUDE.md에 기록했다.
  - ⚠️ **다른 PC(집/직장 중 나머지)에서는 `clasp login`을 한 번 더** 해야 한다(인증은 머신별, `.clasprc.json`은 홈 디렉터리에 있고 커밋 안 됨).

## 2026-08-12 (91) / — **워치독 신설: '침묵'과 '정상'을 구별하게 만들었다** (Gmail 이상 보고 + 월요일 카톡 하트비트)
- **사용자 확인**: (90) 수정 후 **"공지 날라옴"** — `_EXEC_URL` 복구가 실제 발송으로 확인됐다. 이어서 하트비트 구현 승인("만들어" → "ㄱㄱ").
- **설계 문서**: `docs/superpowers/specs/2026-08-12-dist-alert-watchdog-design.md` (커밋 `3566306`). 사용자 선택 = **Gmail 이상 보고 + 주1회 정상신호**, 점검 항목 **4개 전부**.
- **왜 Gmail인가**: 감시 대상과 같은 통로(카톡·캘린더)로 보고하면 그 통로가 죽었을 때 그 사실도 못 알린다 — 8/5~8/12이 정확히 그 상황이었다. `MailApp`은 독립된 구글 서비스라 같이 죽지 않는다. 수신자는 `Session.getEffectiveUser().getEmail()`로 하드코딩하지 않는다.
- **구조**: 단일 함수 `distWatchdog()` + **매일 08:30** 트리거 1개(08:10 flush 뒤에 둬서 밤새 대기분이 먼저 나갈 기회를 준다). 30분마다 도는 `checkDistNotices`에 끼워넣는 안은 **거부** — 6개사 force 213초짜리에 붙이면 6분 한도 위험이 커진다.
- **점검 4개**: ① 발송 전제(`_EXEC_URL`이 https 문자열인지 · 카카오 키 2개 · 캘린더 접근) ② **대기열 적체** — 큐엔 타임스탬프가 없으므로 '비어있지 않은 걸 처음 본 시각'을 `WD_QUEUE_SINCE`에 적어 나이를 재고 **6시간** 초과면 이상 ③ 파서 **0건·stale** ④ **공지 지연** — 공지창인데 이번 달 공시일 없음, 단 **창 시작 후 3일** 지나야 판정(월중 11일·월말 26일부터)해 오탐 방지.
- **하트비트**: 월요일엔 이상이 없어도 `sendKakaoMemo()`를 **직접**(대기열 우회) 호출해 6개사 상태 1통. 정상 신호가 분배 알림에 섞이지 않고, **실패하면 그 사실을 이상으로 잡아 Gmail로** 보낸다. ⇒ 통로가 죽으면 사용자에게 침묵이 아니라 '월요일 신호가 안 왔다'로 드러난다.
- **소음 억제**: 이상 조합의 지문을 `WD_LAST`에 저장해 같은 지문은 **3일 간격**으로만 재발송. 지문이 바뀌면 즉시. 이상이 사라지면 `WD_LAST` 삭제.
- **검증**: `node --check` OK. 원문에서 `distWatchdog`을 떼어내 모의 객체(Properties·Mail·Calendar·Session·getDistributionAll)로 **36종 ALL PASS** — 전제조건 3종 / 큐 4단계(비움·최초발견·2시간·7시간) / 0건·stale·중복보고 안 함 / 지연 5케이스(9일·11일·25일·26일·창밖) / 메일 5종 + 재발송 억제 4종 / 월요일 하트비트 4종 / **메일 자체가 실패해도 죽지 않고 `WD_LAST`를 안 남겨 다음에 재시도**.
- ⚠️ **다음 할 일(사용자)**: Code.gs 붙여넣고 저장 → **`setupWatchdogTrigger()` ▶실행**(1회). **첫 실행 때 구글이 Gmail 발송 권한을 묻는다 — 허용해야 한다.** 그리고 `distWatchdog()`를 직접 ▶실행해 콘솔에 `워치독: 이상 0건`과 6개사 줄이 찍히는지 확인. 재배포 불필요.
  - 참고: 지금은 대기열이 비었고 6개사 다 정상이라 **이상 0건**이 나와야 정상이다. 이상이 뜨면 그게 진짜 남은 문제다.

## 2026-08-12 (90) / — 🎯 **범인 확정: `_EXEC_URL` 미선언. 8/5부터 알림 발송이 전면 사망했다.** 내가 (80)에서 지운 것
- **`_diagDistAlert()` 출력이 결정타**: 대기열에 **7건이 두 채널 모두**에 갇혀 있었다 — `⚠️ PLUS 회차 미상 10건`(8/10), `SOL 8월 11일`, 그리고 오늘 KODEX·TIGER·ACE·RISE·PLUS. 그런데 **카톡키=있음 / 리프레시토큰=있음 / 캘린더 접근=정상**. 자격증명이 다 멀쩡한데 두 채널이 동시에 실패 ⇒ **공통 원인**이 있다는 뜻.
- **공통 원인 = `_EXEC_URL`**. `sendKakaoMemo`(카톡 링크)와 `flushCalPending`(캘린더 설명)이 둘 다 이걸 쓴다. grep 결과 **사용 2곳, 선언 0곳** → 런타임 **ReferenceError**.
  - 카톡: `sendKakaoMemo` 안에서 터짐 → `flushKakaoPending`의 try가 잡음 → `ok=false` → 대기열 유지.
  - 캘린더: `const desc = … + _EXEC_URL` 이 **`try` 밖**이라 예외가 함수 밖으로 튀어 `_notifyCal` → `checkAndLogAlerts`까지 올라감 → 대기열 유지.
- **git 이력으로 확정**: `f143cd0`(7/28)이 keepWarm과 함께 선언을 추가했고, **`d9cb46b`(8/5, "블로킹 원인 제거: keepWarm 자기호출 삭제")가 자기호출 코드를 지우면서 선언까지 같이 삭제**했다. 사용 2곳은 그대로 남았다.
  - ⚠️ **내 검증이 부실했던 지점**: WORKLOG (80)에 *"`_EXEC_URL`은 카톡 링크·캘린더 설명에서 계속 쓰이므로 유지(고아 아님 — grep으로 확인)"*라고 적었다. **grep으로 확인한 것은 '사용처가 존재한다'였고 '선언이 살아남았다'가 아니었다.** 참조를 지울 때는 선언이 여전히 필요한지 **선언 쪽을** 봐야 한다. GAS는 문법 검사가 없어 미선언 전역이 조용히 런타임까지 간다.
  - ⇒ 그래서 (87)의 결함 A(발송 전 메타 덮어쓰기)와 겹쳐 **8/5~8/12 전 기간 알림이 조용히 사라졌다.**
- **수정 3건**: ① `_EXEC_URL` 선언 복구(지우지 말라는 경고 주석 포함) ② `flushCalPending`의 `desc` 계산을 **try 안으로** 이동 + 실패 시 `false` 반환 ③ `flushKakaoPending`/`flushCalPending`이 **성공 여부를 반환**(빈 대기열은 true) → 호출부·진단이 '갔는지'를 판별할 수 있다.
- **점검 수단 2건 추가**: ① `_diagDistAlert()`에 **`①-2 _EXEC_URL` 확인** 줄 — 같은 미선언 사고가 즉시 드러난다. ② **`_diagSendNow()`** 신설 — 진단만 하는 `_diagDistAlert`와 달리 **실제로 발송**해 채널별 ✅/❌와 대기열 비움 여부를 찍는다(대기열이 비면 점검용 1건을 넣어 보낸다).
- **검증**: `node --check` OK. 원문에서 발송 경로를 떼어내 모의 객체로 **19종 ALL PASS** — 선언 존재·사용처보다 앞 / `desc`가 try 안 / 정상 시 true·대기열 비움·설명에 링크 포함 / 캘린더 실패 시 false·대기열 유지 / **회귀: `_EXEC_URL`이 다시 미선언돼도 예외가 함수 밖으로 새지 않고 false로 보고** / `_diagSendNow`가 두 채널 flush 호출.
- ⚠️ **다음 할 일(사용자)**: Code.gs 붙여넣고 **저장** → **`_diagSendNow()` ▶실행**. 성공 기준 — `카톡: ✅ 발송됨` / `캘린더: ✅ 발송됨`이 찍히고, **갇혀 있던 7건이 실제로 카톡 1통 + 캘린더 일정 1건으로 도착**한다. 재배포 불필요.
- 📌 **남은 제안(미구현)**: 하트비트. 지금은 '침묵'과 '정상'이 구별되지 않는다. 주 1회 '정상' 신호를 보내면 통로가 죽었을 때 침묵이 아니라 **신호 없음**으로 드러난다.

## 2026-08-12 (89) / — **알림 미수신 원인 확정: 감지·기록은 성공, 발송만 죽었다.** DB 시트를 Drive로 직접 읽어 판정
- **방법(앞으로 이걸 쓸 것)**: 앱 DB 스프레드시트를 **Google Drive 커넥터로 직접 읽었다**(`read_file_content`, fileId=`1iNlOU1YBRyJ…`). `getHoldings`·`getAlerts`는 APP_TOKEN이 필요해 내가 못 부르지만, **시트는 토큰 없이 읽힌다.** 사용자에게 콘솔 스니펫을 시킬 필요가 없다.
- **`알림로그` 실측 — 오늘 감지는 완벽했다**:
  ```
  2026-08-12 9:21 | KODEX | 신규공지 | 공시일 8월 12일 (월중) | 신규
  2026-08-12 9:21 | TIGER | 신규공지 | 공시일 8월 12일 (월중) | 신규
  2026-08-12 9:21 | ACE   | 신규공지 | 공시일 8월 12일 (월중) | 신규
  2026-08-12 9:21 | RISE  | 신규공지 | 공시일 8월 10일 (월중) | 신규
  2026-08-12 9:22 | PLUS  | 신규공지 | 공시일 8월 11일 (월중) | 신규
  ```
  ⇒ 감지 ✅ / 알림로그 기록 ✅ / 상태 전부 `신규`라 **앱 🔔 패널엔 지금도 떠 있다** / **카톡·캘린더 발송만 ❌**. 파싱·가드·감지 쪽을 더 파는 건 의미 없다.
- **(87) 결함 A가 실제로 발동한 것이 확인됨**: `_파서메타`가 `kodex 8월 12일 … updated 2026-08-12 13:20`으로 이미 갱신돼 있다. 9:21에 발송이 실패했는데 옛 코드가 메타를 먼저 덮어써서, 이후 30분마다 도는 실행은 전부 '변화 없음'으로 보고 **재시도조차 하지 않았다.** (87)의 수정(발송 성공 시에만 메타 갱신)이 정확히 이걸 막는다.
- **남은 미확인 1개**: 카톡·캘린더 중 무엇이 왜 실패했는지. `KAKAO_PENDING`·`CAL_PENDING`은 **ScriptProperties라 시트가 아니어서 Drive로 못 본다** → `_diagDistAlert()` ▶실행 필요. 1순위 후보는 **카카오 리프레시 토큰 만료**(약 2개월 주기).
- **보유 판정(사용자 지적 반영, 내가 직접)**: 보유 65행 중 SOL은 **`SOL 팔란티어커버드콜OTM채권혼합`(0040Y0, 1000주) 단 하나**이고 `SOL_TICKER`에 이미 있다. (88)에서 매핑 없다고 보고한 5종목(미국나스닥100·200 Top10·KRX기후변화솔루션·미국S&P500ESG·KRX300)은 **전부 미보유** ⇒ 추가 불필요.
- 🔎 **구조적 문제(사용자 지적: "왜 매번 내가 먼저 아냐")**: 알림 파이프라인에 **자기점검이 없다.** ① 실패를 알리는 통로가 고장난 그 통로다. ② `_addAlert` 중복억제로 같은 경고는 **딱 한 번만** 기록된다. ③ 그 기록을 보는 곳은 앱 🔔 패널뿐. 실제 증거 — `2026-08-05 16:43 | SOL | 파싱경고 | SOL 자사 파서 실패 — 뉴스사이트로 우회 중`. **시스템은 8/5에 이미 알았고 한 번 찍은 뒤 조용해졌다.** 그 사이 SOL 두 회차가 계속 0건이었다. → **하트비트(정상일 때도 주기적으로 '정상' 신호를 보내 통로 생존을 증명)** 도입을 제안했으나 아직 미구현.

## 2026-08-12 (88) / — **SOL 파서 수정: OCR 문제가 아니라 정규식이 형식 변화를 못 따라간 것**(월중·월말 둘 다 깨져 있었음)
- **사용자 지적**: "솔은 블로그에 잘 있잖아, OCR로 읽기로 했는데 왜 안 읽어."
- **정정할 전제**: **SOL 파서엔 OCR 경로가 아예 없다.** `_solParsePost`는 텍스트 정규식만 쓰고, 주석([Code.gs](Code.gs) `// 본문이 HTML 표(텍스트)라 OCR 불필요`)도 그렇게 적혀 있다. OCR을 쓰는 건 **KODEX·ACE·PLUS**뿐(파싱 결과에 `_ocr:true`가 그 셋에만 붙는다). 그리고 이번에 실제 글을 받아보니 **표가 진짜 텍스트로 있다** — OCR이 필요한 상황이 아니었고, 텍스트 정규식이 안 맞았을 뿐이다.
- **직접 확인**(공개 액션이라 토큰 불필요): 오늘 글 `blog.naver.com/soletf/224375206507` 200 OK, `se-main-container` 있음, `분배금 내역` 있음, `SOL ` 31회. 본문 추출도 정상(33,449자), 일정 4개(`공시일 8월 11일`·`분배락일 8월 13일`·`기준일 8월 14일`·`지급일 8월 18일`)도 잡힌다. **표만 못 읽었다.**
- **원인 2건 (실측)**:
  - ① **월중 공지는 분배율 칸을 통째로 비운다.** 실제 텍스트: `1 SOL 코리아고배당 60 2 SOL 미국배당미국채혼합50 21 …`. 예전 정규식은 금액 뒤에 **소수 분배율 또는 '업데이트 예정'을 필수**로 요구했다. 주석대로 예전엔 SOL이 빈칸에 '업데이트 예정'이라고 **써 줬는데**, 지금은 글자 없이 비운다 → 전 행 불일치.
  - ② **월말 공지는 분배율에 `%`가 붙는다**(`0.28%`). 소수만 받으니 여기도 0건. ⇒ **월중·월말 두 회차가 다 깨져 있었다.**
  - ③ 게다가 이름을 `.+?`로 열어둔 채 분배율을 필수로 두면, 실패 시 `.+?`가 페이지 끝까지 달아나 **종목명에 본문·댓글·스크립트가 다 들어간 쓰레기 1건**을 만든다(실측 `name` 길이 **28,810자**). 즉 4행을 놓치고 쓰레기 1행을 만들고 있었다.
- **수정**: `%`를 먼저 떼고(`replace(/(\d)\s*%/g,'$1')`), 분배율은 **선택**으로, 이름은 `[^※]{1,40}?`로 묶고, 행 끝을 **다음 행 번호(`\s+\d{1,2}\s+SOL\s`) 또는 표 끝 `※`**로 확정한다. 폭주 방지 + 두 형식 동시 지원.
- **검증**: 실제 글 3건을 저장해 `_solParsePost`를 **원문에서 떼어내** 돌림 — **13종 ALL PASS**. 오늘 8월 중순 **4건**(rate=null) / 7월 월말 **19건**(`0.28%`→`0.28`) / 7월 중순 **4건**(0.46). 종목명 전부 40자 이하, 전부 `SOL `로 시작, amount>0, 일정 4개 일치, **오늘 4종목 티커 전부 매핑**(0105E0·490490·493420·0080X0). `node --check` OK.
- 🔎 **보고만 (안 고침)**: 월말 19종 중 `SOL 미국나스닥100`·`SOL 200 Top10`·`SOL KRX기후변화솔루션`·`SOL 미국S&P500ESG`·`SOL KRX300`이 **`SOL_TICKER`에 없어 ticker 빈값**이다(표시는 되지만 보유 매칭이 안 된다). 보유 종목이면 추가 필요.
- ⚠️ **다음 할 일(사용자)**: **Code.gs 붙여넣고 저장**(트리거·공개 액션 모두 저장된 코드로 도니 재배포 불필요). 성공 기준 — 분배금공지 탭에서 **SOL 월중 4종목·월말 19종목이 보이고**, `_diagDistAlert()`의 sol 줄이 `종목 0건` 경고 없이 공시일을 갖는다.

## 2026-08-12 (87) / — 내가 만든 로그인 불가 수정 + **분배 알림 유실 결함 2건**(발송 전 메타 덮어쓰기·중복체크 열 오류)
- ⚠️ **먼저 내가 만든 사고**: (86)에서 넣은 **20초 타임아웃이 `getBootstrap`을 끊어 로그인 자체가 안 됐다.** `getBootstrap`은 계좌·보유·분배·종목목록·환율·알림을 한 실행에서 다 읽는 **앱 최대 무게 읽기**인데 `SLOW_ACTIONS`에 안 넣었다. 느린 구간에 20초를 넘기면 정상 작동 중인 요청을 3번 연달아 끊고 → `gateCheck` throw → '서버에 연결할 수 없습니다'.
  - **수정**: `SLOW_ACTIONS` 구분을 없애고 **`API_TIMEOUT = 90000` 백스톱 하나**로. 증거가 확실한 부분(404 등 나쁜 상태코드면 재시도)만 남긴다. **잘 돌고 있는 요청은 끊지 않는다.** 20초는 상태 좋은 구간의 측정치였고 가장 무거운 액션에 쓸 값이 아니었다. → 사용자 확인 "로그인은 됬는데".
  - **교훈**: 타임아웃 값을 정할 땐 **가장 무거운 경로의 최악 시간**을 기준으로 잡아야 한다. 평균으로 잡으면 그 경로가 죽는다.
- 🔁 **(86) 날짜 혼동 정정**: (86)의 측정·로그는 **8/6 당시 실시간**이었다(그날 day=6이라 `checkDistNotices`가 공지창 밖으로 조기 return → 실행기간 0.66~2.0초가 그것). 이 세션이 **8/12에 이어지면서** 내가 그 로그를 오늘 것으로 읽어 "6일이라 창 밖"이라고 오진했다가, 다시 "사용자가 6일 전 로그를 줬다"고 잘못 정정했다. 둘 다 아니다 — **각 날짜 기준으로 둘 다 맞았고, 내가 두 날짜를 섞었다.** 날짜는 로그 화면이 아니라 `Get-Date`로 확인할 것.
- **"오늘 9시 공지 떴는데 알림 안 옴" 조사** — `getDistribution`은 공개 액션이라 토큰 없이 내가 직접 확인했다.
  - **파싱은 정상**. 오늘(8/12) 월중 공시일이 다 들어와 있다: KODEX·TIGER·ACE **8월 12일**, RISE 8월 10일, PLUS 8월 11일. `_fingerprint`를 원문 그대로 옮겨 재현 → `fp.pubDate`가 전부 8월 값 ⇒ **감지 조건은 충족**. 가드도 전부 통과(day=12 창 안, 9시 → 09~18 통과, 발송창 08~23 통과).
  - **⇒ 파싱·가드가 아니라 발송/실행 쪽**이라는 뜻. 코드에서 결함 2건 확정:
  - **결함 A (영구 유실)**: `checkAndLogAlerts`가 **알림을 보내기 전에 `_파서메타`를 덮어썼다**. 발송이 실패해도 메타는 이미 새 공시일이라, 다음 실행부터 `fp.pubDate === prev.pubDate` ⇒ **그 공지는 영구히 다시 알림되지 않는다.** 발송은 `catch`로 삼켜 `console.log`만 남으니 **조용히** 사라진다.
    - **수정**: 발송을 **먼저** 하고, **한 채널이라도 접수됐을 때만** 메타를 갱신. 둘 다 실패하면 메타를 그대로 둬서 30분 뒤 재시도. 반환값에 `notified`·`accepted` 추가.
  - **결함 B**: `_addAlert`가 `getRange(..., 5)`로 **5열만 읽고 `r[5]`(상태)를 비교** → 항상 `undefined`라 `r[5] !== '확인'`이 늘 참. "확인 처리한 알림은 다시 알린다"는 의도가 죽어 있었다. → **6열로 수정**.
  - **추가**: `_diagDistAlert()`를 '수동 실행' 맨 아래에 신설. ▶실행 한 번으로 ①트리거 ②가드 ③대기열(KAKAO_PENDING·CAL_PENDING)+카톡키·캘린더 접근 ④저장된 메타 vs 지금 공시일 ⑤알림로그 최근 5건을 다 찍는다. **실행 기록·시트는 사용자 구글 계정 안이라 내가 볼 수 없어서**, 네 갈래(트리거 없음/가드 막힘/대기열에 갇힘/이미 소비됨) 중 어디서 끊겼는지 사용자가 한 번에 보게 만든 것.
- **검증**: `node --check` OK(.gs는 확장자 때문에 .js로 복사해 검사). 원문에서 발송·메타 블록을 그대로 떼어내 모의 객체로 **13종 ALL PASS** — 두 채널 실패 시 메타 미갱신 / 한쪽만 성공 시 갱신 / 새 공지 없을 때 갱신 / 발송이 `setValues`보다 앞 / `_addAlert` 6열.
- 📌 **실측 참고**: 6개사 `force` 스크랩 합계 **213초**(kodex 16.0 · tiger 36.4 · ace **84.6** · rise 27.1 · plus 8.3 · sol 40.8). 트리거 한도 6분(360초)에 위험하게 가깝다. 공지가 새로 올라온 날은 OCR 캐시가 비어 더 걸린다 — `checkAndLogAlerts`가 한도에 걸려 죽으면 알림이 안 나간다. 아직 확정된 원인은 아니지만 후보로 남긴다.
- 🔎 **보고만 (안 고침)**: ① **SOL이 `getDistribution` items 0건** — 파서 사망. ② `getEtfNoticesAll`에서 **TIGER·PLUS가 `stale:true`, items 0건** (분배 파서는 멀쩡한데 공지목록 파서만 죽음). ③ `dist_notice.html:195`의 무방비 `api()`. ④ `loadAccountStats`의 `Promise.all` 4건 중 2건에 `.catch` 없음.
- ⚠️ **다음 할 일(사용자)**: ① **Code.gs를 편집기에 붙여넣고 저장** — 트리거는 배포본이 아니라 저장된 코드로 도니 **재배포 불필요**((80) 확인). ② **`_diagDistAlert()` ▶실행** 후 출력 알려주면 오늘 알림이 어디서 끊겼는지 확정한다. 특히 **대기열이 비어있지 않으면** 감지는 됐고 발송이 막힌 것.

## 2026-08-06 (86) / — **"엄청 느리게 열린다" 원인 확정 = 구글 응답 배달 404. `api()`에 타임아웃·재시도 추가**
- **증상**: 열 때 매우 느림. 실행 기록 4:22:47에 doGet 6건이 몰려 **8.5·17.6·29·54·56.6초 + 실패 1건**, 그 뒤 4:23:28부터는 전부 0.4~4.5초.
- **가설 4개를 실측으로 전부 기각**(추측으로 안 고치려고 순서대로 쳐냄):
  - ~~`getLivePrices` 폴백~~ — `prices=35`, **4.76초**. 어제 (83)에서 만든 경로 정상. 네이버 0.16초·야후 0.1~0.4초로 소스도 멀쩡.
  - ~~같은 스프레드시트 경합~~ — 내가 세운 가설이었는데 **정반대**. 같은 시트 4건 동시 = **3.24초**. (`getLivePrices`가 `getHoldings()`를 불러 SHEET_ID 레인으로 들어온 건 사실이나 원인 아님.)
  - ~~트리거 블로킹~~ — 스파이크 시각과 겹치는 트리거 없음. ~~공개 페이지 트래픽~~ — 방문 카운터 누적 **14**, 오늘 1.
  - ~~콜드스타트~~ — **5분 유휴 후 단독 1건 = 1.75초**. 유휴 후 병렬2 = 3.31초. 유휴는 무관.
- **범인**: 요청별 상태코드를 같이 찍자 드러남 — **느린 요청은 예외 없이 404**, 200은 전부 1~4초.
  ```
  hitCounter n=4:  1.68/200  1.81/200  1.87/200  32.93/404
  zzz        n=4:  1.50/200  1.69/200  51.21/404  57.59/404
  hitCounter n=6:  1.31 1.43 1.55 3.00 3.12 10.59  전부 200 → 10.6초
  ```
  동시 6건도 전부 200이면 10.6초다 ⇒ **동시성이 원인이 아니라, 일부 요청이 25~57초 끌다 404를 뱉는 게 원인**(동시에 쏠수록 그중 하나가 걸릴 확률만 오름). 같은 시각 **서버 실행 기록은 0.5~0.8초 '완료됨'** ⇒ 서버는 끝냈는데 `/exec`→`script.googleusercontent.com` **배달 구간**이 깨진 것. 구글 쪽이라 백엔드로는 못 고침. **간헐적**(어떤 창에선 16건 중 4건, 다른 창에선 40건 중 0건).
- **왜 앱이 무너졌나**: `api()`에 타임아웃·재시도·상태코드 확인이 **전혀 없었다**. 404 본문은 JSON이 아니라 `res.json()`이 throw → ① `fetchPriceItems`가 `.catch`로 삼키고 **`getSheetData`(10~50초) 폴백**으로 떨어짐(30초+40초 이중), ② `loadAccountStats`의 `Promise.all`이 통째로 reject → **화면 자체가 안 그려짐**. 어제 액션을 아무리 빠르게 만들어도 404 한 건이 옛 느린 경로를 도로 불러왔다 — "어제 한 게 무색"의 정체.
- **수정(`portfolio.html` 한 곳)**: `api()`에 **상태코드 확인 + 타임아웃 + 최대 3회 재시도**. `SLOW_ACTIONS`(시트 재계산·6개사 스크랩 등 8종)는 90초, 나머지는 20초 — 관측상 정상 200은 1~4초, 404 지연은 25~57초라 20초가 둘을 가른다. **재시도는 `api()`가 읽기 전용에만 쓰이므로 안전**하고, 쓰기(`apiPost`)는 손대지 않았다.
- **검증**: 원문에서 `api()`를 그대로 떼어내 모의 fetch로 **12종 ALL PASS** — 문법 / 404 뒤 재시도 복구 / 계속 404면 3회 후 throw / 무응답 20.0초에 끊고 재시도 / `getSheetData` 22초 응답은 안 끊김 / 일반 액션 22초는 끊고 재시도 / 토큰·action URL 적재.
- ✅ **재배포 불필요** — 프론트만 고쳤다. `Code.gs`는 변경 없음.
- 🔎 **보고만 (안 고침)**: ① `dist_notice.html:195`에 같은 무방비 `api()`가 있다. 거기서 쓰는 액션은 전부 느린 액션이라 타임아웃 이득은 작고, 고치면 jjk-dist 수동 미러 작업이 생겨 사용자 판단에 맡김. ② `loadAccountStats`의 `Promise.all`은 4건 중 2건에 `.catch`가 없어 한 건만 실패해도 화면이 빈다(재시도가 방아쇠를 줄여주긴 함). ③ `gateCheck`의 자체 재시도가 이제 중복이다 — 최악 120초까지 '확인 중…'이 될 수 있으나, 예전엔 타임아웃이 아예 없어 무한 대기였으므로 그래도 개선.
- ⚠️ **다음 할 일**: 앱에서 **Ctrl+Shift+R로 새로고침**한 뒤 체감 확인. 성공 기준 — 열 때 30초 이상 멈추는 일이 사라지고, 느린 순간에도 20초 안에 재시도로 붙는다. 그래도 느리면 **실행 기록의 기간이 아니라 브라우저 Network 탭의 상태코드**를 볼 것 — 이번 교훈: 서버 기간만 보면 절대 안 잡힌다.

## 2026-08-05 (85) / — 재배포 후 검증: getLivePrices는 성공, **그런데 블로킹 재발** → 내가 만든 위험에 가드 추가
- **`getLivePrices` 성공**: 라이브 응답 **35종목 전부**(국내 29 + 해외 6), 값 정확(005380 404,500 +3.06% / TSLA 322.91 -1.36%). 페이로드 **1,896B**(기존 `getSheetData` 15,139B). 콜드 28.6초 후 **웜 1.72초**.
- **⚠️ 그런데 블로킹 재발**: 3라운드 실측에서 `zz_noop` **66.8초**, `getNavMap`(1시간 캐시된 가벼운 액션) **41~52초**, 부팅 24/44/46초. 시트를 안 건드리는 요청까지 느리므로 또 **실행 슬롯 점유**.
- **내가 만든 위험일 가능성**: (79)에서 `refreshAllDistributions`의 `return` 버그를 고쳤는데, **그 버그 덕분에 이 트리거는 그동안 사실상 아무 일도 안 하고 있었다.** 고친 뒤로는 진짜로 **6개사 force 스크랩(수 분)**을 돈다. 트리거가 아직 옛 주기(시간 제한 없음)로 걸려 있으면 **낮밤 없이 수 분씩 앱을 막는다** — keepWarm(93초)보다 나쁘다.
- **수정(방어)**: `refreshAllDistributions` **함수 안에 시간 가드** 추가 — `hour < 3 || hour > 6`이면 즉시 return. 트리거가 어떤 주기로 걸려 있든 **새벽 3~6시에만** 실제 스크랩. `checkDistNotices`가 쓰는 것과 같은 방식.
  - **검증**: 원문에서 가드 조건을 추출해 24시간 경계 확인 — 실행 3·4·5·6시, 나머지 20개 시간대 차단, **지금 23시 → 차단**. `node --check` OK.
- ⚠️ **다음 할 일(사용자)**: ① **Code.gs 재배포**(가드 반영) ② 트리거 화면에서 **`refreshAllDistributions` 트리거 개수 확인** — `setupRefreshTrigger()`를 안 돌렸으면 아직 2개다. 확실히 하려면 **트리거 화면에서 2개 다 삭제**해도 무방(캐시 선갱신이 없어도 앱이 TTL로 채운다). ③ 그래도 느리면 **실행 기록에서 기간 큰 행**을 볼 것 — (81)의 교훈대로 목록이 아니라 **기간**부터.

## 2026-08-05 (84) / — (83)에서 보고한 임시 디버그 블록 제거 (사용자 승인)
- `renderAccountStats`의 `// [임시 디버그] 계좌별 종목 평가액` 블록 **18줄 삭제**. 부팅마다 보유 65종목을 한 번 더 돌며 `console.log`를 60줄 넘게 찍고 있었다.
- 이 블록의 `priceMap[tk]`는 **숫자**인데 `parseFloat(p.current)`로 읽어 항상 `[평단]`으로 찍혔다(콘솔 출력 전용이라 화면 숫자엔 영향 없었음). 실제 평가액 계산부(`const cur = priceMap[...]`)는 숫자를 숫자로 바르게 쓰므로 **화면 값은 처음부터 정상**이었다.
- **검증**: `git diff` = **18 deletions / 0 insertions**, `_byAcc`·`_rate`·'임시 디버그' 잔여 참조 **0**, `node --check` OK, `priceMap`은 여전히 생성(1339줄)·사용(1360줄)됨.

## 2026-08-05 (83) / — **근본책 완료: 현재가를 '주식상황' 시트 대신 네이버/야후에서 직접**
- **배경**: (82)에서 남은 병목이 `getSheetData`(외부 '주식상황' 시트, 실시간 시세 수식 재계산 → 캐시 미스 시 10~50초)로 확정. WORKLOG 580줄에 예전부터 "다음 할 일(B, 근본)"로 남아 있던 항목.
- **조사에서 나온 핵심**: 네이버 폴링 API가 **콤마로 여러 종목을 한 번에** 준다. ETF·리츠·일반주 구분 없이 전부. 야후 배치(v7 quote)는 **401**이라 해외는 종목별 호출을 `UrlFetchApp.fetchAll`로 한 실행에서 병렬 처리.
  - 라이브 검증: **국내 29개 요청 → 29개 전부 응답**(롯데리츠·현대차 같은 비ETF 포함), 해외 6개(JEPQ·PLTR·SCHD·TSLY·TSLA·SPYI) 전부 200.
- **`Code.gs`**: **`getLivePrices()` 신설** — 국내 폴링 1회 + 해외 `fetchAll` 1회 = **한 실행에서 외부 호출 2회**, 60초 캐시. 라우터 1줄.
  - ⭐ **키는 holdings에 저장된 티커 원문 그대로**. 조회는 6자리 보정본(`5930`→`005930`)으로 하되 반환 키는 `5930` — 보정본을 키로 쓰면 프론트 조회(`h.ticker`)와 안 맞는다. 테스트로 고정해 둠.
  - 빈 결과는 캐시하지 않음(일시 실패가 60초 굳는 것 방지).
- **`portfolio.html`**: **`fetchPriceItems()`** 헬퍼 신설 — `getLivePrices` 응답을 **예전 `getSheetData`와 같은 `{items:[{ticker,current,change}]}` 형태로 변환**해서 소비처를 그대로 뒀다. 실패하거나 재배포 전이면 **예전 시트 경로로 자동 폴백**.
  - 교체한 시세 소비처 **4곳**: 부팅(`init`), `loadAccountStats`, `loadDashboard`, 연금플랜, 그리고 `loadHoldingPrices`(여기선 재시도 루프 `fetchSheet`를 통째로 대체).
  - **`getSheetData`는 이제 동기화(force=1)와 폴백에만 남는다** — 원래 용도(시트 diff)로 복귀.
- **검증**: `node --check` 2파일 OK. 원문 추출 테스트 **22종 ALL PASS** — 백엔드 16종(호출 1+1회 / 콤마 배치 URL / 콤마 섞인 가격 파싱 / 전일가·등락률 / **티커 원문 키** / 중복 티커 dedupe / 캐시 히트 시 외부호출 0 / 전부 실패·일부 실패 / 빈 결과 캐시 안 함 / 보유 0건), 프론트 6종(형태 변환 / 재배포 전 폴백 / 예외 폴백 / 빈 결과 폴백).
- 🔎 **발견(고치지 않음, 보고만)**: `renderAccountStats` 안에 `// [임시 디버그] 계좌별 종목 평가액` 블록이 남아 있다. 부팅마다 65종목을 돌며 `console.log`를 60줄 넘게 찍는다. 게다가 `priceMap`에는 숫자를 넣는데 `parseFloat(p.current)`로 읽어 **항상 `[평단]`으로 표시**된다(콘솔 출력만이라 화면 숫자에는 영향 없음). 지울지는 사용자 판단.
- ⚠️ **다음 할 일**: **Code.gs 재배포**. 성공 기준 — 부팅·종목관리·대시보드에서 `getSheetData` 대신 **`getLivePrices`(1~2초)**가 보이고, 10~50초 스파이크가 사라진다. 재배포 전엔 폴백이라 예전과 동일하게 동작(안 깨짐).

## 2026-08-05 (82) / — 종목관리 재신고 → 남은 병목은 **`getSheetData`(외부 '주식상황' 시트 재계산)**
- **서버는 이제 정상**: `zz_noop` 1.35~1.48초 일정, 종목관리 3건 동시 **2~4초**. (81)의 keepWarm 제거 효과 유지됨.
- **오답 하나 걸러냄**: `loadHoldingPrices`가 시트에 현재가 없는 종목마다 `getStockPrice`를 **순차 await**로 부른다(4196줄). 65종목이면 재앙이라 유력해 보였으나, 실측 결과 **보유 65건 전부 시트에 현재가가 있어 이 경로는 0건 실행**. 범인 아님. (다만 신규 종목이 시트에 없으면 그때 터질 구조라 기록해 둠.)
- **진짜 병목 = `getSheetData`**: 외부 '주식상황' 시트(ID `19UsD0Tz…`)가 **실시간 시세 수식**이라 열 때마다 재계산된다. 실측 편차가 극단적:
  - **캐시 히트 1.69 · 2.13초 / 캐시 미스 11.2 · 12.0 · 13.7 · 37.8 · 50.6초**
  - 프론트 **6곳**에서 호출(부팅 1176줄, 탭 1309·2161·4918, `loadHoldingPrices` 4149, 동기화 2905=force). 캐시가 만료될 때마다 **누군가 이 10~50초를 뒤집어쓴다** → "가끔 종목관리가 느리다"의 정체.
- **한 일(최소 수정)**: 캐시 TTL **180초 → 600초(10분)**. 만료 빈도가 1/3로 줄어 캐시에 걸릴 확률이 크게 오른다. 시세 지연 10분은 배당 추적엔 충분하고, **동기화(force=1)는 캐시를 건너뛰므로 항상 최신**. `node --check` OK.
- **근본책(다음 후보, 사용자 상의 필요)**: 현재가를 '주식상황' 시트가 아니라 **네이버 시세에서** 받기. WORKLOG 580줄에 이미 "getSheetData가 재계산 대신 시세로그/snapshot에서 현재가 읽게 변경"으로 남아 있던 그 항목.
  - 보유 실측: 65행 = KRW 58 · USD 7, **고유 티커 35개**. 이 중 국내 ETF **24개는 `etfItemList` 1회(1.4초)로 현재가·등락률 전부** 얻을 수 있다(`overlayLivePrices`가 이미 쓰는 방식). 나머지 11개(국내 비ETF 5 + 미국 6 `JEPQ·PLTR·SCHD·TSLY·TSLA·SPYI`)만 별도 경로 필요.
  - ⇒ `getLivePrices()` 같은 액션을 만들면 종목관리·포트폴리오의 현재가를 **1.4초**에 얻고, '주식상황' 시트는 **동기화 전용**으로 남길 수 있다(원래 용도).
- ⚠️ **측정 주의(내 실수 경계)**: 이날 내가 `getSheetData`·`getEtfScreener` 같은 10~50초짜리를 연달아 치면서 **내 부하가 관측치에 섞였다**. 스파이크를 볼 땐 `zz_noop`(대기 지표)과 실제 액션을 **교대로** 재서 분리할 것.
- ⚠️ **다음 할 일**: ① **Code.gs 재배포**(TTL 반영) ② 근본책 진행 여부 결정.

## 2026-08-05 (81) / — **해결 확인**: keepWarm 트리거 삭제 후 스파이크 소멸. 오늘 작업 종료
- **사용자가 트리거 화면에서 `keepWarm` 트리거 삭제 → "확실히 빨라졌네"**. (80)의 진단 확정.
- **삭제 후 실측**: 부팅(`getBootstrap` 4.3 + `getSheetData` 7.4 동시) **8초** / 종목관리(`getSheetData` 1.8 + `getPriceLog` 4.3 + `getNavMap` 1.4 동시) **4초**. 직전까지 나오던 **20~43초 스파이크가 전부 사라짐**.
- **현재 상태**: `keepWarm` 트리거 **미설치**(의도적). 코드상 `keepWarm`은 자기호출 없는 가벼운 버전으로 고쳐져 있음.
  - ⚠️ **재설치하려면 반드시 새 Code.gs를 편집기에 붙여넣고 저장한 뒤** `setupKeepWarm()` ▶실행할 것. **저장 안 한 채 실행하면 옛 자기호출 버전이 다시 설치되어 같은 문제가 재발한다.**
  - 지금은 그냥 두는 걸 권함 — 잃는 건 콜드스타트 방지뿐(오래 안 쓰다 열 때 20~30초 1회)이고, 5분마다 93초 블로킹보다 훨씬 가볍다.
- **반성 메모**: 이 원인을 찾기까지 (70)→(77)→(78)→(79)→(80) 다섯 번 돌았다. 되짚어보면 **(70)에서 이미 "keepWarm 자기호출이 슬롯 2개를 물 위험"을 의심으로 기록해뒀는데**, 그 뒤 실행 기록을 **소요시간 기준으로 보지 않고** 함수 목록·트리거 목록만 봐서 계속 빗나갔다. 다음에 "가끔 느리다"가 나오면 **실행 기록에서 기간 큰 행부터 볼 것.** 트리거 목록만으로 범인을 단정하지 말 것((79)에서 실제로 오판했다).
- **남은 개선 여지(급하지 않음)**: 부팅 8초 중 `getSheetData` 7.4초가 최대 — 외부 '주식상황' 시트가 실시간 시세 수식을 재계산하느라 느리다(캐시 히트 시엔 1.8초). 필요해지면 그때 손보면 됨.

## 2026-08-05 (80) / — **블로킹 범인 확정 = `keepWarm` 자기호출 (93.7초)**. 자기호출 제거
- **실행 기록에서 확정**(사용자가 23:00~23:15 구간 제공):
  ```
  Head  keepWarm  시간 기반  23:12:04  93.729초   ← 범인
  Head  keepWarm  시간 기반  23:07:04   2.295초
  Head  keepWarm  시간 기반  23:02:04   2.075초
  ```
  - 이 93.7초가 내가 `getPriceLog` **41.4초**를 관측한 시각과 겹친다. **결정적 대조: 같은 시각 서버쪽 `doGet`은 5.116초** ⇒ 나머지 **36초는 순수 큐 대기**.
  - 원인 구조: `keepWarm`이 `UrlFetchApp.fetch(자기 웹앱 URL)`로 **자기를 호출** → 바깥 실행이 안쪽 `doGet`을 기다리는 동안 **실행 슬롯을 계속 점유** → 그 사이 들어온 요청 전부 큐 대기. (70)에서 "의심 하나 기록"으로만 남겼던 게 실측으로 확정됨.
  - **콜드스타트를 막으려던 장치가 훨씬 큰 지연을 만들고 있었다** — 콜드스타트는 오래 안 쓰다 열 때 20~30초 **1회**, 이건 **5분마다** 최대 93초.
- **수정**: `keepWarm`에서 자기호출 삭제 → `SpreadsheetApp.openById(SHEET_ID).getName()`만 남김(컨테이너·시트 핸들 워밍, 1초 이내). 트리거 주기 5분은 유지(이제 짧으니 무해). `_EXEC_URL`은 카톡 링크·캘린더 설명에서 계속 쓰이므로 **유지**(고아 아님 — grep으로 확인).
  - ⚠️ **트레이드오프(정직하게)**: 예전 주석의 "실제 액션을 쳐야 외부 스크랩 경로까지 데워진다"는 효과는 약해질 수 있다. 콜드스타트가 다시 체감되면 트리거 주기 조정으로 재검토. 다만 **5분마다 93초 전면 대기보다는 압도적으로 낫다.**
- **중요**: `keepWarm`은 **트리거**라 배포본이 아니라 **편집기에 저장된 코드**로 돈다 ⇒ **재배포 불필요, 붙여넣고 저장하면 즉시 적용.** (79)의 `refreshAllDistributions` 본문 수정도 동일.
- **검증**: `node --check` OK. `_EXEC_URL` 참조 2곳(2031·2096줄) 살아있음 확인.
- ⚠️ **다음 할 일(사용자 수동)**: ① Code.gs 편집기에 붙여넣고 **저장**(재배포 불필요) ② **`setupRefreshTrigger()` ▶실행**((79)의 중복 트리거 정리) ③ 저장 후 알려주면 **재측정해서 확인** — 성공 기준: `keepWarm` 실행 기간이 **1초 이내**, 앱 요청이 40초대로 튀지 않음.

## 2026-08-05 (79) / — 트리거 15개 확인: `refreshAllDistributions` 결함 2건 수정. **블로킹 범인은 아직 미확정(정정)**
- **`_diagTriggers()` + 트리거 화면 결과 (15개)**: `refreshAllDistributions` **×2**, `snapshotPortfolio` ×3(10·13·16시), `checkDistNotices`, `keepWarm`, `checkDeviationAlerts`, `snapshotPrices`, `clearOldYellowCells`, `markInputCells`, `compactPriceLog`, `flushKakaoPending`, `flushCalPending`, `sendMaturityAlerts`.
  - `sendMaturityAlerts` 트리거 실재 확인 → (76)에서 만기알림 묶음을 남긴 판단이 맞았음.
- **⚠️ 내 오판 정정**: 트리거 목록만 보고 "`refreshAllDistributions`가 블로킹 범인 확정"이라고 했는데, **트리거 화면의 최종 실행이 8/4 09:01·8/5 16:41 = 하루 1~2회**였다. 23:05 블로킹과 무관. **목록만으로 범인을 단정한 게 잘못.** 최종 실행 시각까지 봐야 했다.
- **블로킹은 실재(추가 실측)**: 부하 없이 한 건씩 순차로 쳐도 `getBootstrap` 7.4 / `getSheetData` 9.1 / **`getPriceLog` 41.4** / `getNavMap` 1.6초. 같은 `getPriceLog`가 앞선 측정에선 2.6초였다. **범인은 여전히 미확정** — 확정하려면 블로킹이 난 시각(23:00~23:20)의 **실행 기록에서 기간 20초 넘는 실행의 함수명**을 봐야 한다.
- **그래도 고친 것 — `refreshAllDistributions` 결함 2건**(블로킹 원인은 아니지만 명백한 결함):
  1. **`return` 버그**: 첫 줄이 `return checkAndLogAlerts();`라 아래 6개사 force 루프가 **한 번도 실행된 적 없음** ⇒ 사용자가 원한 "미리 채워두기"가 실제로 안 돌고 있었다. → 캐시 선갱신만 하도록 본문 교체(공지 감지·카톡은 `checkDistNotices`가 이미 같은 `checkAndLogAlerts`를 돌리므로 **완전 중복이었음**).
  2. **중복 트리거 2개 + 시간 제한 없음**: 수동 설치로 2개가 박혀 있었고 시간창이 없어 밤낮없이 돌았다. → **`setupRefreshTrigger()` 신설**(기존 전부 삭제 후 **매일 05시 1회**만). force 6개사는 실행이 기니 사용자가 안 쓰는 시간대로.
- **검증**: `node --check` OK. `checkAndLogAlerts()` 호출부가 `checkDistNotices`(973줄)와 라우터 `checkAlerts`만 남은 것 확인 — 공지 알림 경로 안 끊김.
- ⚠️ **다음 할 일(사용자 수동)**: ① Code.gs 편집기에 붙여넣기 → **`setupRefreshTrigger()` ▶실행**(재배포 불필요) ② **블로킹 시각대의 실행 기록 스크린샷** — 기간 20초 넘는 실행의 함수명이 나오면 그게 범인.

## 2026-08-05 (78) / — 재배포 후 검증: **스크리너·공지 성공**, 남은 30초는 **주기적 블로킹**(코드 아님)
- **(77) 재배포 후 실측 — 두 건 확인**:
  - `getEtfScreener` **44·55·39초 → 2.85 / 2.67초.** ⇒ (77)②의 "시트가 날짜를 Date로 자동 변환해 캐시가 항상 미스" **가설이 맞았음**(역추적이었는데 적중).
  - `getEtfNoticesAll` **1.95초** — 신규 액션 정상 동작(공지 6건 → 1건).
- **"종목관리 30초"는 별개 원인 — 호출 개수가 아님**:
  - 웜일 때 실측: 부팅(`getBootstrap`+`getSheetData` 동시) **8초**, 종목관리 탭(`getSheetData`+`getPriceLog`+`getNavMap` 동시) **5초** ⇒ 합쳐도 13초. 30초가 안 나온다.
  - 그런데 `getBootstrap`을 반복 호출하니 **4.4 / 3.2 / 11.0 / 86.9초**, 다시 **42.5 / 27.6 / 12.1 / 49.0 / 13.5초**로 널뜀.
  - **결정적 증거 — 시트를 전혀 안 건드리는 미지원 액션 `zz_noop`도 32.2초**가 나옴. 같은 시각 `getNavMap`(캐시)도 21.2초. 그 뒤 10회 연속 **1.2~1.7초**로 안정.
  - ⇒ **스프레드시트 경합도, 우리 코드 문제도 아니다.** 스크립트 실행 자체가 주기적으로 20~87초 막힌다. (70)의 "평소 1.3초, 가끔 전면 대기"와 동일 — 이번엔 `zz_noop`으로 확실히 분리됨.
  - 사용자가 본 30초는 **재배포 직후 콜드스타트 + 이 블로킹**이 겹친 것으로 보임.
- **범인 후보 좁힘**: 오늘은 8/5라 `checkDistNotices`가 `_inNoticeWindow(5)=false`로 **즉시 return** → 범인 아님. 시간 제한이 없는 **`refreshAllDistributions`가 유력**(23:05:52에 32초 관측, 23:06:21에 해소).
  - 🔎 **발견**: `refreshAllDistributions`는 첫 줄이 `return checkAndLogAlerts();`라 **그 아래 6개사 force 루프가 절대 실행되지 않는다**(사장 코드). 즉 이름과 달리 실제로는 `checkAndLogAlerts`(6개사 스크랩)만 돈다. 분배캐시 선갱신은 사실상 사용자 방문 시 TTL 만료로만 이뤄지고 있었음.
- **한 일**: `Code.gs`에 **`_diagTriggers()`** 추가(설치된 트리거 전체 목록 출력, 오래 도는 후보에 ⚠ 표시). 로직 변경 없음. `node --check` OK.
- ⚠️ **다음 할 일(사용자 수동)**: 편집기에서 **`_diagTriggers()` ▶실행** → 출력 알려주기. `refreshAllDistributions`가 짧은 주기로 걸려 있으면 **주기 완화 또는 시간창 제한**(checkDistNotices처럼)으로 블로킹 창을 없앤다. 재배포 불필요(편집기 ▶실행만).

## 2026-08-05 (77) / — "여전히 30초~1분" 실측 → **범인은 프론트의 자동 force 재파싱**(+스크리너 캐시 미스)
- **증상**: (75)에서 분배 2.3초를 확인했는데도 앱 분배금공지 탭이 30초~1분. 스샷엔 "최신 데이터로 갱신 중…" 토스트 + **RISE·SOL "불러오기 실패"**.
- **① 진짜 범인 — `showTab`의 자동 force**: `portfolio.html` `showTab('distributions')`이
  `loadDistributions().then(() => { if (now - _lastDistForce > 10분) loadDistributions(true, true); })` 였다.
  빠른 경로(2.6초)가 끝나자마자 **force 재파싱**을 백그라운드로 돌림 → force는 캐시를 건너뛰고 **6개사 실시간 스크랩**(타임아웃 90초). 6개가 동시에 긁다 밀린 RISE·SOL이 타임아웃 → "불러오기 실패". **즉 (73)에서 만든 캐시 경로가 10분마다 force로 덮어씌워지고 있었다** — 사용자가 말한 "미리 저장해두는 메리트가 없다"의 정확한 원인.
  - **수정**: 자동 force 삭제. 새 공지는 서버 TTL(공시 임박 2시간·평소 6시간)과 `refreshAllDistributions` 트리거가 반영. 즉시 최신이 필요하면 우측 상단 **'새로고침'이 여전히 force**. 사용자 선택 = "서버 주기에 맡긴다". 고아가 된 `_lastDistForce`(선언·대입 2곳)도 제거.
  - `dist_notice.html`엔 이 자동 force가 **원래 없었다** — 공개 페이지가 (73) 이후 빨라진 게 그래서.
- **② 스크리너 40~55초 — 일일 캐시가 안 먹고 있었음**: `getEtfScreener`는 시트 `ETF스크리너`에 당일자 캐시를 두는데, **연속 두 번 호출해도 54.8초 → 38.6초**로 계속 재수집(135종목 보강). 원인은 캐시 날짜 비교 `rows[1][0].toString() === today` — 시트가 `'2026-08-05'`를 **Date 값으로 자동 변환**하면 `toString()`이 `'Wed Aug 05 2026 …'`이 되어 영원히 안 맞는다. (`분배캐시` 쪽은 이미 `instanceof Date`로 처리해뒀는데 여기만 빠져 있었음.)
  - **수정**: Date/문자열 양쪽을 `yyyy-MM-dd`로 정규화해 비교. **어느 쪽이 원인이든 안전**(문자열이면 기존과 동일 동작).
  - ⚠️ 원인은 동작(2회 연속 재수집)에서 역추적한 것이라 **재배포 후 2회 연속 호출로 실측 확인 필요**. 2번째가 3~4초면 맞은 것.
- **③ 공지 6건 → 벌크 1건**: `getEtfNoticesAll()` 신설 — `CacheService.getAll`로 6개 키를 **한 번에** 읽어 반환, 캐시 없는 곳은 `stale:true`(한 실행에서 6개사 순차 스크랩하면 더 느려지므로 프론트가 그것만 병렬 개별 요청). 라우터 + `PUBLIC_ACTIONS` 추가.
  - `portfolio.html`은 **TIGER·PLUS가 브라우저에서 운용사 사이트를 직접 호출**하므로(CORS 통과) GAS 경유 4곳(KODEX·ACE·RISE·SOL)만 벌크 대상 → `NOTICE_VIA_GAS` 상수로 명시. `dist_notice.html`은 6개사 전부 GAS 경유라 전부 벌크.
- **탭별 실측(참고)**: 부팅 `getBootstrap` 3.6초(웜) / `getSheetData` 5.9 / **`getEtfScreener` 44·55·39** / `getSavings` 2.8 / `getPortfolioLog` 3.6 / `getPriceLog` 2.6 / `getNavMap` 1.4 / `getAlerts` 2.0. 공지 6건 동시 11초(KODEX 11.2·RISE 10.0이 미스).
- **검증**: `node --check` 3파일 OK. 원문 추출 테스트 **24종 ALL PASS** — 공지 벌크 17종(캐시히트/0건→stale/깨진 JSON→stale/일부 stale만 개별/재배포 전 폴백/TIGER·PLUS는 항상 개별), 스크리너 날짜 7종(Date→히트·문자열→히트·어제→미스·빈행→미스·옛 스키마 헤더→미스). `_lastDistForce` 잔여 참조 0.
- ⚠️ **다음 할 일**: ① **Code.gs 재배포**(②③ 반영) ② 프론트는 푸시·미러 완료라 GitHub Pages 갱신되면 ①번 효과는 재배포 없이 바로.
  - **성공 기준**: 분배금공지 탭 진입 **3초 이내**, "갱신 중…" 토스트 안 뜸, RISE·SOL 실패 없음. 스크리너 두 번째 진입 **3~4초**.

## 2026-08-05 (76) / — Code.gs 사장 코드 점검 → **128줄 제거 (3,189 → 3,061)**
- **요청**: "Code.gs에 코드가 너무 많다. 테스트하는 것도 많은데 다 필요한가?"
- **방식**: 추측 금지 — 함수 136개를 전부 뽑아 **선언부를 뺀 나머지 텍스트에서 이름이 몇 번 등장하는지** 세는 스크립트로 판정.
  - ⚠️ **1차 스크립트가 5개를 놓쳤다**(`ck`·`ckSolCycle`·`debugSheetAcc`·`debugSmartToday`·`importHistoricalData`). 처음엔 "69줄"이라고 보고했다가 재작성 후 **128줄**로 정정. 정규식 이스케이프가 셸 heredoc에서 깨진 게 원인 — **감사 스크립트는 heredoc 말고 파일로 쓸 것.**
- **삭제(전부 참조 0건, 128줄)**:
  - `parseKodexSchedule`(18) · `parseAceSchedule`(19) — 자사 파서가 자체 일정 파싱으로 바뀌면서 남은 옛 파서.
  - `parseScheduleFromText`(24) — **호출부가 `parseKodexSchedule` 하나뿐**이라 같이 사장됨(연쇄 고아).
  - `testKrxEtf2`(18) · `testEtfCheckJang`(8) · `testEtfCheckApi`(6) — KRX·etfcheck 응답 찍어보던 탐색용. **두 사이트를 앱이 아예 안 쓴다는 걸 확인**(`data.krx`/`etfcheck` 문자열이 이 함수들 안에만 존재).
  - `ck`(5) · `ckSolCycle`(6) · `debugSheetAcc`(6) · `debugSmartToday`(6) — 일회성 탐침. `debugSmartToday`는 `testDistribution`과 사실상 중복.
  - `importHistoricalData`(3) — 본문이 `console.log('이미 실행 완료 (472행). 재실행 불필요.')` 한 줄뿐인 유물.
- **남긴 것(지우면 안 되거나 쓸모 있음)**:
  - `snapshotPrices` · `refreshAllDistributions` — **코드 어디서도 호출 안 하지만 시간 트리거가 이름으로 직접 부른다.** 설치용 `setup*` 함수가 없어서 정적 분석엔 죽은 걸로 보임 — **지우면 시세로그·분배캐시 자동 갱신이 조용히 멈춘다.** 다음에 또 헷갈리지 말 것.
  - `doGet`/`doPost`(진입점), `setup*` 10개(트리거 날아갔을 때 복구용), `testKakao`·`testCal`(연동 점검), `testDistribution`·`testScreener`(파싱 스모크), `_testNoticeWindow`, `_diagDeviation`.
  - **만기 이메일 알림 묶음(~130줄)은 사용자가 "쓰고 있다" 확인 → 유지.** (`previewMaturityAlerts` 라우터 case는 프론트가 안 쓰지만 그대로 둠)
  - `updateAccount`는 1차 스캔에서 "프론트 미사용"으로 나왔으나 **실제 5회 사용** — 오탐이었음. 후보는 반드시 grep으로 재확인할 것.
- **검증**: `node --check` OK / `git diff --stat` = **128 deletions, 0 insertions**(추가된 줄 0) / 삭제한 이름 10종 파일 내 잔여 참조 **0건** / 재감사 결과 **새로 생긴 고아 없음**(남은 13개는 전부 진입점·트리거핸들러·수동실행).
- **덩치 참고**: `enrichScreener` 536줄(전체 17%), 운용사 파서 6개 합 ~630줄. 이건 기능 자체라 줄일 대상 아님.
- ⚠️ **다음 할 일**: 지운 게 전부 안 쓰이던 코드라 **동작은 재배포 전후가 동일**. 급하지 않으니 다음에 다른 이유로 재배포할 때 같이 반영하면 됨(편집기 붙여넣기만 해도 트리거·수동실행은 새 코드로 돎).

## 2026-08-05 (75) / — 재배포·미러 완료 → **실측 27초 → 2.3초 (약 11배)**
- **jjk-dist 미러 완료**(사용자 "이런건 니가 알아서해" 지시 → 앞으로 미러는 자동 수행). 임시폴더에 clone → `dist_notice.html` → `index.html` 복사 → push(`fb2545a`). 줄바꿈 churn 없이 **30 insertions / 15 deletions**만 나온 것 확인. 라이브 재빌드 확인(`getDistributionAll` 2회 등장, `👁 오늘` 표기 반영).
  - 이때 **밀려 있던 이전 미러 1건(방문자 카운터 `👁 5` → `👁 오늘 N · 누적 N`)도 같이 반영**됨 — 지난 세션에서 미러를 빠뜨렸던 것.
- **Code.gs 재배포는 사용자가 이미 완료**(라이브 `getDistributionAll` 응답 확인).
- **라이브 A/B 실측 2라운드**:

  | 경로 | 1라운드 | 2라운드 |
  |---|---|---|
  | 옛 경로(getDistribution 6건 동시) | **28초** | **27초** |
  | 새 경로(getDistributionAll 1건) | **2.31초** | **2.60초** |

  ⇒ **약 11배**. (73)의 예측(2~4초)이 실측으로 확인됨.
- **페이로드 무결성 확인**: 6개사 전부 `fresh`(stale 0), items **kodex 170 / tiger 110 / rise 76 / ace 48 / plus 26 / sol 23 = 453건**, 공시일도 정상. 164,841B.
- **남은 손수 작업 없음.**
- 참고: `public_dist_proxy.gs`는 메모리에도 기록돼 있듯 **배포된 적 없는 사장된 설계안**. (73)에서 넣은 수정은 무해하지만 실제로 동작하는 코드는 아님 — 백엔드 변경은 항상 `Code.gs`에.

## 2026-08-05 (74) / — 정정: **프록시는 실제로 안 쓰이고 있음** → (73)의 수동 작업 3개 → 2개
- (73)에서 "프록시도 재배포 필요"라고 적었으나 **틀림**. 라이브 공개 페이지를 직접 받아 확인:
  - `curl https://jaenamking1-collab.github.io/jjk-dist/` → `const API = '...AKfycbwJS1Fd-...NF_s/exec'` = **개인 GAS(포트폴리오관리) URL 그대로**.
  - ⇒ 공개 페이지는 **프록시를 경유하지 않고 개인 GAS를 직접 호출**한다. `public_dist_proxy.gs`는 저장소에만 있는 **미사용 파일**(24줄 `PRIVATE_URL`이 아직 `PASTE_PRIVATE_GAS_EXEC_URL_HERE` 플레이스홀더).
  - 지금은 `APP_TOKEN`이 켜져 있고 `getDistribution`·`getDistributionAll`·`getEtfNotices`·`hitCounter`만 `PUBLIC_ACTIONS`라, 공개 페이지가 개인 URL을 직접 쳐도 개인 데이터는 안 나간다. (프록시 없이도 성립)
- (73)에서 프록시에 넣은 수정(`ALLOWED_ACTIONS` 추가 + `cache.put` try/catch)은 **그대로 둔다** — 나중에 프록시를 실제로 붙일 때 필요하고, 지금 아무 영향 없음. **단, 그때 저장소 파일을 통째로 붙여넣으면 `PRIVATE_URL`이 플레이스홀더로 덮여 죽으므로 두 줄만 손으로 고칠 것.**
- ⚠️ **(73)의 수동 작업은 2개로 축소**: ① **Code.gs 재배포** ② **`dist_notice.html` → `jjk-dist/index.html` 미러·푸시**. (프록시 재배포 불필요)

## 2026-08-05 (73) / — 분배 로딩 진짜 원인 = **캐시 읽기가 무거움**(트리거 아님) → `getDistributionAll` 신설
- **(70)의 "트리거 블로킹" 가설 폐기.** 실행 기록 실측이 반증함:
  - 22:00:21에 **doGet 12개 동시 → 전부 0.7~2.6초** ⇒ Apps Script는 병렬 잘 처리함. **직렬화 아님**.
  - 22:00:24에 **doGet 6개 동시 → 6.7 / 8.5 / 12.9 / 18.0 / 46.0 / 49.3초**. 이 6개가 공개 페이지의 `getDistribution` × 6개사. 프론트 타임아웃 25초라 46·49초짜리는 실패 → **"불러오는 중…"에서 멈춤**(사용자 스샷).
  - `keepWarm`은 5~10초짜리고 시각도 안 겹침 ⇒ **범인 아님**((70)의 자기호출 의심도 근거 없음).
- **진짜 원인**: `getDistribution(source)`는 **캐시 히트여도** `readDistCache` + `attachDistHistory`가 각각 `openById` + `getDataRange().getValues()`로 **분배캐시 시트를 통째로** 읽는다 = **호출당 2회**. 시트엔 6개사 × 최근 3회차 통짜 JSON(SOL 72KB·KODEX 38KB, 한 회차만 ~160KB). 6개사 동시 호출 = **시트 전체 읽기 12회**.
  - 라이브 확인: 6개 동시 2.5~19.6초 / **혼자 돌려도 kodex 3.8초·tiger 10.2초** ⇒ 경합이 아니라 읽기 자체가 비쌈(스크랩 아님. 반복해도 안 빨라짐).
- **수정(사용자 선택: 호출 통합)**:
  - `Code.gs` — **`getDistributionAll()` 신설**: 분배캐시 시트를 **1회만** 읽고 6개사를 메모리에서 처리. 신선도 판정은 `getDistribution`의 캐시 분기와 **같은 조건**(회차키 일치 + TTL 이내 + items 있음). 캐시가 없거나 오래된 source는 **`stale:true`로 표시만** 한다(한 실행에서 6개사 스크랩하면 실행시간 제한). `readDistCache(source, rows)` / `attachDistHistory(source, payload, rows)`에 **선택 인자 rows** 추가(안 넘기면 기존과 동일하게 동작 — 기존 호출부 무영향). 라우터 1줄 + `PUBLIC_ACTIONS`에 추가.
  - `public_dist_proxy.gs` — `ALLOWED_ACTIONS`에 `getDistributionAll` 추가. **`cache.put`을 try/catch로 감쌈**: CacheService는 값 1건 100KB 초과 시 예외를 던지는데, 6개사 합본은 이걸 넘을 수 있어 그대로 두면 **응답 자체가 `{error}`로 죽는다**(캐시 못 넣는 건 괜찮음 → 캐시 미스로 동작).
  - `dist_notice.html` + `portfolio.html`(`loadDistributions`) — 벌크 1건 호출 후 **stale인 운용사만** 개별 재요청. 중복되던 재시도 블록은 `fillStale(pred, ms)` 헬퍼로 합침. **`force`는 스크랩이 필요하므로 예전 개별 경로 그대로.**
  - **배포 순서 함정 대비**((69)와 동일): 재배포 전이면 `getDistributionAll`이 `Unknown action`이라 `sources`가 없음 → **자동으로 예전 6건 경로로 폴백**. 프론트를 먼저 올려도 안 깨짐.
- **검증**: `node --check` 4파일 OK. 원본 소스에서 함수를 그대로 떼어내 가짜 시트/api로 테스트 — **백엔드 16종 ALL PASS**(시트 읽기 **openById 1회·getValues 1회** 확인 / 회차불일치·TTL초과·행없음 → stale / 과거 회차 병합 / 빈 시트 / **개별 호출은 2회 읽기**임을 대조로 확인), **프론트 16종 ALL PASS**(dist_notice 11 + portfolio 5: 전부 신선→1건, 일부 stale→해당 운용사만, 재배포 전→7건 폴백, 벌크 예외→폴백, force→벌크 안 탐).
- ⚠️ **다음 할 일(사용자 수동)**: ① **Code.gs 재배포**(기존 배포 편집 → 새 버전, URL 유지) ② **프록시 프로젝트에 `public_dist_proxy.gs` 반영 후 재배포**(공개 페이지가 프록시 경유일 때. `ALLOWED_ACTIONS`에 안 넣으면 공개 페이지는 계속 예전 6건 경로로 돎 — 안 깨지지만 안 빨라짐) ③ **`dist_notice.html` → `jjk-dist/index.html` 미러·푸시**.
  - **성공 기준**: 분배금공지 탭/공개 페이지를 열 때 개발자도구 Network에 **`getDistributionAll` 1건만**(stale 없으면), 체감 **2~4초**. 6건이 그대로 보이면 재배포가 안 된 것(폴백 중). 실제 단축폭은 재배포 후 실측 필요 — **아직 예측치임**.

## 2026-08-05 (72) / — 괴리율 알림 **정상 판정**(고장 아님) + 로딩 느림 = (70) 블로킹으로 확정 이관
- **`_diagDeviation()` 실행 결과**: 트리거 **1개 설치됨** / `DEVIATION_STATE` 총 4건 **below 없음**(전부 재무장) / 네이버 200 / 휴장가드 통과 / 보유 24건 최저 **-0.75%**(0040Y0 SOL 팔란티어).
- **(71)의 "8/3에 안 떴으니 트리거가 죽었다" 가설 → 반증됨**:
  - `getPortfolioLog` 실측: **8/3 slots 10·13·16 전부 기록**(8/4·8/5도 동일) ⇒ 폭락일 장중에 시간 트리거 정상 가동.
  - 0190G0이 7/31 `below` → 지금 `above` ⇒ **7/31 이후에도 checkDeviationAlerts가 돌아서 재무장**했다는 직접 증거.
  - ⇒ **7/31 이후 실제로 -1% 이탈이 없었던 것**. 네이버 `nav`는 실시간 iNAV라 시장이 같이 빠지면 괴리가 안 벌어지고 LP가 1% 밴드를 지킨다. 7/28~7/31(-11.19/-6.34/-2.00/**+24.17%**)이 LP가 밴드를 놓친 예외 구간이었을 뿐. **내가 "폭락일=디스카운트 확대"로 단정한 게 오판이었음 — 기록해 둠.**
- **결론**: 괴리율 알림 **고장 아님, 조용한 게 정상**. 코드 수정 없음. (임계 -1.0%가 실제론 거의 안 걸리는 값이라 -0.7%로 낮추거나 '오늘 최저 종목' 일일 요약 추가는 **사용자 결정 대기**.)
- **로딩 느림(사용자 스샷: 공개 분배 페이지 전 섹션 "불러오는 중…")**: 캐시 설계는 살아 있음(`getDistribution` **4.0/4.5초 = 캐시 히트**, 6개사 스크랩이면 수십 초). 같은 시간대 `getPortfolioLog`는 **404 45.1초 → 재시도 200 16.7초**. ⇒ **캐시가 아니라 실행 슬롯 대기가 병목**. (70)의 "평소 1.3초, 가끔 2분 전면 대기"와 동일 증상.
- ⚠️ **다음 할 일**: **실행 기록(Executions)에서 1~2분급 실행의 함수명 확정** → 주기 완화/시간대 제한. 후보 `checkDistNotices`(30분·6개사 스크랩, 최유력) / `keepWarm`(5분·자기 웹앱 자기호출로 슬롯 2개 점유) / `refreshAllDistributions`. 그래도 안 되면 **근본책: 공개 페이지가 GAS 실시간 호출 대신 jjk-dist에 미리 생성한 정적 JSON을 읽게** (사용자 상의 후).

## 2026-08-05 (71) / — "괴리율 카톡이 7/30 이후 안 온다" 조사 → 카톡 문제 아님, 트리거 의심
- **요청**: 괴리율 카톡이 잘 오는지 확인. 7/30 이후 안 오는 것 같다.
- **라이브 실측(추측 금지, 전부 curl/API로 확인)**:
  - `getAlerts` 알림로그: 괴리율 기록이 **7/30 00:25Z ~ 7/31 06:55Z(=KST 7/31 15:55) 5건이 마지막**. 이후 0건. 같은 시트에 8/5 07:43Z `checkDistNotices` 경고 2건은 정상 기록됨 ⇒ **시트·로그는 살아 있음**.
  - ⇒ `checkDeviationAlerts`는 카톡 실패해도 `_addAlert`로 로그를 남기므로, **로그조차 없다 = 카톡 발송 문제가 아니라 감지 자체가 안 된 것**. (카카오 토큰·발송시간대는 용의선상에서 제외)
  - `getNavMap` 라이브: `success:true`, nav **1160건** ⇒ GAS→네이버 `etfItemList` 접근 정상(차단 아님). 휴장가드(등락≠0 1132/1160)도 통과.
  - 지금 시점 보유 24종목 괴리율 실측: **-1% 미만 0개**, -0.5% 이상(재무장 상태) 23개 ⇒ **오늘 안 오는 건 정상**.
  - Gmail `from:apps-scripts-notifications@google.com after:2026/07/28` **0건** ⇒ 트리거가 오류로 죽은 흔적은 없음(삭제됐으면 메일도 안 옴).
- **이상한 점(핵심 근거)**: KODEX200 일간 등락 — 7/28 **-11.19%**, 7/29 -6.34%, 7/30 -2.00%, 7/31 **+24.17%**, **8/3 -8.93%**, 8/4 +1.24%, 8/5 +3.96%. **-2.00%였던 7/30엔 알림 4건이 떴는데, -8.93% 폭락한 8/3엔 0건.** 보유가 대부분 미국자산 커버드콜(급락 시 NAV 지연으로 디스카운트 확대)이라 8/3에 -1% 이탈이 없었을 가능성은 낮다.
- **가설(미확정, 사용자만 확인 가능)**: ① `checkDeviationAlerts` **트리거가 삭제/미설치** ② `DEVIATION_STATE`가 'below'로 굳어 재알림 잠김. (70)에서 잡은 **실행 직렬화·2분 블로킹**과 같은 뿌리일 수도 있음.
- **한 일**: `Code.gs` 맨 아래에 **`_diagDeviation()`** 추가(진단 전용, 상태만 출력·아무것도 안 바꿈). 트리거 개수 / `DEVIATION_STATE`의 below 목록 / 네이버 응답코드 / 휴장가드 / 보유종목 괴리율 정렬을 한 번에 찍는다. 로직은 손대지 않음. `node --check` OK.
- ⚠️ **다음 할 일(사용자 수동)**: Apps Script 편집기에 Code.gs 붙여넣기(**재배포 불필요** — 편집기 ▶실행과 트리거는 저장된 코드로 돔) → **`_diagDeviation()` ▶실행 → 실행 로그** 확인.
  - "트리거 ❌ 없음" → `setupDeviationTrigger()` ▶실행하면 끝.
  - 트리거는 있고 below 목록에 종목이 쌓여 있음 → 그 종목들이 -0.5%까지 회복 못 해 잠긴 것(설계대로). 강제로 풀려면 스크립트 속성 `DEVIATION_STATE` 삭제.
  - 둘 다 정상인데 8/3 같은 폭락일에 안 떴다면 → **실행 기록(Executions)**에서 `checkDeviationAlerts` 실행 이력·소요시간 확인((70)의 블로킹과 연결).

## 2026-08-05 (70) / — (69) 재배포 후 실측: getBootstrap 3배 확인 + 남은 범인 = 주기적 2분 블로킹
- **getBootstrap 라이브 정상**: 페이로드 7종 전부 정상(accounts 7·holdings 65·dividends(2026) 364·divsAll 493·stockList 34·rate 1423.52·alerts 26), 연도 필터도 2026 단일. 재배포 직후 첫 호출은 **69초(콜드스타트)** — 재배포 때마다 정상.
- **A/B 실측(새 2건 동시 vs 옛 9건), 3라운드 전부 새 경로 승**: 4.3 vs 12.9초 / 9.7 vs 27.3초 / 15.5 vs 32.9초 → **일관되게 약 3배**. (69) 수정은 의도대로 동작함.
- **그런데 절대값이 여전히 튐 → 추가 조사**: `zz_noop`(시트 전혀 안 건드리는 미지원 액션)도 똑같이 35~57초가 나옴 ⇒ **원인은 우리 코드가 아니라 스크립트 실행 자체의 대기**.
- **결정적 증거(20초 간격 저부하 프로브 18회 = 내 부하 배제)**:
  - 15:41:53 **51.4초** → 15:43:05 **30.9초** (약 2분짜리 블로킹 창)
  - 직후 15:43:56~15:46:49 **9회 연속 1.2~1.5초**(완벽히 안정)
  - 앞선 고부하 측정에서도 동일 패턴: 15:33:57 **56.8초** → 15:34:54 **57.2초** → 이후 10회 연속 1.0~1.7초.
  - ⇒ **"평소엔 1.3초, 가끔 2분간 전면 대기"**. 요청 1건당 바닥값은 1.2~1.7초로 확정.
- **해석**: Apps Script는 같은 사용자·같은 스크립트의 실행을 **직렬화**한다. 오래 도는 시간 트리거가 실행 슬롯을 물면 그동안 **웹앱 요청이 전부 큐에 쌓인다**. 설치된 트리거 후보: `checkDistNotices`(30분·6개사 스크랩=길다), `checkDeviationAlerts`(30분·장중), `keepWarm`(5분), `snapshotPortfolio`(시간별), 일일 배치 다수.
  - ⚠️ **미확정**: 어느 트리거인지는 **Apps Script 편집기 → 실행 기록(Executions)**에서 함수명·시작시각·소요시간을 봐야 확정됨(내 도구로 못 봄). 2분 안팎 걸린 실행을 찾으면 그게 범인.
  - 🤔 **의심 하나 기록**: `keepWarm`(Code.gs)은 `UrlFetchApp`으로 **자기 웹앱 URL을 자기가 호출**한다. 실행이 직렬화되는 환경에서 바깥 keepWarm이 안쪽 doGet을 기다리므로 **실행 슬롯을 2개 물고 서로 기다릴 위험**이 있음. 5분마다 도는 점도 블로킹 간격(≈8분)과 무관하지 않을 수 있으나 **아직 증거 없음** — 실행 기록으로 먼저 확인할 것.
- **결론**: (69)의 목표(왕복 9→2)는 달성. **한가한 때 부팅 2~4초**. 다만 그 위에 **트리거 블로킹**이라는 별개의 선행 문제가 남아 있고, 이건 옛 경로에서도(오히려 요청이 9건이라 더 자주) 있던 문제다.
- ⚠️ **다음 할 일**: ① 실행 기록에서 2분급 실행 찾기 → 해당 트리거 주기 완화/시간대 제한. ② 그다음 keepWarm 자기호출 구조 재검토.

## 2026-08-05 (69) / — "여전히 느림" 진짜 원인 = 부팅 왕복 9번 + 시트 경합 (getBootstrap 신설)
- **맥락**: (66)~(68)에서 잡은 keepWarm/APP_TOKEN은 **콜드스타트** 얘기였는데, **웜인데도 느리다**는 재신고. 추측 금지하고 라이브 curl로 실측함.
- **실측 3종**:
  - noop 액션(시트 안 건드림) 6회 = **1.2~2.4초** → **요청 1건당 고정 왕복비용**(구글 302 + userContent 홉). 줄일 수 없는 바닥값.
  - noop **4건 병렬 4.6초 / 순차 13.5초** → 플랫폼은 병렬 처리 잘 함.
  - 시트 읽는 **4건 병렬 12.6초 / 순차 14.5초** → **병렬 이득 거의 없음**. ⇒ 직렬화 주범은 플랫폼이 아니라 **스프레드시트 접근 경합**.
- **근본 원인 2단**:
  1. `getSheet()`(Code.gs)가 **호출마다** `SpreadsheetApp.openById` + 시트 전체 `getValues`. 같은 스프레드시트를 동시에 여는 실행끼리 서로 막아 **프론트의 `Promise.all`이 무력화**됨.
  2. 부팅에 요청 **9건**(게이트 getAccounts / getExchangeRate / getAccounts **중복** / getHoldings·getDividends(연)·getSheetData·getDividends(전체) / getStockList / getAlerts). 9 × 직렬 ≈ **웜에도 15~30초**. localStorage 스냅샷 즉시렌더가 이걸 가려줬을 뿐, 캐시 없는 첫 방문·다른 기기에선 그대로 노출.
- **수정**:
  - `Code.gs` — ① `getSheet`가 스프레드시트 핸들을 **실행당 1회만** 열도록 `_ss` 캐시(전역은 실행 끝나면 소멸). ② **`getBootstrap(year)` 신설** — 한 실행에서 accounts·holdings·dividends(연/전체)·stockList·rate·alerts를 모아 반환. dividends는 전체를 **1회만 읽고 JS로 연도 필터**(시트 2회 읽기 제거). doGet에 라우팅 1줄.
  - `portfolio.html` — `gateCheck`가 검증용 `getAccounts` 대신 **`getBootstrap`을 쳐서 부팅 데이터까지 겸함**(검증 왕복 낭비 제거). `bootApp(boot)`→`init(boot)`가 그 응답을 그대로 씀. `getSheetData`는 **다른 스프레드시트라 경합이 없어** 동시에 1건만 따로. `applyBootstrap(b, sheet)` 신설. **9왕복 → 2왕복**(게이트 통과 시엔 사실상 1+1).
  - 기존 `loadExchangeRate`/`loadAccounts`/`loadAccountStats`/`loadStockList`/`refreshAlerts`는 탭 전환·저장 후 부분갱신에서 계속 쓰이므로 **그대로 둠**(init에서만 안 씀).
- **배포 순서 함정 대비**: 프론트를 먼저 푸시하면 재배포 전 백엔드가 `getBootstrap`을 몰라 `{error:'Unknown action'}` → 빈 화면. `init`에 **`b.error`면 예전 9건 경로로 폴백**을 넣어 **배포 순서에 의존하지 않게** 함. 재배포 후 자동으로 빠른 경로로 전환됨.
- **검증**: `node --check` 양쪽 OK. 원본 소스에서 함수를 그대로 추출해 가짜 시트/DOM으로 **18종 ALL PASS** — openById 실행당 1회, 페이로드 7종 포함, year 필터, boot 넘기면 getSheetData 1건만, boot 없으면 2건이 **동시 발사**, gateCheck가 getBootstrap을 치고 응답 반환, **첫 실패 시 3초 재시도 유지**, 재배포 전 응답 → 예전 경로 폴백.
- ⚠️ **다음 할 일(수동)**: **Code.gs 재배포**(자동 아님). **성공기준: 캐시 없는 상태(시크릿창)에서 비번 입력 → 숫자 다 뜰 때까지 3~5초**(지금은 15~30초). 개발자도구 Network에 부팅 요청이 **getBootstrap + getSheetData 2건만** 보이면 새 경로로 도는 것. 9건 그대로면 재배포가 안 된 것(폴백 중).

## 2026-08-05 (68) / — 재배포 후 최종 검증: 잠금 유지 + 프론트 반영 + 워밍 정상
- **재배포 직후 실측**(그동안 매번 깨지던 지점이라 선제 확인): `getAccounts` 토큰없음/9999 🔒차단, 1231만 데이터(792B). **재배포로 잠금 안 풀림** ✅
- **GitHub Pages 프론트 반영 확인**: 공개 `portfolio.html`(298,814B)에 `gateCheck()` + '비밀번호 문제 아님' 문구 **둘 다 존재** → (65) 수정분이 실제 서비스에 떠 있음 ✅
- **콜드→웜 확인**: 재배포 직후엔 4.7~32.6초(콜드스타트). 몇 번 친 뒤 **1.3 / 3.0 / 1.4초**로 안정 — 3039줄 주석의 "warm 1s대"와 일치. (66)의 토큰 첨부 덕에 keepWarm이 실제 경로를 계속 데울 수 있음 ✅
- 🧪 **테스트 함정(또 하나)**: Git Bash에 **`rev` 명령이 없음**. 파이프에 쓰면 조용히 빈 문자열이 되어 판정이 전부 뒤집힘(잠긴 걸 "통과"로 오판할 뻔). 파싱은 `-o 파일 -w '%{http_code} %{time_total}'` 조합으로 할 것.
- ⚠️ **미확인**: `markInputCells()` 실행 결과(시트 7월칸 노랑)는 셀 배경색이라 API/텍스트로 확인 불가 — 눈으로 볼 것.

## 2026-08-05 (67) / — APP_TOKEN 적용 완료 · 라이브 검증 통과 (이제 진짜 잠김)
- 사용자가 스크립트 속성 `APP_TOKEN=1231` 추가. **curl 라이브 검증 7종 전부 설계대로**:
  - 개인 액션 `getAccounts` — 토큰없음 🔒`unauthorized`(24B) / 틀린 9999 🔒`unauthorized` / **1231만 데이터(792B)**.
  - 공개 액션 — `getEtfNotices`·`hitCounter` 토큰 없이 통과 ✅ (공개 분배 페이지 무사).
  - `getExchangeRate` — 토큰없음 🔒차단 / 1231 통과. **(66)의 keepWarm 선제수정이 실제로 필요했음이 입증됨** (안 고쳤으면 5분마다 핑이 전부 막혀 콜드스타트 재발).
- **CORS 확인(중요)**: 오답 경로도 **302·200 두 홉 모두 `Access-Control-Allow-Origin: *`**, 응답은 CORS 에러가 아니라 **정상 JSON 200 `{"error":"unauthorized"}`**. → (65)에서 만든 분기가 브라우저에서 의도대로 동작: **오답은 "비밀번호가 틀렸습니다", 연결장애만 "서버에 연결할 수 없습니다"**.
- 🧪 **테스트 방법 함정 기록**: 리다이렉트 URL(`user_content_key`)은 **일회용**. `redirect_url`만 따로 떼어 두 번째 curl로 치면 **404 '현재 파일을 열 수 없습니다'** 가 나와 배포가 깨진 것처럼 보인다. 반드시 `curl -sSL`로 **한 번의 체인**에서 따라가야 함. (이번에 실제로 헛짚었다가 재측정으로 정정.)
- ⚠️ **남은 것**: `Code.gs` 재배포 여부 미확인. **재배포 전이면 배포본 keepWarm엔 토큰이 없어 5분 핑이 전부 차단 → 첫 로딩 20~30초**로 느려짐(그리고 게이트에서 "서버에 연결할 수 없습니다"가 뜰 수 있음). markInputCells·setupInputMarkTrigger도 재배포 후에만 존재.

## 2026-08-05 (66) / — APP_TOKEN 실제 적용 준비: keepWarm 토큰 누락 선제 수정
- **맥락**: (65)에서 "비번이 사실상 없다"를 확인 → 사용자가 `APP_TOKEN=1231` 적용 결정. 켜기 전에 **토큰 때문에 조용히 깨질 곳**을 전수 조사함(안 하면 4번째 사고).
- **전수 조사 결과**:
  - ❌ **`keepWarm`(Code.gs:3045)** — `?action=getExchangeRate`를 **토큰 없이** 핑. `getExchangeRate`는 PUBLIC_ACTIONS가 아니라 토큰 켜는 순간 `_authOk`에서 즉시 차단 → 실제 경로가 안 데워져 **콜드스타트(20~30초) 재발**. 3039~3042줄 주석에 "빈 핑으론 안 데워져서 실제 액션을 친다"고 명시돼 있어 그 의도가 무력화됨. **→ 수정**: 스크립트 속성에서 토큰을 직접 읽어 붙임(소스에 비번 안 박음, 미설정이면 종전대로).
  - ✅ 공개 페이지 `dist_notice.html` — 호출 액션이 `getDistribution`·`getEtfNotices`·`hitCounter` 3개뿐, **전부 PUBLIC_ACTIONS**. 영향 없음.
  - ✅ `public_dist_proxy.gs` — 화이트리스트가 `getDistribution`/`getEtfNotices`뿐. 영향 없음.
  - ✅ 프론트 `portfolio.html` — `api`/`apiPost`/`getAlerts`(1064)/`markAlertRead`(1106) **4곳 모두 token 전송 중**. 영향 없음.
  - ✅ 시간 트리거들(snapshot·refresh·checkDistNotices 등)은 함수 직접 호출이라 `doGet`을 안 거침. 영향 없음.
- **검증**: `node --check` OK. `_EXEC_URL` 사용처 4곳 확인 — 1997·2062는 카톡 링크(액션 없음), 3044 선언, 3049가 수정된 핑. 웹앱 URL로 나가는 자기호출은 keepWarm 하나뿐임을 확인.
- ⚠️ **다음 할 일(사용자 수동)**: ① Code.gs 재배포 ② **프로젝트 설정 → 스크립트 속성 `APP_TOKEN` = `1231`** 추가(재배포 불필요·즉시 적용) ③ 적용 확인은 curl 3종(토큰없음/1231/9999) → **없음·9999는 `unauthorized`, 1231만 데이터**여야 성공.

## 2026-08-05 (65) / — "비번이 자꾸 안 먹힘"(3회째) 진짜 원인 = 거짓말하는 에러 메시지
- **증상**: 재배포할 때마다 진입 게이트에서 "비밀번호가 틀렸습니다". 벌써 3번째. 콘솔엔 CORS 에러(`No 'Access-Control-Allow-Origin'`, `net::ERR_FAILED`)에 `token=1231`은 정상 전송됨.
- **라이브 실측(curl)**: ① `/exec`는 **302 + `Access-Control-Allow-Origin: *`** → googleusercontent 리다이렉트 → JSON 200. **배포·액세스 권한 정상**. ② `getAccounts`를 **토큰 없이 / 1231 / 틀린 9999** 세 가지로 호출 → **전부 792바이트 정상 데이터**.
- **원인 2단**:
  1. **백엔드는 비번을 검사한 적이 없음.** 스크립트 속성 `APP_TOKEN` 미설정 → `_authOk`의 `if (!secret) return true;`(하위호환 분기)로 **무조건 통과**. (474~476줄의 "다음 할 일: APP_TOKEN=1231 추가"가 계속 미이행 상태.)
  2. 그러니 그 빨간 문구는 비번 판정 결과가 아님. `submitGate`의 `catch`가 **CORS·콜드스타트·로그인페이지 등 모든 실패를 "비밀번호가 틀렸습니다"로 표시**. → **재배포 직후 엔드포인트가 흔들릴 때마다 "비번 오류"로 둔갑**. 재배포 3번 = 비번 문제 3번. 원인이 아니라 **오진 유도 메시지**가 범인.
- **수정(portfolio.html)**: `submitGate` 분기 분리 + `gateCheck()` 신설.
  - 서버가 **명시적으로 `error:'unauthorized'`를 준 경우만** "비밀번호가 틀렸습니다".
  - 그 외 예외는 **"서버에 연결할 수 없습니다 (비밀번호 문제 아님). 재배포 직후면 1~2분 뒤 다시 시도하세요."** 로 구분 안내.
  - `gateCheck()`가 **3초 뒤 1회 재시도** → 콜드스타트/재배포 직후 첫 실패는 자동 회복(사용자가 아무것도 안 해도 됨).
  - `#gateErr`에 `max-width:260px;text-align:center;line-height:1.5`(긴 문구 줄바꿈용).
- **검증**: 인라인 script `node --check` OK. **원본 함수를 파일에서 그대로 추출**해 가짜 DOM/api로 4종 ALL PASS — 정상→진입 / unauthorized→"비밀번호가 틀렸습니다" / 네트워크 2회 실패→연결오류 문구(호출 2회) / 1회 실패 후 성공→진입(3001ms, 재시도 실동작 확인).
- ⚠️ **보안 미결(사용자 결정 필요)**: 지금 **비번은 사실상 없는 상태**. GitHub Pages 공개 페이지 + 토큰 미검증이라 **exec URL만 알면 누구나 계좌·보유·분배 데이터 조회 가능**(curl로 실증됨). 진짜로 잠그려면 Apps Script → **프로젝트 설정 → 스크립트 속성에 `APP_TOKEN` = 원하는 비번** 추가(재배포 불필요, 즉시 적용). 추가 순간부터 그 값과 다른 입력은 진짜로 차단됨. 그냥 두면 편하지만 무방비.

## 2026-08-05 (64) / — 입력할 칸 노란색 표시 (markInputCells 신설)
- **증상**: 7월말 분배금을 넣어야 하는데 시트엔 **8월(N열)**이 노랗게 칠해져 있음. 입력할 칸은 **7월(M열)**.
- **원인규명**: 전 히스토리 조사(`git log --all -p -S 'ffff00' -- Code.gs`) 결과 **repo엔 노란색을 칠하는 코드가 존재한 적 없음** — `setBackground`은 전부 `setBackground(null)`(지우기)뿐. 노란색 커밋 4개(25e23d0·7588d93·ed2ea3f·fdf6ae7)도 전부 지우기/진단. fdf6ae7 메시지에 원인=**"매크로 미실행"**이라 적혀 있음 → **칠하는 주체는 시트 B2 `마크로스` 버튼(스프레드시트 내장 매크로)**이고, 그게 '현재 달'을 칠해서 한 달 어긋남. 세션 기록 검색도 0건(색인 없음). 시트 내장 매크로는 이 repo(역대 8파일)에 없고 내 도구로 열 수도 없음.
- **해결 방향**: 매크로를 찾아 고치는 대신, **`Code.gs`에 칠하는 기능을 직접 신설**. `clearOldYellowCells`가 이미 같은 시트 배경을 건드리므로(`_monthCols` 재사용) 손이 닿음. 기존 `마크로스` 버튼은 안 누르면 됨.
- **구현(Code.gs)**: `markInputCells()` + 헬퍼 `_yearBlockStart(isYearTab, values, year)`, 상수 `MARK_COLOR='#ffff00'`, 트리거 `setupInputMarkTrigger()`(수동 실행 섹션, 매일 06시).
  - **대상 달**: 1~9일 = 지난 달(전월 월말분 입력 시기), 10일 이후 = 이번 달. 연도 넘김 처리(1/5 → 2025-12).
  - **대상 행**: D열이 `월중`/`월말`인 월배당 행만. 반기·분기·무배당은 매달 안 들어와 제외(안 그러면 PLTR 등이 매달 노래짐).
  - **이미 값 있는 칸은 건너뜀** → 7월 이미 채운 월중 행(6·7·9·11·16)은 안 칠해짐.
  - 실행 시 **먼저 '값 없는 노랑칸'(= 안 채운 묵은 표시)을 전 연도 블록에서 제거** → 표시가 한 달치만 남음. 빈 칸이라 `clearOldYellowCells`의 7일 규칙엔 안 걸리므로 이 청소가 필요. 값을 채우면 그때부터 7일 뒤 기존 규칙이 지움(두 함수가 자연스럽게 맞물림).
- **검증**: `node --check` OK. 로직 테스트 12종 ALL PASS — 대상달 7종(8/5→2026-7, 8/9→7, 8/10→8, 8/12→8, 9/3→8, 1/5→2025-12, 1/15→2026-1), 컬럼 4종(7월→M, 8월→N, 1월→G, 12월→R), 행선별 1종(스크린샷 실제 D열/M열로 → M8·M10·M12·M18만 칠해짐).
- ⚠️ **다음 할 일(수동)**: ① Code.gs 재배포(자동 아님) ② 편집기에서 **`setupInputMarkTrigger()` ▶ 1회**(트리거 설치) ③ 지금 바로 보려면 **`markInputCells()` ▶ 1회** → 성공기준: **8월(N열) 노랑 사라지고 7월(M열) 빈 칸만 노래짐**, 로그에 `2026년 7월 칸 N개 표시`. ④ 시트 `마크로스` 버튼은 이제 누르지 말 것(다시 8월 칠함).

## 2026-07-29 (63) / — 분배금 파싱 이상을 카톡으로 선(先)알림 (checkAndLogAlerts 보강)
- **맥락**: "공지 올라왔는데 왜 반영 안 되냐"가 반복됨. 근본 원인 = **고장을 알려주는 신호가 없어 유일한 탐지기가 사용자**. 파서가 죽어도 `알림로그` 시트에만 조용히 쌓이고 카톡은 신규공지만 나갔음.
- **라이브 진단(force=1 실측)**: 지금은 6개 운용사 전부 정상 파싱(kodex170/tiger110/ace48/rise76/plus26/sol40), 캐시=강제파싱 동일, 공개 dist_notice 렌더도 완벽(월중7/13~20·월말7/28~8/4, SOL 포함, 분배락 fillRakil로 자동채움). 즉 **이번엔 표시 버그 없음** — raw JSON 이상(KODEX 상단 공시일 6월/분배락 None)은 프론트가 per-item sched·prevBizDay로 보정해 화면엔 안 뜸. 남은 실질 취약점만 추림: SOL 자사파서 사망(fallback), PLUS 회차 '?' 5건.
- **수정(Code.gs, surgical)**: `checkAndLogAlerts` 파싱경고 블록에 ① fallback 사용(자사파서 사망) ② 회차 미상(cycle 없음/'?') 감지 추가, 그리고 **0건·fallback·회차미상을 카톡으로도 발송**(기존엔 로그만). 카톡 스팸 방지 위해 `_addAlert`가 신규추가 여부(bool) 반환 → 새로 감지된 것만 push. `flushKakaoPending` 헤더 '새 분배 공지'→'ETF 분배 알림'(공지+경고 겸용). 트리거·새 함수 없음(기존 30분 `checkDistNotices` 그대로 활용).
- **검증**: `node --check` OK. 배포 후 성공기준 = 편집기서 `checkAndLogAlerts()` ▶ 1회 → 현재 라이브 상태상 카톡 **"⚠️ SOL 자사 파서 실패(뉴스 우회)" + "⚠️ PLUS 회차 미상 5건"** 도착 + `알림로그`에 동일 2행.
- ⚠️ **다음 할 일(수동)**: Code.gs 재배포(자동 아님). 카톡은 기존 KAKAO_* 속성 필요.

## 2026-07-29 (62) / — TIGER 월말 분배금 안 뜸: 금액 '83원' 형식 파싱 실패
- **증상**: 분배금공지에 TIGER가 안 나옴("또"). 새로고침 무관.
- **진단(라이브+원본 HTML 실측)**: `getEtfNotices(tiger)`엔 7월말 공지 2건(2026.07.29, 공시일 본문상 7/31) 존재. 그런데 `getDistribution(tiger)`는 7/13 월중 + 6/26(6월 월말, 묵음)만 반환, 7월 월말 0건. 원본 상세(key 752) HTML을 `fetchDist_tiger_detail` 로직으로 직접 파싱 → **티커 77행 매칭되나 amount 전부 null**. 원인: **월말 표의 분배금 칸이 `"83원"`(숫자+원) 형식**인데 파서 [Code.gs] 금액 정규식이 `/^[\d.]+$/`(맨숫자만)이라 전 행 스킵(→ 0건 → smarttoday/이력 폴백으로 옛 6/26이 남음). 월중(750)·6월말(748)은 `"68"`·`"100"` 맨숫자라 정상이었음 = TIGER가 7월말부터 표기 바꿈.
- **수정**: 금액 정규식 `/^[\d.]+$/` → `/^\d[\d.]*\s*원?$/`(끝 '원' 허용, `parseFloat('83원')=83`). 다른 파서·이슈어는 미변경(YAGNI).
- **검증**: 실제 752 HTML fixture 회귀테스트 — 기존 0건 / 수정 77건(전부 amount>0), 월중 맨숫자 형식도 9건 정상. `node --check` OK.
- ⚠️ **다음 할 일(수동)**: Code.gs 재배포. 배포 후에도 TIGER 분배캐시가 옛 데이터를 들고 있으면 분배금공지에서 **강제 새로고침(force)** 1회로 갱신(또는 캐시 TTL 만료 대기). 다른 운용사도 '원' 표기로 바뀌면 같은 증상 → 이 커밋 참고.

## 2026-07-29 (61) / — 보유 ETF 괴리율 -1% 이탈 카톡 알림 신설
- **요청**: 보유 종목 괴리율이 -1% 밑으로 내려가면 카톡 알림.
- **구현(Code.gs)**: `checkDeviationAlerts()` — 보유 국내(KRW) 종목을 holdings에서 뽑아, 네이버 `etfItemList` 1회 호출로 티커별 현재가·NAV→괴리율 계산(기존 소스 재사용). **괴리율 < -1% 크로싱 시 1회 발송**, -0.5% 회복 시 재무장(히스테리시스로 -1% 근처 깜빡임 스팸 방지). 상태는 Script 속성 `DEVIATION_STATE`(JSON, 티커별 above/below). 미국종목·비ETF는 목록에 없어 자동 제외. 휴장(등락률 거의 0)이면 스테일 방지로 스킵.
- **발송**: 장중(평일 09~16시)에만 체크→항상 카톡 발송시간대(08~23) 안이라 `sendKakaoMemo` 즉시 발송(밤 배치 불필요). 카톡 실패해도 앱 🔔 알림로그(`_addAlert`, 운용사='괴리율')엔 기록. 상수 `DEV_ALERT=-1.0`/`DEV_REARM=-0.5`.
- **트리거**: `setupDeviationTrigger()`(수동 실행 섹션) — 30분마다, 내부에서 장중만 체크.
- **검증**: Node 상태머신 테스트(크로싱·재무장·히스테리시스·흐름 11종) ALL PASS, `node --check` OK.
- ⚠️ **다음 할 일(수동)**: Code.gs 재배포 → 편집기에서 `setupDeviationTrigger()` ▶ 1회 설치. (테스트는 장중에 `checkDeviationAlerts()` ▶ 수동 실행 — 현재 -1% 밑인 보유 ETF 있으면 카톡 옴.) 카톡 발송엔 기존 KAKAO_* 속성 필요(분배알림과 동일).

## 2026-07-29 (60) / — 누적수익률 차트 헤더 손익을 대시보드와 통일
- **문제**: 차트 헤더의 -값(`tDiff = tLast − tFirst` = 차트 기간 시작 대비 변동, 창 크기 따라 변동 -54,785,924)이 대시보드 수익금(평가금−원금 -39,572,112)과 달라 혼란. "통일해달라".
- **수정([portfolio.html])**: 대시보드 렌더에서 `valued/pl/plPct`를 `window._dashV/_dashPl/_dashPlPct`에 저장 → 차트 헤더가 이 값(원금 대비 손익)을 표시(총액·손익·%). 실시간 평가금 없으면 기존 기간변동으로 폴백. 델타 포맷도 `toLocaleString`으로 대시보드와 동일. 같은 탭·같은 렌더라 타이밍 안전.
- **검증(브라우저 실측)**: 대시보드 `ds-pl` = `-39,572,112 (-6.8%)`, 차트 헤더 = `5.42억 -39,572,112 (-6.8%)` → 완전 일치 확인. 프론트만 수정, 재배포 불필요.

## 2026-07-29 (59) / — 대시보드 누적수익률 '일' 모드 한 달치로 확대
- 계좌별 누적 수익률 차트 일 모드가 최근 14평일만 표시([portfolio.html] maxN 로직) → **22평일**로 확대(주말 제외라 달력상 약 한 달). month(24)·year(10)은 그대로. 프론트만 수정, 재배포 불필요. 데이터가 그만큼 없으면 있는 만큼만 표시(에러 없음).

## 2026-07-29 (58) / — 노란색 "안 지워짐" 원인규명 + 해결(diagYellow)
- **증상**: (57) 재배포·실행했는데도 노랑 그대로. 색코드/조건부서식/탭 후보가 여럿이라 추측 대신 진단으로 확정.
- **진단**: 임시 `diagYellow` 액션(doGet, 토큰 게이트, 읽기전용) 추가 → 재배포 후 curl로 실측. 결과: **탭 정확**(isYearTab=false, 보는 탭 맞음), **색 정확**(M6·M25 등 실제 `#ffff00`, isYellow=true), **범위 정확**(monthColsLen=24=2026+2025), **스캔 27칸 매칭**. 즉 코드는 정상 — `clearAllYellowNow()`가 **실제로 실행이 안 됐던 것**(setup 트리거만 돌렸거나 clearOldYellowCells만 실행→7일 규칙이라 당일 미삭제).
- **해결**: 편집기에서 **`clearAllYellowNow` ▶ 1회** 실행. 재실측 → `matchedCount:0`, 프로브 전부 `#ffffff`. 노랑 27칸(2026 7월 + 2025 12월) 전부 삭제 확인.
- **정리**: `diagYellow` 액션·함수 repo에서 제거. ⚠️ **라이브 배포엔 아직 diagYellow 남아있음**(직전 재배포분) — 토큰 게이트·읽기전용이라 무해, **다음 재배포 때 자동 소멸**(지금 굳이 재배포 불필요). 배운 점: "안 지워진다" 류는 매크로 미실행/서식종류(조건부 vs 수동) 확인이 우선 → diagYellow 패턴 재사용 가능.

## 2026-07-29 (57) / — 노란색 자동삭제: 전 연도 블록으로 확대 + 즉시정리 함수
- **피드백**: (56) 구현이 **올해(2026) 블록만** 스캔 → 오른쪽 2025년 블록(X:AI)의 묵은 노랑은 영영 안 지워짐. 또 "아직 노란색 안 없어졌다" = 첫 실행은 기록만·7일 뒤 삭제라 즉시 안 사라지는 게 설계상 정상(오해 소지).
- **수정(Code.gs)**:
  - `_monthCols(isYearTab, values)` 신설 — 상단 3행에서 `YYYY년` 라벨을 **전부** 찾아 각 블록 1월~12월(2026 G:R + 2025 X:AI + …) 컬럼을 모두 수집. `clearOldYellowCells`가 이걸 써서 **모든 연도 블록**을 대상으로 7일 규칙 적용(기존 단일 startCol/endCol 제거).
  - `clearAllYellowNow()` 추가(수동 실행 섹션) — 누적 노랑(전 연도)을 **7일 안 기다리고 지금 즉시** 전부 해제. 도입 시점 백로그(2025년 12월 등) 정리용 1회 실행. 이후 신규분은 매일 트리거가 7일 규칙으로 관리.
- **검증**: `_monthCols` 다중블록 인식 테스트(2026+2025 = [6..17,23..34]) PASS, `node --check` OK.
- ⚠️ **다음 할 일(수동)**: 재배포 후 ① `setupYellowClearTrigger()` ▶(트리거 설치) ② 백로그 즉시 정리 원하면 `clearAllYellowNow()` ▶ 1회. 실제 하이라이트가 연노랑(#fff2cc류)이면 `_isYellow` b≤160 임계값 완화 필요(현재는 밝은 노랑 기준).

## 2026-07-29 (56) / — 분배금 탭 노란색 신규표시 7일 뒤 자동삭제
- **요청**: 분배금 시트에 새로 입력한 값을 노란색으로 표시하는데, 며칠 지나면 없애기로 함. 안 지우면 다음(월말) 입력분도 노랑이라 헷갈림. → **입력분 노랑을 7일 뒤 자동 해제**.
- **구현(Code.gs)**: `clearOldYellowCells()` 신설(+헬퍼 `_ymd`/`_daysBetween`/`_isYellow`, `YELLOW_CLEAR_DAYS=7`). 매일 06시 트리거(`setupYellowClearTrigger`, 수동 실행 섹션).
  - 분배금 탭 **올해 블록 월별칸만**(G:R, 5행~) 스캔 — 라벨(A~C)·D열 배당구분색·헤더/총계행은 범위 밖이라 무관.
  - 숫자 든 노랑칸을 발견하면 앱 DB `_노란셀추적` 탭에 **최초발견일** 기록 → 7일 경과 시 `setBackground(null)`. 7일 내 직접 지운 칸은 추적에서 자동 정리.
  - 노랑 판정 `_isYellow`: r≥200·g≥180·b≤160 (ffff00·ffff66·ffd966 등 잡고 흰색·연노랑#fff2cc·타색 제외). 첫 실행은 기록만·삭제 없음(7일 안전창).
- **검증**: Node 테스트(_isYellow 9종 + 7일 경과 4종) ALL PASS, `node --check` OK.
- ⚠️ **다음 할 일(수동)**: Code.gs 재배포 후, 편집기에서 `setupYellowClearTrigger()` 1회 실행(▶)해 트리거 설치. 색 인식 확인은 `clearOldYellowCells()` 수동 1회 → `_노란셀추적`에 현재 5개 셀(M6·7·9·11·16) 잡히는지 보면 됨(삭제 안 됨). 실제 하이라이트가 연노랑이면 `_isYellow` b임계값 완화 필요.

## 2026-07-29 (55) / — 공지 알림 중복 발송 수정 (공시일 형식 정규화)
- **증상**: "📢 새 분배 공지" 알림이 이미 올라온 운용사까지 반복해서 옴("여전히 모든 회사가 다 온다").
- **근본 원인**: 중복 방지는 `checkAndLogAlerts`가 `fp.pubDate !== prev.pubDate`([Code.gs] `_fingerprint`/메타 비교) 하나로 판단. **TIGER만 공시일을 `7/29` 숫자형으로 뱉는데**, 이 문자열을 `_파서메타` 시트에 `setValues`하면 **구글 시트가 Date로 자동변환** → 다음 회차 `getValues`가 Date 객체를 돌려줌 → `Date !== "7/29"` 항상 참 → TIGER가 30분마다 재발송. `_addAlert`는 동일 메시지를 중복억제해 **알림로그엔 안 보이지만**, `_notifyCal`/`_notifyKakao`는 억제 안 해 캘린더·카톡만 반복(+공용 CAL_PENDING 큐 flush로 "모든 회사"처럼 보임). 라이브 알림로그로 확증(TIGER=`7/13`,`7/29` 숫자형 / 나머지 5개사=`N월 N일` 한글형).
- **수정**: `_normPubDate(v)` 헬퍼 추가 — 공시일을 `M월 d일` 한글형으로 통일(`7/29`·`7.29`·`2026-07-28`·Date 객체 모두 처리). 한글형은 시트가 Date로 안 바꿈 → 문자열 그대로 왕복. `_fingerprint` 반환 pubDate와 직전 메타 읽기(prev.pubDate) **양쪽**에 적용 → 배포 직후 재발송 1회도 없음.
- **검증**: 스크래치 Node 테스트(형식 정규화 9종 + 왕복 버그 재현/수정) ALL PASS, `node --check` 문법 OK.
- ⚠️ **다음 할 일(수동)**: Apps Script 편집기에 Code.gs 붙여넣고 **기존 배포 편집(새 버전) 재배포**해야 실제 적용(URL 유지). 그 전까지 TIGER 반복 지속.
- ⏸ **대기 중 요청**: 분배금 탭 노란색 셀 자동삭제(입력 N일 뒤 배경 해제) — 며칠로 할지 사용자 회신 대기(추천 7일). 정해지면 매일 트리거 + 최초발견일 기록 방식으로 구현 예정.

## 2026-07-29 (54) / 집 — SOL 월말 파서: 분배율 '업데이트 예정'도 종목 로드
- **증상**: SOL 월말 공지가 09:41 게시됐는데 앱에 종목이 거의 안 뜸(1건). 화면 잔여 14건은 지난달(6월) 이력 캐시.
- **근본 원인**: SOL 월말 글의 **분배율(%) 칸이 아직 "업데이트 예정"**(숫자 미입력, 금액은 있음). `_solParsePost` 정규식이 각 행을 `금액 + 분배율(소수)`로 끊어서, 분배율이 숫자가 아니면 그 행을 통째로 스킵 → 종목 0~1건. (월중 글은 분배율이 채워져 있어 정상.) *캐시 아님(force로도 재현).*
- **수정(Code.gs, `_solParsePost`)**: 정규식 3번째 칸을 `(소수 분배율 | '업데이트 예정')`로 완화 → 확정 전이라도 종목·예상분배금 로드, `rate=null`(화면엔 '-'). SOL이 분배율을 채우면 다음 파싱에서 소수 분기로 잡혀 **자동 갱신**(재남 요구 2번 = 캐시 TTL·폴링으로 저절로 됨).
- **검증**: 실제 월말 글(logNo 224361381567) 파싱 → 19종목 전부 정확(이름 내 숫자 "SOL 200 Top10" 등 포함), rate=null. null-rate 안전: 두 프론트 렌더가 `it.rate!=null&&it.rate>0?…:'-'`, `attachDistHistory`는 rate 계산 안 함, 스크리너는 별개 경로, 배당입력 탭은 수동.
- **다음 할 일(재남 손수)**: (53)과 동일 — Code.gs 저장 → `setupDistTriggers()` → `refreshAllDistributions()` 실행하면 SOL 월말 19종목 반영. (방문자 카운터는 별도 웹앱 재배포.)

## 2026-07-29 (53) / 집 — 알림 30분 폴링(OCR 캐시) + 공시일/회차 오류 수정
- **한 일 3가지**:
  - **① 30분 폴링 + OCR URL 캐시** (사용자 선택 "2번"): `checkDistNotices` 트리거를 30분 간격으로 바꾸고(창 날·09~18시만 통과), `ocrImageText`에 **이미지 URL 키 CacheService 캐시(6h)** 추가 → 폴링이 잦아도 Vision OCR은 '새 이미지'에만 호출. Vision 무료 1,000건/월 안에서 여유 유지.
  - **② KODEX 6월(stale) 오독 수정**: 최상위 `schedule.공시일`이 월중/지난달로 stale(예: KODEX 6월 11일)이라 알림이 틀린 날짜를 냈음. `_fingerprint`를 **'현재 회차(월중/월말)+이번 달' 종목들의 sched.공시일 최빈값**으로 산출하게 변경(+`_pubMonth` 헬퍼). → KODEX 7월 28일 등 정확한 값.
  - **③ 월말·월중 혼재 → 월말만**: 같은 ②변경으로 `fp.cycles`가 현재 회차 하나만(`월말`) 표기. 지난달 잔여·`cycle:None`은 자동 제외. 이번 달 해당 회차 없으면 발송 안 함(ACE·SOL 오탐 방지).
  - 감지 조건도 `빈값→값` 전환 발송되게 완화(신규 월말 공지 첫 등장 놓침 방지).
- **검증**: 라이브 API로 6개 시뮬 → KODEX 7/28·TIGER 7/29·RISE 7/28·PLUS 7/28(월말), ACE·SOL 미발송. 정확.
- **다음 할 일(재남 손수, Apps Script 편집기)**:
  1. 새 `Code.gs` 붙여넣고 **저장**(②③·OCR캐시·09~18시 게이트는 저장만으로 트리거에 반영, 재배포 불필요).
  2. **`setupDistTriggers()` 1회 ▶실행** — 트리거를 30분 간격으로 갈아끼움(①). 저장만으론 트리거 스케줄이 안 바뀜.
  3. **`refreshAllDistributions()` 1회 ▶실행** — 오늘 잘못 온 알림을 바로잡아, 정정 알림(KODEX 7/28·TIGER 7/29·RISE 7/28·PLUS 7/28) 재발송.
  4. (별개) (51) 방문자 카운터는 여전히 **웹앱 새 버전 재배포** 필요.

## 2026-07-29 (52) / 집 — 월말 공지 놓침 버그: 공지창 23~월말로 확대
- **증상**: TIGER 월말 공지가 올라왔는데 카톡·구글캘린더 알림이 안 옴.
- **근본 원인**: 감지(`checkAndLogAlerts`)를 도는 트리거는 `checkDistNotices`(10·14·18시) 하나뿐인데, 맨 앞 `_inNoticeWindow(day)`가 **8~12일·23~27일만** 통과시킴. 오늘 29일은 창 밖 → 감지가 아예 안 돌아 카톡·달력 둘 다 없음. 월말 공지가 요즘 28~29일에 올라와(예: KODEX 7/28) 27일 경계 밖으로 샘. (`refreshAllDistributions`는 트리거로 안 걸려 있음.)
- **수정(Code.gs)**: `_inNoticeWindow` 월말 창을 `day>=23`(23일~말일 전체)로 확대. `_testNoticeWindow` self-check와 주석도 갱신.
- **다음 할 일(재남 손수)**:
  - ① **이번 TIGER 공지 지금 잡기**: Apps Script 편집기에 새 Code.gs 붙여넣기·저장 → 함수 드롭다운에서 **`refreshAllDistributions`** 선택 후 ▶1회 실행. (창 게이트 없이 감지 → 카톡+캘린더 즉시 발송.)
  - ② 트리거 창 수정은 **저장만 하면 반영**됨(시간 트리거는 저장된 코드로 돎, 웹앱 재배포 불필요). 단 (51)의 방문자 카운터 변경은 여전히 **웹앱 새 버전 재배포** 필요.

## 2026-07-29 (51) / 집 — 방문자 카운터: 오늘 + 누적 표시
- **한 일**: 방문자수를 `👁 오늘 N · 누적 M` 형태로 변경.
  - **Code.gs** `hitCounter` 케이스: 누적(`VISIT_COUNT`)에 더해 오늘(`VISIT_TODAY`, 날짜 `VISIT_TODAY_DATE`)을 별도 집계. 날짜(Asia/Seoul 자정) 바뀌면 오늘 카운트 리셋. 응답에 `today` 필드 추가.
  - **dist_notice.html** `bumpVisitor()`: 표시 문구를 `👁 오늘 X · 누적 Y`로 변경.
- **다음 할 일(재남 손수)**: ① **메인 Code.gs 재배포**(카톡/캘린더 때 하던 그 배포) — 안 하면 `today`가 안 와서 오늘은 0으로만 뜸. ② **dist_notice.html → jjk-dist repo의 index.html로 수동 미러** 후 push해야 공개 페이지에 반영됨.

## 2026-07-29 (50) / 집 — 공개 분배금공지 페이지(dist_notice.html) 모바일 UI 4건 + 방문자 카운터
- **✅ 최종 완료 확인(재배포까지)**: 메인 Code.gs 재배포됨 → 라이브 API 검증 완료. 방문자 카운터 작동(페이지에 `👁 5` 표시), KODEX 월말 공시일 7/29→**7/28** 반영 확인. 프론트(탭·표·제목)도 jjk-dist 라이브 반영 완료. **남은 손수 작업 없음.**
- **배경**: 공개 페이지(github.io/jjk-dist)에 광고 붙여 수익화 계획 → 우선 모바일 UI 다듬기. (스티비 이메일 구독은 나중에 하기로 보류.)
- **수정(dist_notice.html)**:
  - ① 달력↔일정 사이 큰 빈 공간 제거: 세로 스택에서 `.p-cal`의 `flex:0 1 792px`가 **높이**로 먹혀 792px 강제되던 것 → 모바일 미디어쿼리에 `.p-notice,.p-cal,.p-sched{flex:none}` 추가. 갭 321→19px 확인.
  - ② 운용사별 일정 표 '지급' 열 잘림: 모바일 `#distScheduleList{min-width:420px}`→`0`, 표 `font-size:10px`, th `padding:5px 2px`. 스크롤폭 320≤화면321 → 좌우 스크롤 없이 지급까지 표시 확인.
  - ③ 제목에 (몇월): 공지 패널 `📢 (7월) 분배금 공지`, 상단 로고·브라우저 탭 제목도 `(7월) ETF 분배금 공지`로 JS에서 동적 세팅.
  - ④-1 (추가) 공지종목 전체 표 '주기' 칸 세로쪼개짐 수정: 월중/월말 배지 span과 td에 `white-space:nowrap` → 가로 한 줄 유지(칸 자동 확장).
  - ④-3 (추가) 탭 메뉴 신설(**모바일 전용**): 제목 아래 sticky 탭바(달력·분배금공지·분배일정·종목전체). **PC는 예전처럼 전부 한 페이지**(탭바 `display:none`, 숨김 규칙을 모바일 미디어쿼리 안에만 둠). `showSection`은 인라인 display가 아니라 `.tab-active` 클래스만 토글 → PC엔 영향 없음. 기본 탭=달력(p-cal에 tab-active 하드코딩). + 종목 카드 내부 스크롤(max-height) 모바일서 제거 → 카드 안에 스크롤 갇히던 문제 해결.
  - ④-2 (추가) 공지종목 표 모바일서 '분배율' 열 잘림 수정: 종목명 max-width를 인라인→클래스 `.dg-name`(데스크톱 160px), 모바일 미디어쿼리서 100px로 축소. 표폭 361→308(=카드폭), 넘침 0, 분배율 열까지 다 보임.
  - ④ 방문자 카운터: 헤더 로고 옆 `👁 N`. `bumpVisitor()`가 공개 프록시 `hitCounter` 호출(세션당 1회 +1, 표시는 매 로드). 실패해도 페이지 무영향.
- **수정(public_dist_proxy.gs)**: `doGet`에 `action=hitCounter` 처리 추가 — 프록시 자체 ScriptProperties(`VISIT_COUNT`)에 집계, 캐시 우회, `bump='1'`이면 +1. 개인 GAS·시트는 안 건드림.
- **배포 방식 확인+실행**: 공개 페이지는 별도 저장소 **`jjk-dist`의 `index.html`**(GitHub Pages). `dist_notice.html`을 `jjk-dist/index.html`로 **수동 미러**해야 반영됨(데이터는 프록시로 live fetch라 무관). → 오늘 수정분을 jjk-dist에 복사·푸시 완료(f0a4ae7), 라이브에서 갭·표·제목 3건 반영 확인. (메모리 [[public-page-deploy]] 저장)
- **공시일 OCR 오독 수정(Code.gs)**: KODEX 월말 공시가 실제 등록일 7/28인데 달력·표엔 7/29로 뜸 → 원인은 `getDistribution`이 공시일을 게시일(등록일)로 잡아놓고 **OCR 값으로 덮어써서**(OCR이 28→29 오독). 세 파서(삼성/KODEX 1129·ACE 1430·PLUS 1560)에서 **공시일 OCR 덮어쓰기만 제거** → 공시일=등록일 유지, OCR은 분배락/기준/지급만 채움. ⚠️ 재배포 필요 + 분배캐시 때문에 앱에서 **force 새로고침**해야 새 값 반영.
- **[정정] 방문자 카운터 위치**: 공개 페이지 백엔드는 별도 프록시가 아니라 **메인 Code.gs 하나**임(API 상수 3곳 전부 같은 배포 ID 확인). `public_dist_proxy.gs`는 배포 안 된 사장 파일이라 거기 넣은 hitCounter를 되돌리고, **Code.gs**의 `PUBLIC_ACTIONS`에 `hitCounter` 추가 + `doGet` case 추가로 이식함.
- **다음 할 일(재남 손수)**: **메인 `Code.gs`를 재배포**(카톡/캘린더 때 하던 그 배포). 그러면 방문자수(👁) 뜸. 카톡·캘린더 알림 코드도 같이 최신화됨. (이것만 남음)
- **URL 개인이름 문제**: `jaenamking1-collab.github.io/...`에 계정명 노출 → 넷리파이/클라우드플레어 Pages 연결 시 실명 없는 URL + push 자동배포(수동 미러도 없어짐). 재남 결정 대기.

## 2026-07-28 (49) / 집 — 카톡 백업으로 구글 캘린더 알림 추가(Code.gs)
- **배경**: 카톡 '나에게 보내기'가 씹힐 때를 대비해, 새 분배 공지 감지 시 **구글 캘린더에도 "지금 울리는" 일정을 자동 등록**. 폰 구글 캘린더 앱 알림이 확실한 백업. (네이버·카카오 달력은 구글 구독 방식이면 동기화 지연/알람 누락 가능 → 보너스 취급.)
- **Code.gs 수정**(전부 카톡 로직 미러링):
  - `_notifyCal(lines)`/`flushCalPending()` 신규 — CAL_PENDING 대기열, 야간대기 규칙은 카톡과 동일(`_kakaoWithinWindow` 재사용). `flushCalPending`이 `CalendarApp.getDefaultCalendar().createEvent`로 10분짜리 일정 생성 + `addPopupReminder(0)`.
  - **카톡과 독립** — `checkDistNotices`에서 `_notifyKakao` 옆에 `_notifyCal`를 별도 try로 호출(카톡 실패해도 캘린더는 뜸).
  - `setupKakaoTrigger`에 `flushCalPending` 8시 트리거 추가, 점검용 `testCal()` 추가.
- **다음 할 일(재남 손수)**: ① Apps Script에 Code.gs 붙여넣고 **재배포** ② 첫 실행 시 **캘린더 접근 권한 허용** ③ `setupKakaoTrigger` ▶재실행(트리거 재설치) ④ `testCal` ▶실행 → 폰 캘린더 알림 오는지 확인.

## 2026-07-28 (48) / 집 — 카카오 알림 연동 완료(testKakao 발송 성공) + client_secret 지원
- **완료**: 카카오 개발자 앱 `배당알림` 생성 → 스크립트 속성 3개 저장(`KAKAO_REST_KEY`·`KAKAO_REFRESH_TOKEN`·`KAKAO_CLIENT_SECRET`) → `testKakao` **발송 성공** 확인. `setupKakaoTrigger`(8시 flush) 설치 안내까지.
- **Code.gs 수정**: `_kakaoAccessToken`이 `KAKAO_CLIENT_SECRET` 속성 있으면 refresh 요청에 `client_secret` 동봉(시크릿 '사용함' 앱 대응, 없으면 생략).
- **삽질 로그(다음에 또 하면 참고)**:
  - **Redirect URI 위치**(신 콘솔): 카카오 로그인 메뉴 아님. **앱 → 플랫폼 키 → REST API 키(⋮/카드) → "카카오 로그인 리다이렉트 URI"** 에 등록. (Web 플랫폼 등록 개념 사라짐, 리다이렉트가 REST 키에 딸림.)
  - **KOE010**(Bad client credentials): 앱의 **클라이언트 시크릿 '사용함'**이면 authorization_code 교환·refresh 둘 다 `client_secret` 필수. authorize(동의)는 secret 없이 통과하지만 token 단계에서 막힘.
  - **KOE322**(invalid refresh token): 스크립트 속성에 붙여넣은 refresh_token 값이 **앞글자 누락**돼 있었음(`MV4C…`→`V4C…`). 값 정확성 필수.
  - 인가 URL은 브라우저(또는 `Start-Process "..."`)로 열기 — PowerShell에 그냥 붙이면 `&` 파서에러.
- **다음 할 일**: 실사용 관찰(10·14·18시 신규공지 → 카톡). 보안상 노출된 REST키/시크릿은 필요 시 재발급(단 refresh_token 재발급 동반).

## 2026-07-28 (47) / 집 — 새 분배 공지 카톡 알림(B: 백엔드 직접, Code.gs) [재배포+카카오설정 필요]
- **요청**: 각 운용사 새 공지 나오면 나에게 카톡. 야간(23~08시) 발송 상황이면 아침 8시로 미룸.
- **구현(Code.gs)**: `checkAndLogAlerts`의 신규공지 감지 분기에서 메시지 수집(`kakaoMsgs`) → 실행 끝에 `_notifyKakao` 호출(try/catch로 감지·로그는 절대 안 깨짐). 카카오 memo(나에게 보내기) 연동: `_kakaoAccessToken`(리프레시→액세스, 회전된 리프레시 저장), `sendKakaoMemo(text≤200자)`, `_kakaoWithinWindow`(08~23), `_notifyKakao`(대기열 `KAKAO_PENDING`에 쌓고 시간대면 즉시 flush), `flushKakaoPending`(한 통으로 발송·성공 시 큐 삭제). 야간 감지분은 대기 → 아침 8시 `flushKakaoPending` 트리거가 발송.
- **수동 실행(맨 아래 배너)**: `setupKakaoTrigger`(매일 8시 트리거 설치), `testKakao`(연동 점검용 테스트 발송).
- **필요 설정(사용자)**: 스크립트 속성 `KAKAO_REST_KEY`, `KAKAO_REFRESH_TOKEN`. 없으면 조용히 skip(감지·앱 알림은 그대로). 카카오 개발자 앱 생성 → talk_message 동의 → 인가코드→refresh_token 발급 → 속성 저장 → **Code.gs 재배포** → `testKakao`▶ → `setupKakaoTrigger`▶.
- **검증**: `node --check` 문법 통과. 카톡 실제 발송은 토큰 설정+재배포 후에만 확인 가능(미완).
- **다음 할 일**: 사용자 카카오 토큰 발급·속성 저장·재배포·testKakao 확인.

## 2026-07-28 (46) / 집 — 분배금공지: 진입 시 새 공지 누락 → 백그라운드 자동 force
- **증상**: 오늘 올라온 KODEX·RISE 7월 월말 공지가 왼쪽 패널엔 NEW로 뜨는데 달력·운용사별 일정 표엔 `-`. **새로고침 누르면 나옴**.
- **원인**: 탭 진입(`showTab`)은 `loadDistributions()`=**캐시(비force)**, 새로고침 버튼만 `loadDistributions(true)`=**force**. 새 공지가 서버 캐시에 반영되기 전이면 force로 재파싱해야만 일정이 잡힘. (서버 캐시 staleness 자체는 백엔드 `Code.gs` 문제 → 재배포 필요, 이번엔 프론트로 우회.)
- **수정(프론트만, 재배포 X)**: `loadDistributions(force, silent)`로 확장. 탭 진입 시 ①캐시로 즉시 렌더 → ②세션 10분 throttle(`_lastDistForce`)로 **백그라운드 자동 force**(silent=스피너로 안 비우고 `⟳ 갱신 중` 배지만) → ③완료 시 달력·표 조용히 교체. 새로고침 버튼(force)도 `_lastDistForce` 갱신하므로 직후 진입은 재force 스킵.
- **검증(헤드리스)**: 진입→캐시 6콜(force 없음) → 백그라운드 force 6콜(`force=1`)+배지 [on,off], 10분 내 재진입은 캐시만(force 스킵). 콘솔 무에러.
- **다음 할 일**: 카톡 알림(각 운용사 새 공지 시 나에게 카톡 push) — 감지·발송 메커니즘 설계 논의 중(백엔드 Kakao API vs 스케줄 에이전트). 근본 캐시 staleness는 백엔드 과제로 잔존.

## 2026-07-28 (45) / 집 — 콜드스타트 체감 제거: localStorage 즉시 렌더 + 갱신 배지
- **접근**: 콜드스타트(서버 20~30s)를 없앨 순 없으니, 첫 화면이 서버를 **안 기다리게** 함. 지난 로딩 데이터를 `localStorage` 스냅샷(`jjk_snap_v1`)에 저장 → 다음 실행 때 **즉시 렌더** → 백그라운드로 최신 fetch → 조용히 교체.
- **구조 변경**: `loadAccountStats()`를 fetch 오케스트레이터 + 순수 렌더 `renderAccountStats(holdings,divs,sheet,divsFull)`로 분리(캐시/최신 양쪽에서 호출). `renderAccounts()`에서 `loadAccountStats()` 자동호출 제거 → 호출측(`loadAccounts` await / `moveAccount` / `paintFromCache`)이 명시적으로 실행. `init()` = `paintFromCache()`(있으면 즉시 렌더+배지 ON) → 기존 병렬 로딩 → 배지 OFF. 스냅샷 저장은 최신 `loadAccountStats` 끝에서.
- **UI**: 상단 중앙 `⟳ 최신 데이터로 갱신 중…` 배지(`showRefreshing`), 갱신 끝나면 페이드아웃. 최초 로딩(스냅샷 없음)엔 배지 안 뜸(기존 스피너 경로 유지).
- **검증(헤드리스)**: 스냅샷 seed + 2초 지연 api 스텁으로 `init()` 호출 → 100ms 시점에 이미 계좌 셀 채워짐(캐시가), 배지 opacity=1, 2초 후 평가금액이 최신값(11000→12000 반영)으로 교체·배지 opacity=0·스냅샷 재저장. 최초 로딩(캐시 없음)도 `paintedFirstLoad=false`로 정상 렌더+스냅샷 저장. 콘솔 무에러.
- **한계/주의**: 처음 뜨는 숫자는 '지난번 값'이라 살짝 stale(배지로 고지). 스냅샷에 sheet(현재가 전체)·divsFull 포함 → localStorage 용량은 수백 KB로 여유. **Code.gs 변경 없음(재배포 불필요)**.

## 2026-07-28 (44) / 집 — 분배금 "분배율 추이" 뷰 신설 + 월합계 상단 이동 + 콜드스타트 재진단
- **분배율 추이 뷰(②)**: 분배금 탭에 하위 토글 2개 추가 — `💰 금액`(기존 편집 그리드) / `📈 분배율 추이`(신규, 읽기 전용). `loadDivRateGrid()` 신설, 토글은 `divView`+`setDivView`+`reloadDivGrid()` 디스패처 경유(연도·계좌·글자 슬라이더 공유). 기존 `loadDividendGrid()` 호출부 5곳을 `reloadDivGrid()`로 교체.
- **셀 구성**: 각 월 셀 = 윗줄 금액, 아랫줄 `[증감]  [분배율%]`. 분배율 = `amtKrw/investKrw×100`(2자리, 롤오버 툴팁과 동일). 증감 = 이번달% − **전달(m-1)%**, 퍼센트포인트, `빨강(#dc2626)=상승 / 파랑(#2563eb)=하락`, |Δ|<0.005·1월·전달값없음이면 공백. 증감은 %값 **왼쪽**. 합계/실분배율/환산분배율 열은 금액 그리드와 동일.
- **월 합계 행 상단 이동(③)**: `<tbody>${rows}${totalRow}` → `${totalRow}${rows}`. 금액·분배율 두 그리드 모두 맨 위(월 헤더 바로 아래)로.
- **검증**: 로컬 http 서버 + stub 데이터로 `loadDivRateGrid` 헤드리스 확인 — 증감 색/값(+0.20 빨강, −0.30 파랑), 1월·전달빈칸 증감없음, USD 행 `$5.00`+`0.50%`, 합계행 top, 토글 read-only↔editable 전환·active 클래스 정상. 콘솔 무에러.
- **콜드스타트 재진단(①, 코드만·수정 안 함)**: 부팅 2파 — `init()`의 `[getExchangeRate·getAccounts·getStockList]` 병렬 → `renderAccounts` → `loadAccountStats()`의 `[getHoldings·getDividends(year)·getSheetData·getDividends(full)]` 병렬(**getDividends 중복 2회**). (42) 측정상 병목은 시트 아님(getSheetData 1~2s 캐시됨), **cold start**(getExchangeRate 31.8s)임. **핵심 가설**: 부팅의 병렬 요청이 Apps Script 인스턴스를 여러 개 띄워 keepWarm(단일 핑)이 못 데운 인스턴스가 cold → keepWarm이 부분적으로만 듣는 이유. **다음 할 일**: (a) 사용자: Apps Script 편집기 트리거 목록에 5분 keepWarm 트리거 설치됐는지 확인(`setupKeepWarm` 1회 실행). (b) 진짜 해법 = 프론트 localStorage 즉시렌더(캐시 렌더 후 백그라운드 갱신) — 승인 대기(라이브 금융앱이라 신중).

## 2026-07-28 (43) / 집 — keepWarm를 실제 액션 핑으로 개선 + PLUS 월중 누락은 캐시 타이밍(정상)
- **keepWarm 개선**: 빈 'ok' 핑은 컨테이너만 데우고 스크랩 경로는 cold라, warm ping 직후에도 실제 요청이 20초 걸리는 경우 있었음(실행기록 doGet 20.7s). `keepWarm`이 `_EXEC_URL + '?action=getExchangeRate'`를 치도록 변경 → 실제 경로까지 warm. **트리거는 Head 실행이라 재배포 불필요, 편집기에 붙여넣고 저장만** 하면 다음 5분부터 적용.
- **PLUS 월중 "사라짐"**: 실제 배포 페이지(jjk-dist) `_distData`·렌더 직접 확인 → PLUS **21종목(월중 4 + 월말 17) 정상 렌더**. 스샷의 17종목은 force 재파싱 중 캐시 중간상태를 잡은 것. 버그 아님, 새로고침이면 정상.
- **미해결/한계**: 무료 Apps Script cold start는 완전 제거 불가. keepWarm으로 빈도만 줄임. 진짜 체감개선은 프론트 localStorage 캐시(즉시 렌더 후 백그라운드 갱신)지만, getAccounts는 계좌 메타만 주고 평가금액은 holdings/dividends에서 계산되므로 캐시 즉시렌더는 holdings+dividends까지 캐시해야 함 → 다음 세션 후보(라이브 금융앱이라 신중히).
- **권한 팝업**: settings.local.json allow 확장은 **Claude Code 재시작 후 적용**(권한은 시작 시 1회 로드). 세션 중 편집은 현재 세션에 반영 안 됨.

## 2026-07-28 (42) / 집 — 앱 초기 로딩 느림 원인=Apps Script cold start → keep-warm 트리거 추가
- **증상**: 오랫만에 열면 로딩이 너무 느림.
- **측정(브라우저 UA로 API 시간재기)**: cold일 때 `getExchangeRate` **31.8초**, `getAccounts` **21.6초** → 곧바로 다시(warm) 재면 각각 **1.1초 / 1.7초**. `getSheetData`는 1~2초로 이미 빠름(2026-07-08 캐시 효과). 즉 병목은 시세시트가 아니라 **오래 안 써서 컨테이너가 잠든 cold start**. 게이트가 `getAccounts`(cold 21s)를 기다렸다 앱을 열어서 "비번 넣고 20초 멍" 구간이 생김.
- **수정**: `Code.gs`에 `keepWarm()`(자기 exec URL 무파라미터 핑=‘ok’ 반환, 시트/외부호출 0) + `setupKeepWarm()`(5분마다 트리거) 추가. 기존 트리거는 하루 특정시각뿐이라 사이에 잠들었음.
- ⚠️ **다음 할 일(수동)**: Code.gs 재배포 후 **편집기에서 `setupKeepWarm` 1회 실행(▶)** → 5분 트리거 설치. 이후 cold start 거의 사라짐(1~3초).
- **부가**: 권한 팝업('한번만 허용') 반복 → `.claude/settings.local.json` allow에 `Bash(*)`·`PowerShell(*)` 추가(임시 `$base=...` PS 스크립트가 기존 패턴에 안 걸려 매번 물었던 것). 파괴적 명령 deny(rm -rf/force push/reset --hard/Remove-Item -Recurse) 유지.

## 2026-07-28 (41) / 집 — PLUS 월말 분배 일정 누락 버그 수정 (OCR 금액 정규식 % 앵커)
- **증상**: PLUS "7월 월말배당" 공지가 왼쪽 패널엔 NEW로 떴는데 **달력·운용사별 일정 표엔 PLUS 월말 행이 공란**. 다른 운용사 월말은 아직 미공지라 정상.
- **진단(재현→근본)**:
  1. `getDistribution?source=plus`(+`force=1`) 결과에 `cycle:월중`만 있고 월말 항목 0건. 캐시 아님(force도 동일).
  2. PLUS 공지목록 직접 조회 → `(월말)` 글 존재(n=30823, 7.28). 파서 제목필터 `\(월말\)`엔 걸림.
  3. 월중(n=30822)·월말(n=30823) 상세 이미지 직접 다운로드·육안 비교 → **월중 이미지는 분배율에 `%`(1.27%), 월말 이미지는 `%` 없음(0.15, 헤더에만)**.
  4. `fetchDist_plus`의 OCR 금액 추출 `amtM = /(\d+)\s+\d+\.\d+\s*%/` 가 `%`를 **필수 앵커**로 씀 → 월말 22행 전부 `amount=null` → `continue` 스킵 → 월말 0건. 월중은 `%` 있어 통과.
  5. node로 월말 OCR 근사텍스트에 옛/새 정규식 대조: **옛 0건 vs 새 22건**(상단 상향노트 중복코드 오검출 없음).
- **수정**: `Code.gs` `fetchDist_plus`의 `rateM`/`amtM` 정규식 `%` → `%?`(선택). 일정은 세부내용표 요일붙은 날짜로 `parseScheduleFromOcr`가 자동 추출(분배락 7.30·기준 7.31·지급 8.4).
- ⚠️ **다음 할 일(수동)**: Apps Script 편집기에 `Code.gs` 붙여넣기 → **기존 배포 편집(새 버전) 재배포**. 그 후 분배금공지 탭 새로고침 → PLUS 월말 행/달력에 8.4 지급 등 표시 확인.
- 참고: PowerShell 기본 UA로 API GET 시 403(구글 봇차단) — 브라우저 UA면 200. **배포 접근권한 문제 아님**, 앱은 정상.

## 2026-07-28 (40) / 집 — 은경 저축 만기 알림 색상: 6개월 파랑→주황 (집 PC 버전과 병합 정리)
- **분기 사고 정리**: 집 PC에서 57dd50f 위에 저축 추가작업(만기공지·정렬·색상 등)을 커밋(32fb5cc)했는데, 직장 PC가 같은 57dd50f 위에 33개 커밋으로 저축을 더 완성(가입일 열·만기 이메일 알림·모바일수정 등)해 원격에 올림. **원격이 상위호환**이라 집 커밋은 버리고 원격 채택(사용자가 `git reset --hard origin/main` 직접 실행 — deny 규칙). 집 커밋은 `home-savings-32fb5cc` 브랜치에 백업.
- **색상만 조정**: 직장 PC 만기색(6개월 파랑#2563eb/3개월 #ea580c/1개월 #dc2626/지남 #16a34a) 중 **6개월을 주황#f59e0b로** 변경(사용자 요청: 6개월 주황/3개월 진주황/1개월 빨강). `svMatInfo` 6개월 color·tint + 범례 span 색 두 곳. 3개월(#ea580c 진주황) 틴트도 rgba(234,88,12,.16)로 색과 일치시킴. 1개월·지남 유지.
- 검증: 로컬 — 공지 항목·범례 색 실측(1개월 빨강220,38,38 / 3개월 진주황234,88,12 / 6개월 주황245,158,11 / 지남 녹색), 콘솔 무에러. 프론트만 — 재배포 불필요.
- 참고: 집 커밋에 있던 날짜 오름차순 정렬·계좌명 가운데정렬·글자 확대는 원격 버전엔 미반영(원격은 ▲▼ 순서이동·글자 축소 채택). 필요하면 `home-savings-32fb5cc`에서 개별 이식 가능.

## 2026-07-21 (37) / — 연금플랜에 "목돈 출금 가능액" 뷰 추가 (프론트만, 재배포 불필요)
- **배경/구분**: 기존 연금플랜은 *매달 들어오는 분배금(현금흐름)*만 봄. 퇴직(2029-03, 52세) 후 *계좌를 헐어 뽑는 목돈*이 총 얼마인지(세후) 확정하는 뷰가 없었음. 배당세(9.9/15.4 등)는 매년 원천징수라 **원금 인출엔 추가세 대부분 0** → 목돈 뷰 세율은 배당세와 분리.
- **신규 카드 `💵 목돈 출금 가능액`** (재남/은경 컬럼 아래, `#pp-funds`): 자동행 4개(연금저축 안세공·ISA·일반계좌 = 퇴직연도부터 / IRP 세공 = 55세부터 16.5%) + 수동 자금원 행(퇴직금·사학공제 저축·예금 등 ➕추가·✕삭제·직접입력). 요약 3카드: 즉시 가용/이후 추가(55세~)/총 가용 — 전부 세후.
- **데이터 모델**(`ppDefaultCfg` + 하위호환 기본값 주입): `birthYear:1977`(나이·IRP 55세 계산), `fundTax:{ISA:0,일반:0,연금저축:0,IRP:16.5}`(유형별 목돈 인출 세율, 수정가능), `funds:[]`(수동 자금원). `loadPensionPlan`에서 `ppAuto.byType`(계좌유형별 평가액 합계) 계산.
- **세공/안세공 확정**: 세공=우체국연금·미래에셋 IRP / 안세공=연금저축계좌 전체(세액공제 한도 밖, 원금 무세금 인출). 세금 정밀도는 **간단 모드**(안세공·ISA·일반=0%, IRP만 55세전 16.5%) — 사용자가 세율 직접 수정 가능.
- 검증: 로컬(게이트 우회 mock) — 즉시 180,000,000(ISA50+일반30+연저20+퇴직금80) / 55세후 IRP 10M×83.5%=8,350,000 / 총 188,350,000, 나이 52·55세 표기, 파싱·콘솔 무에러. 프론트만 — **Code.gs 재배포 불필요.**
- **⚠️ 다음 할 일**: (1) 은경 사학연금 보류. (2) 예금 자료 확보 시 수동 자금원으로 입력. (3) 사학 퇴직연금 개시연령 2041(64세) 가정 — 실제 개시연령 확인 후 `o-sahak-pen` startYear 수정. (4) 2029 실퇴직 시 공식 예상액이 현재값보다 소폭 상향 — 재고지서 나오면 갱신.

## 2026-07-21 (39) / — 사학 퇴직연금 개시연령 2041→2040 교정 (96년이후 임용 정상개시 2040/07·63세)
- 사용자 제공 "연금지급개시연령비교" 스샷: (4)년도별 퇴직연령(96년이후임용) = **2040/07**. 앞서 가정한 2041(64세)이 틀림 → 정상 개시 **2040(63세)**.
- 시드 리터럴 `o-sahak-pen` startYear 2041→2040, + `loadPensionPlan` 상단에 1줄 마이그레이션(기존 저장분 2041이면 2040으로 교정, 두 PC 자동 반영).
- 참고 조기퇴직연금(감액·평생): 2035/58세 75% · 2036/59세 80% · 2037/60세 85% · 2038/61세 90%. 조기수령 비교는 요청 시 추가.
- 검증: 로컬 mock — 기존분 2041→2040 마이그레이션, 신규 시드 2040, ppCalcAt 2039=0·2040=2,502,710, 콘솔 무에러. 프론트만 — **재배포 불필요.**

## 2026-07-21 (38) / — 사학연금(재남) 공식 예상액 시드 (퇴직연금 방식 선택)
- 사용자가 사학연금공단 **퇴직급여 예상액** 스샷 제공(총재직 350개월, 평균기준소득월액 6,138,601). 3가지 방식 중 **② 퇴직연금** 선택.
- **1회 시드**(`loadPensionPlan`, `ppCfg.sahakSeeded` 플래그·삭제 시 재생성 안 함 — 계좌행 시드와 동일 철학): (a) 목돈 자금원 `사학연금 퇴직수당` 70,582,690원(2029·실수령·세율0) → 💵 목돈 카드, (b) 기타 연금 `사학 퇴직연금` 월 2,502,710원(실수령), 개시 **2041(64세)**.
- 전부 **실수령액**(소득세·지방세 이미 차감)이라 세율 0. 참고 다른 방식: ①퇴직일시금 실수령 282,258,540 / ③공제일시금 목돈 207,526,560+월 861,940.
- 검증: 로컬 mock — 시드 중복방지(sahakSeeded), 목돈카드에 퇴직수당 70,582,690 표시, 월연금 ppCalcAt 2040=0·2041=2,502,710(64세 개시), 콘솔 무에러. 프론트만 — **Code.gs 재배포 불필요.**

## 2026-07-21 (36) — 분배금 스프레드시트 동기화를 연도별 탭 지원으로 확장 ⚠️Code.gs 재배포 + 시트에 분배금2025 탭 필요
- **배경**: 배당 이력이 앱에 2026년치(1~7월)밖에 없어 "원금 대비 총이득(=평가손익+누적배당)" 계산이 반쪽. 2025년 데이터를 하나씩 입력하기 힘드니 기존 '분배금 스프레드시트 동기화'로 채우려 함.
- **문제(근본원인)**: `getDivSheetData`가 **연도 무관하게 항상 '분배금' 탭 하나만** 읽음(현재 그 탭엔 2026 데이터). 그래서 앱 연도만 2025로 바꿔 동기화하면 2026 숫자가 2025 칸에 복사돼 오염됨.
- **수정**: `getDivSheetData(year)`로 확장 — `분배금<year>` 탭이 있으면 그걸, 없으면 기존 '분배금' 탭 폴백(`Code.gs`). 프론트 `syncDividendsFromSheet`가 divYear를 API에 전달(`portfolio.html`). 2026은 폴백이라 기존과 동일 동작.
- ⚠️ **필요 조치**: ①시트(19UsD0…)에 **`분배금2025` 탭** 생성 — '분배금' 탭을 복사해 계좌(A)·티커(B)·월(G~R열) **컬럼 위치 그대로** 두고 값만 2025로 교체(라벨/컬럼 어긋나면 매핑 실패). ②Code.gs 붙여넣기+재배포. 검증: `?action=getDivSheetData&year=2025`가 2025 값 반환. ③앱 분배금 탭 → 연도 2025 → 🔄 동기화 → 적용.
- **다음 할 일**: 2025(및 그 이전) 배당 다 채운 뒤 대시보드에 '총이득(배당포함)'·'실납입 추정 수익률' 카드 추가 검토. 실납입 정확도 원하면 계좌별 입금액 기록칸(방법 B) 신설.

## 2026-07-21 (35) — 앱 '내 보유 종목 분배 내역'에서 6월 월말분 노출 제거 + SOL 코드 채움 ⚠️Code.gs 재배포 필요
- **증상**: ①앱 하단 '내 보유 종목 분배 내역'이 7월인데 6월 월말분(기준일 6/30, 7월 초 지급)을 계속 보여줌. ②공지페이지(스샷2) SOL 종목 코드가 다수 빈칸.
- **①근본원인(프론트, `portfolio.html` renderDistGrid만)**: 이 섹션 필터가 공지페이지(dist_notice)와 달리 **전월~익월 허용 + `if(it.cycle) return true`로 월 검사 자체를 우회**해서 cycle 태그 달린 월말분(KODEX·SOL 등, 기준일 6/30)이 7월에 그대로 떴다. 공지페이지 필터는 `monthOf(기준일)===선택월`이라 이미 정상. → 앱 필터도 **`monthOf(기준일‖지급일)===curMonth`로 통일**. `monthOf` 헬퍼 추가(‘7월 15일’·TIGER ‘7/15’ 둘 다 파싱). 라이브 API 6개 운용사 실측으로 확인: 월중=기준일 7/15(유지), 월말=기준일 6/30(제외). **프론트만이라 재배포·jjk-dist 복사 불필요.**
- **②SOL 코드(백엔드, `Code.gs` `SOL_TICKER`)**: 블로그 공지엔 코드가 없어 이름→코드 수동 맵으로 표시하는데 6종목만 등록돼 나머지 빈칸. Naver ETF 목록(euc-kr JSON)에서 현재 공지 SOL 전 종목 코드 확보해 **12종목 추가**(코리아고배당 0105E0, 미국배당미국채혼합50 490490, 미국S&P500미국채혼합50 0080X0, CD금리&머니마켓 497880, 국제금커버드콜 0022T0, 200타겟위클리 0167B0, 코스피200채권혼합50 0192S0, 미국500타겟데일리 494210, 미국S&P500 433330, 미국S&P500엔화노출(H) 499150, 배당성향탑픽 0152E0, 미국배당다우존스(H) 452360). 코드는 백엔드 `it.ticker`라 재배포하면 앱·공지페이지 양쪽 다 채워짐.
- ⚠️ **Code.gs 편집기에 붙여넣기 + 재배포 필요**(②용). 검증: `?action=getDistribution&source=sol`의 items에서 위 종목 ticker가 채워지면 성공. ①은 이미 반영(파일 열면 적용).
- **추가(프론트, `portfolio.html`만)**: 위 트레이드오프 대응으로 앱 '내 보유 종목 분배 내역'에도 **←/→ 월 이동 화살표** 추가(공지페이지 방식과 동일). `_distGridMonthOffset`(±1)·`distGridShiftMonth`, 제목 `N월 내 보유 종목 분배 내역`으로 동적 표기. renderDistGrid가 offset 반영한 `curMonth`로 필터. 재배포·jjk-dist 불필요.

## 2026-07-21 (34) — 분배금공지 "가끔 일부 종목 안뜸" 원인 규명 + 빈 운용사 1회 재시도 (프론트만)
- **증상**: 공지페이지/앱 분배금공지 열 때마다 **가끔 일부 운용사 종목이 통째로 안 뜸**, 새로고침하면 정상.
- **근본원인**: `getDistribution`은 캐시 신선하면 즉시 응답하지만, 캐시 만료(TTL 2~6h)/회차 변경 직후엔 **라이브 스크레이핑**(KODEX·PLUS OCR, SOL 블로그). 이게 프론트 `loadDistributions`의 **25초 타임아웃**을 넘기면 그 운용사는 `items:[]`로 렌더 → 종목 사라짐. 재시도 없음. 서버 스크레이핑은 계속 돌아 캐시를 데워놓으므로 새로고침하면 나옴.
- **수정(`dist_notice.html`+`portfolio.html` 동일)**: `fetchOne(s, ms)`로 타임아웃 인자화. 첫 배치 후 **에러+빈 값으로 온 운용사만 45초 타임아웃으로 1회 재시도**. 첫 호출이 이미 서버 캐시를 데워놔서 재시도는 대개 즉시 채워짐. 정상 로드(빈 값 없음)·정당한 빈 값(에러 없음)·force 로드엔 재시도 안 돎 → 평소 속도 영향 없음.
- **검증**: 프로젝트 폴더 밖 파일이라 브라우저 정적 스냅샷으로만 열려 라이브 실행 불가 → 코드 리뷰로 검증(시그니처 양쪽 호출부 갱신, 재시도 가드 조건, allSettled 항상 fulfilled로 `.data` 안전, 렌더 1회). 배포는 프론트만이라 **Code.gs 재배포 불필요**. `/exec` SOL은 이미 `_source:blog`/공시 7·10/18건 정상 확인.
- ⚠️ **jjk-dist 복사·푸시 완료**(dist_notice.html → jjk-dist/index.html).
- ※ 다음 관찰거리: 재시도해도 계속 빈 운용사가 있으면 45초로도 콜드 OCR이 안 끝나는 것 → 트리거로 캐시 상시 워밍 고려.

## 2026-07-20 (33) — /exec 배포 하나로 통합 + SOL 공지를 블로그로 연결 ⚠️붙여넣기+재배포 필요
- **사고 기록**: (31)(32) 작업 후 "재배포 필요"만 반복해 안내한 탓에 사용자가 **Code.gs 붙여넣기 없이 재배포만** 여러 번 함 → 옛 코드가 계속 재배포돼 "고쳤는데 왜 안 되냐"로 한참 헤맴. **앞으로 반드시 "① 편집기에 붙여넣기 + ② 재배포"를 한 쌍으로 안내할 것.** (메모리 `redeploy-just-tell` 갱신함)
- **배포 2개 문제**: 앱(`AKfycbwJS1Fd…`)만 갱신되고 공지페이지(`AKfycbwdk4…`)는 옛 코드로 남아 같은 SOL이 앱=blog/18건, 공지페이지=api/6건으로 어긋남. → **`dist_notice.html`의 `API` 상수를 앱 URL로 교체해 배포를 하나로 합침.** 앞으로 재배포 1회로 두 화면이 같이 갱신된다. (`AKfycbwdk4…` 배포는 이제 안 씀)
- **SOL 공지 패널이 비어 있던 진짜 이유**: 공지 패널은 `getDistribution`이 아니라 **`getEtfNotices`**를 쓰는데, 여기 SOL 분기만 아직 홈페이지 공지 API(`/api/cs/notice?keyword=분배금`)를 보고 있었다. 홈페이지엔 7월 중순 공지가 없으니 `-`. → `_solNotices()`(블로그 RSS)로 교체하고 **url을 `blog.naver.com/soletf/{logNo}`로** 바꿔 클릭 시 블로그 글이 열리게 함(사용자 요청).
- **검증**: ①URL 교체 후 로컬 재구동 — 일정표 SOL 공시 `6/10 → 7/10`, 달력 7/10에 SOL 칩 추가, 공지 종목 SOL 4종목(코리아고배당·미국배당미국채혼합50·미국배당다우존스2호·미국S&P500미국채혼합50) 정상. ②새 `getEtfNotices` sol 분기를 노드 실측 — 5건 수신, 프론트 `normalizeNoticeTitle` 재현 결과 `7월 월중배당 / 2026.07.10 / blog.naver.com/soletf/224342678476`. 제목 분류(`중순`→월중, `②`→월말)는 기존 정규식이 블로그 제목에도 그대로 맞아 프론트 수정 불필요.
- ⚠️ **Code.gs 붙여넣기 + 재배포 필요**(앱 배포 하나만). 그래야 공지 패널 SOL 7월 월중이 채워지고 링크가 블로그로 간다.

## 2026-07-20 (32) — SOL 분배금을 블로그 본문에서 직접 파싱 (OCR 불필요) ⚠️재배포 필요
- **핵심 발견**: SOL 블로그 공지 **본문이 HTML 텍스트 표**다. 이미지가 아니다. 일정(매수기준일·분배락·지급기준일·지급예정일)과 **종목별 분배금·분배율이 전부 텍스트**로 들어있다. OCR 붙일 필요 없었음.
- **그래서 dividend API를 버리고 블로그 본문을 1차 소스로 삼음.** API는 펀드 6개만 하드코딩돼 있었는데 블로그엔 회차별 전 종목(월말 14개)이 다 나온다. `FUND_MAP` 삭제, 이름→코드 표시용 `SOL_TICKER`만 남김(없는 종목은 코드 빈칸).
- **신설**: `_solParsePost(logNo)` — `blog.naver.com/PostView.naver?...&widgetTypeCall=true`를 받아 `se-main-container` 이후를 태그 제거·엔티티 디코드 후 정규식 파싱. `_solNotices()`에 `logNo` 추가(RSS link, 폴백은 CONTENT의 블로그 링크에서 추출).
- **파싱 요령**: 종목명에 숫자가 들어간다(`SOL 미국S&P500`, `SOL 200타겟위클리커버드콜`, `SOL 코스피200채권혼합50`). 그래서 이름을 `.+?`로 두고 **끝의 분배율(소수점)로 행을 끊는다** — `\d{1,2}\s+(SOL\s.+?)\s+([\d,]+)\s+(\d{1,3}\.\d{1,2})`. 소수점 강제가 백트래킹을 잡아줘서 `SOL 미국S&P500 24 0.10`이 정확히 갈린다.
- **프론트 SOL 하드코딩 전부 제거**(양쪽 파일): 달력 skip + `SOL?` 칩 블록, `isSol`→일정표 `-`, 종목 목록 강제 비움, "확정 공지 없음" 문구. (24)에서 "SOL은 공지를 안 한다"고 넣었던 건데 **전제가 틀렸으므로 근거가 사라짐.**
- **검증**: ①파서 단위 — 저장한 실제 글 2건에 assert(일정 4개 키, 월중 4종목·월말 14종목, 숫자 들어간 종목명 3케이스) 전부 통과. ②`fetchDist_sol` 전체를 노드에서 실제 네트워크로 실행 — `_source:'blog'`, 공시 7/10·분배락 7/14·기준 7/15·지급 7/16, items 18건(월중 4+월말 14). ③프론트 로컬 구동(:8791) — 콘솔 무에러, SOL이 달력·일정표·종목목록에 정상 출현.
- **월/회차 귀속은 저절로 맞는다**: 월말 14종목은 기준일이 6/30이라 프론트의 "기준일 월 === 선택 월" 필터에서 7월엔 자동 제외된다. 7월 화면엔 월중 4종목만. 25일쯤 월말② 공지가 뜨면 그때 14종목이 7월로 들어온다.
- ⚠️ **Code.gs 재배포 필요.** 검증: `?action=getDistribution&source=sol&force=1`에 `_source:'blog'`, `schedule.공시일 = '7월 10일'`, items 18건.
- ※ 달력 `addMark`의 `uncertain` 플래그는 이제 아무도 true로 안 넣지만 인자만 남겨둠(제거하면 호출부 여러 곳 건드려야 해서). 다음에 정리할 것.
- ※ `.claude/launch.json` 추가(python http.server 8777) — 로컬 확인용.

## 2026-07-20 (31) — SOL 공지: 네이버 블로그 RSS로 전환 + 자동 탐지 ⚠️재배포 필요
- **발견**: SOL은 공지를 **안 하는 게 아니라 네이버 블로그에 먼저 올린다.** 홈페이지 공지 API(`/api/cs/notice`)는 뒤늦게 미러링하거나 아예 누락 — 7/20 현재 홈페이지 최신은 6/25 "6월 분배금 안내 ②"뿐인데, 블로그엔 **7/10에 "7월 중순 분배금 안내 ① (지급 예정일 7월 16일)"**이 이미 올라와 있었음. 회차 (24)에서 "SOL 확정 공지 없음"으로 결론 낸 건 **홈페이지만 봐서 생긴 오판**.
- **수정(Code.gs)**: `_solNotices()` 신설 — `https://rss.blog.naver.com/soletf.xml` 파싱해 `[{title, date}]` 반환, 실패 시 홈페이지 공지 API로 폴백. `fetchDist_sol`의 공시일·`_월말지급예정` 추출을 이 목록 기준으로 변경(정규식·schedule 키는 기존 그대로라 프론트 무변경).
- **자동 탐지**: `refreshAllDistributions`(트리거용)가 `checkAndLogAlerts()`를 호출하도록 변경. 얘가 6개 운용사를 force 갱신하면서 **공시일이 직전과 달라지면 '신규공지' 알림**을 `알림로그` 시트에 남긴다 → SOL 새 글이 뜨면 자동 감지.
- **탐지 주기(사용자 결정)**: SOL 게시일이 고정이 아니라 **중순① 10일·월말② 25일 전후 ±2일**(8~12, 23~27)에 **매일 10·14·18시** 3회. `checkDistNotices()`가 `_inNoticeWindow(day)`로 걸러 창 밖이면 즉시 리턴, `setupDistTriggers()`가 트리거 3개 생성. 창 밖 날짜엔 안 돌아 GAS 쿼터 절약.
- **18시 슬롯 이유**: 실제 게시가 **17시 전후**(7/10 17:04, 6/25 17:01)라 10·14시만으론 당일에 못 잡고 다음날로 밀린다. 18시를 넣어 게시 당일 저녁에 감지.
- **관례 신설(사용자 요청)**: 손으로 실행해야 하는 함수는 편집기 함수 드롭다운에서 찾기 힘드니 **`Code.gs` 맨 아래 `===== 수동 실행 =====` 배너 밑에 모은다.** `setupDistTriggers`·`setupPortfolioTriggers`·`_testNoticeWindow` 이동, 원래 자리엔 한 줄 포인터만 남김. CLAUDE.md에도 기재.
- **검증**: RSS 로컬 파싱 — item 50건, 중순① `7월 10일`(→공시일), 월말② `지급 예정일 7월 1일` 정상 추출.
- ⚠️ **Code.gs 재배포 + `setupDistTriggers()` 편집기에서 1회 실행 필요.** 재배포 후 `?action=getDistribution&source=sol&force=1` 응답 `schedule.공시일`이 `7월 10일`이면 성공. 프론트는 안 건드렸으므로 jjk-dist 복사 불필요.
- ※ 남은 것: 블로그 본문의 **종목별 좌당 금액은 이미지**라 못 긁음 → 금액은 계속 SOL dividend API 실값 사용. 달력의 `SOL?` 처리도 그대로 두었음(공시일은 잡히지만 종목별 회차 확정 근거는 여전히 API값뿐).

## 2026-07-20 (30) — 동기화가 방금 고친 시트 값을 못 보던 문제 ⚠️재배포 필요
- **증상**: 종목관리 → 스프레드시트 동기화에서 `494300`(재남 ISA)만 차이 인식 실패. 시트 1340주/10,218 vs 앱 1315주/10,535.
- **원인**: `getSheetData`의 **3분 스크립트 캐시**(`sheetData_v1`). 시트를 고치고 3분 안에 동기화를 누르면 수정 전 값이 그대로 와서 diff가 안 잡힌다. 파싱·매핑·계좌명 문제 아님.
- **재현/확인**: `/exec?action=getSheetData` 첫 호출 1315/10535 → 190초 대기 후 재호출 **1340/10218**. 캐시 만료로 값이 바뀌는 걸 확인.
- **수정**: `getSheetData(force)` — `force`면 캐시 읽기 skip(쓰기는 유지). `doGet`이 `e.parameter.force==='1'` 전달. `portfolio.html`의 `syncFromSheet()`만 `force:1`로 호출 → 로드/탭전환 등 나머지 경로는 캐시 그대로 써서 느려지지 않음.
- ⚠️ **Code.gs 재배포 필요.** 그 전까지는 force가 무시되어 기존과 동일(3분 기다렸다 누르면 정상 인식).
- PLUS 분배금공지: 코드 문제 아니었음. (29) 수정은 이미 배포돼 정상 동작했고, **분배캐시에 남은 6월 실패 결과**가 나가던 것. `force=1` 1회로 7월(공시 7/10·분배락 7/14·기준 7/15·지급 7/20, 16종목, `_usedOcr:true`) 갱신 완료. → 후속 후보: `_plusErr` 있으면 캐시 저장 skip.
- `setupPortfolioTriggers()`는 **아직 미실행** — 수익로그가 여전히 slot 16 하루 1회. 편집기에서 1회 ▶실행 필요(재배포 아님).

## 2026-07-20 (29) — PLUS 재시도를 목록 fetch까지 확대 ⚠️재배포 필요
- 버전 103 배포 확인: `getPortfolioLog`에 `slot` 정상 반환(하루 3회 스냅샷 기능 라이브).
- PLUS는 신코드가 돌지만 **타임아웃 지점이 상세→목록으로 이동**: `_plusErr: 시간 초과: .../customer/notice/list`. (28)에서 재시도를 상세에만 넣은 게 빈틈이었음.
- **수정**: `fetchDetailHtml` → `fetchPlus(url)` 범용 헬퍼로 교체하고 **목록·상세 양쪽에 3회 재시도** 적용.
- ⚠️ Code.gs 재배포 필요(배포 관리 → 기존 배포 편집 → 버전 `새 버전`).
- 📌 **배포 교훈**: 편집기 `▶실행`은 HEAD를 돌리므로 함수 실행 성공 ≠ 웹앱 반영. `/exec`는 배포에 고정된 버전을 서빙한다. 반영 판정은 반드시 `/exec` 응답으로 할 것. 활성 배포가 6개쯤 있으니 URL(`AKfycbwJS1Fd…`)로 대상을 특정할 것.

## 2026-07-20 (28) — 수익로그 하루 3회(10·13·16시) 스냅샷 + 날짜 하루밀림 버그 수정 ⚠️재배포 필요
- **사양(사용자 결정)**: 하루 3회 잡되 **그래프는 하루 1점 유지**. 10·13시엔 오늘 점이 갱신만 되고, **16시 값이 그날 확정**. x축 밀도는 그대로.
- **Code.gs**: `수익로그`에 `slot`(4번째 열) 추가. `snapshotPortfolio()`가 KST 실행시각으로 슬롯 판정(`<12→10`, `<15→13`, else `16`)하고 **같은 날짜+같은 슬롯만 교체**(다른 슬롯 보존). slot 빈 과거 행은 확정(16)으로 간주 → 기존 데이터 그대로 호환. `getPortfolioLog`가 slot 반환. `setupPortfolioTriggers()` 추가(기존 snapshotPortfolio 트리거 삭제 후 10·13·16시 3개 생성, 편집기에서 1회 실행).
- **portfolio.html**: 같은 날짜에선 **더 늦은 슬롯이 이김**. 16시 확정 전이면 마지막 점을 **속 빈 원**으로 그리고 총합계 헤더에 "N일 HH:00 기준 · 16:00 확정 전" 안내. '오후 6시 자동저장' 문구도 갱신.
- 🐛 **기존 버그 수정**: `toISO`가 `toISOString()`(UTC)을 써서 **KST 자정 날짜가 전부 하루씩 앞으로 밀렸음**(마지막 데이터가 7/17인데 x축 끝이 16). 로컬 연/월/일 조합으로 교체 → 이제 17로 정상. 차트의 모든 날짜 라벨이 하루 이동하니 참고.
- **검증**: 로컬 실데이터 — slot 없는 레거시 598건은 실선 점 7개·안내 없음(회귀 없음), x축 끝 `17`로 실제 최신일과 일치. slot=13 주입 시 속빈점 7개 + "17일 13:00 기준 · 16:00 확정 전". 콘솔 무에러.
- ⚠️ **Code.gs 수동 재배포 + `setupPortfolioTriggers()` 1회 실행 필요.** 그 전까지는 프론트만 반영되어 기존과 동일하게 보임(전부 확정 취급).

## 2026-07-20 (27) — PLUS 파서 수정: 상세 fetch 타임아웃에 전체가 폐기되던 문제 ⚠️재배포 필요
- **원인 확정**: API 응답의 `_plusErr` = `"PLUS: Exception: Timeout: .../notice/detail?n=30822"`. PLUS 사이트는 정상(curl 0.18s, 200)인데 **Apps Script에서 간헐적 Timeout**. `fetchDist_plus`가 통째로 예외로 죽고 → smarttoday 뉴스 폴백(6월 기사)으로 밀려 **7월 화면에서 PLUS가 달력·일정표·그리드에서 전부 사라짐**.
- **파싱 로직 자체는 정상**이었음: 목록에서 n=30822 = "PLUS ETF 7월 분배금 안내(월중)"(2026.07.10)를 정확히 선택. 7월 월말 공지는 아직 미게시라 endE는 6월(n=30821)이 맞음. 상세페이지 `<table>`은 티커가 `A415920`·값 `-`인 무관한 표라 건너뛰는 게 정상이고, 실제 데이터는 이미지 → **OCR 경로**로 처리됨(성공 시 `_ocr:true`로 공시 7/10·기준 7/15·지급 7/20 정상 산출 확인).
- **수정 2곳**: ①`fetchDetailHtml(n)` 추가 — 상세 fetch 3회 재시도(1s·2s 백오프). ②`parsePlusNotice(midE)`/`(endE)` 호출을 각각 try/catch로 **회차별 격리** → 한쪽이 죽어도 나머지 회차는 살림.
- ⚠️ **Code.gs는 수동 재배포해야 반영됨.** 재배포 후 검증: `?action=getDistribution&source=plus&force=1` 응답에 `_source`가 뉴스 폴백이 아니고 `schedule`이 7월(기준 7/15·지급 7/20), `_usedOcr:true`, `_plusErr` 없음이면 성공.

## 2026-07-20 (26) — 달력에 한국 공휴일 표시
- 달력 날짜 옆에 공휴일명을 빨간 글씨로 표기(`HOLIDAYS` 상수, 키 `YYYY-M-D`). 공휴일이면 날짜도 빨강+굵게. `dist_notice.html`·`portfolio.html` 양쪽.
- 셀에 `y`(연도) 추가해 연말연시에도 연도별로 정확히 조회되게 함(기존 셀은 m/d만 들고 있었음).
- **데이터 출처**: nager.date KR 2026·2027 + 웹 검증. ⚠️ **제헌절은 2026년부터 공휴일로 재지정됨**(국무회의 4/28 의결, 2026-07-17 금). 2008년 이후 공휴일이 아니라는 예전 상식으로 빼면 안 됨. 근로자의날(증시 휴장)도 포함. 대체공휴일은 "(대체)" 표기.
- 달력은 평일만 그리므로 주말 공휴일(6/6 현충일 등)은 표시되지 않고, 대체공휴일만 보인다 — 의도된 동작.
- **검증**: 로컬 두 파일 — 7/17 제헌절, +3주 8/17 광복절(대체), +7주 9/24·9/25 추석, +20주 12/25 성탄절 정상. 콘솔 무에러.
- ※ portfolio.html 편집 중 `HOLIDAYS` 객체가 SCHED_CFG의 `];`로 닫혀 SyntaxError 냈다가 `};`로 수정함. 큰 파일에 상수 삽입 시 닫는 괄호 종류 확인할 것.
- ※ `prevBizDay`(분배락일 계산)는 여전히 **주말만** 제외하고 공휴일 미반영. HOLIDAYS가 생겼으니 반영 가능하나, 표시되는 분배락 날짜가 바뀌는 변경이라 사용자 확인 후 진행 예정.

## 2026-07-16 (25) — 공지 종목 전체: 월 이동(←/→) + 선택 월만 표시, SOL 종목 숨김
- **문제**: 하단 '공지 종목 전체' 필터가 전월·당월·익월을 다 통과시켜 **6·7월 회차가 한 카드에 섞임**. SOL은 확정 공지가 없는데 종목이 그대로 나옴.
- **수정(dist_notice.html만)**: 제목에 월 라벨+←/→ 추가(`_gridMonthOffset`, `gridShiftMonth`, ±1) → "📋 7월 공지 종목 전체". 필터를 **기준일(없으면 지급일)의 월 === 선택 월**로 변경. `monthOf`가 "7월 15일"·"7/15" 두 형식 모두 파싱(기존 `/(\d+)월/`만 봐서 TIGER 슬래시 형식이 필터를 그냥 통과하던 버그도 해소). SOL 카드는 종목 비우고 "확정 공지 없음".
- **검증**: 로컬 실API — 7월 KODEX18/TIGER9/ACE4/RISE6/SOL0, 6월 27/24/15/20/0. 합이 45·33·19로 **기존 혼합 목록과 정확히 일치(누락 없이 월 분리)**. 제목 토글 정상, 콘솔 무에러.
- ⚠️ **별건 발견(백엔드)**: PLUS가 세션 중 7월(OCR 성공)→**6월 회차로 회귀**. `force=1` 재파싱해도 6월(기준 6/15·지급 6/17, 종목별 sched 없음). 그래서 현재 7월 달력·일정표·그리드에서 **PLUS가 통째로 빠짐**. 프론트 문제 아님 — `fetchDist_plus` OCR/파서가 7월 공지를 못 잡는 상태. 조치 필요.
- 앱(`portfolio.html`)의 `distGrid`는 '내 보유 종목 분배 내역'으로 목적이 달라 미변경.

## 2026-07-16 (24) — SOL 확정공지 없음: 달력 '?'·일정 '-' 처리
- **배경**: SOL은 분배금 '공지(안내)'를 안 올리고 자사 배당 API 실값만 있음. 6개 펀드 중 5개는 지난달 회차(기준 6/30·지급 7/1)만, 2호(월중)만 7/15·7/16. 공시일 6/10은 6월 중순안내 게시일이 7월 행에 붙은 것, 분배락은 우리 코드 계산값 → **이번 회차 확정 근거 없음**. 그대로 두면 "미공지인데 날짜 확정된 것처럼" 보여 신뢰도↓.
- **수정(사용자 결정)**: ①공지 패널 그대로 ②달력: SOL을 실제 날짜(7/16 등) 대신 **다른 운용사 공통 지급일**(해당 회차 지급 마크가 가장 많이 몰린 날: 월중 7/20·월말 7/2)에 `SOL?` 칩으로 얹음 ③운용사별 일정 표: SOL 날짜 셀 전부 `-`.
- **구현**: `renderMasterCalendar`에서 `c.id==='sol'` skip → 루프 뒤 SOL 회차별(월중/월말) 최다 지급일 날짜 계산해 `addMark(..., uncertain=true)`. 칩 렌더에 `uncertain`이면 이름 뒤 `?`. 일정표 `dateCell(...,isSol)`이면 `-`. `dist_notice.html`·`portfolio.html` 양쪽 미러.
- **검증**: 로컬(localhost:8777, 실제 API) — 두 파일 모두 7/20·7/2에 `SOL?`, 7/16엔 SOL 없음, 일정표 SOL 행 `- - - -`, 타 운용사 날짜 정상 유지, 콘솔 무에러. **프론트만 — Code.gs 재배포 불필요.** 공개페이지는 jjk-dist/index.html로 복사·푸시.

## 2026-07-15 (23) / 직장 — 만기 알림 공지판 top을 표 top과 정렬
- 공지판이 표보다 위에서 시작하던 원인: `.sv-topbar`(➕ 추가)가 `.sv-inner` 안(표 위)에 있어 표만 아래로 밀림. 토픽바를 `.sv-layout` **위(바깥)**로 이동 → 공지판·표가 같은 Y에서 시작(측정 noticeTop 198 vs tableTop 200, 2px=공지판 테두리). 버튼은 계속 우측 정렬.
- 검증: 로컬 — top 정렬 2px 이내, topbar가 layout보다 앞, 버튼 우측정렬, 콘솔 무에러. 프론트만 — 재배포 불필요.

## 2026-07-15 (22) / 직장 — 숫자 콤마 정렬: 입력칸에 tabular-nums 적용
- **증상**: 은경 저축 금액칸 등 오른쪽정렬 숫자의 콤마가 행마다 어긋남. **원인**: `--mono`가 실은 'Pretendard'(등폭 아님)이고, body의 `font-variant-numeric:tabular-nums`가 **`<input>`엔 상속 안 됨**(computed normal). 그래서 자릿수 폭 제각각("111"18.4px vs "444"26.2px) → 콤마 미정렬.
- **수정**: 전역 `input, select, textarea { font-feature-settings:"tnum"; font-variant-numeric:tabular-nums }` 한 줄. 저축·연금(pp-plain)·분배금 입력칸 전부 등폭 숫자로. 검증: 로컬 — 적용 후 "111"="444"=25.81px, "1,111,111"="4,444,444"=67.31px(콤마 정렬), 콘솔 무에러. 프론트만 — 재배포 불필요.
- ※ 검증 중 프리뷰 HTTP 캐시로 새 CSS가 안 잡혀 `?v=` 캐시버스터로 재확인함(코드 문제 아님).

## 2026-07-15 (21) / 직장 — 은경 저축: 모바일 가로밀림 수정 + "은경 저축" 제목 삭제
- **모바일 표 잘림/페이지 가로밀림 수정**: `.sv-layout`이 세로 스택될 때 `align-items:flex-start`라 표영역(.sv-main)이 표 너비(1400+px)만큼 부풀어 **페이지 전체가 가로로 밀림**(deScrollW 1471). 미디어쿼리(≤900px)에 `align-items:stretch` + `.sv-main{width:100%;min-width:0}` 추가 → 표는 카드 안(285px)에서 가로 스크롤(scrollWidth 1424), 페이지는 안 밀림(375=375).
- **제목 삭제**: 탭 상단 "💵 은경 저축" 큰 제목 div 제거(탭 버튼 라벨은 유지).
- 검증: 로컬 375px — page 가로스크롤 없음·표 wrap 내부 가로스크롤 O / 1280px — row 레이아웃·공지 왼쪽·페이지 안밀림 / 제목 텍스트 제거 확인, 콘솔 무에러. 프론트만 — 재배포 불필요.

## 2026-07-15 (20) / 직장 — 은경 저축: 미입력 플레이스홀더 흐리게 + 메모 미저장 원인=미배포 확인
- **플레이스홀더 흐리게**: 날짜 미입력칸 "20261101" 안내글이 너무 진해 데이터로 오인됨 → `.sv-in::placeholder { color:var(--text3); opacity:.35; font-weight:400 }`. 검증: computed ::placeholder opacity 0.35.
- **메모 새로고침 시 사라짐 원인 규명**: 라이브 `getSavings` 반환 필드에 `memo` 없음(키가 created_at까지 10개) → **배포된 백엔드가 비고 컬럼(15번) 이전 버전**. Code.gs 소스엔 memo 처리 있음(getSavings r[10]·addSaving·updateSaving 11열). **⚠️ 메모 저장하려면 Code.gs 수동 재배포 필요**(15번 미배포 경고 그대로). 프론트는 정상.
- 커밋은 플레이스홀더 수정만. 메모는 재배포 대기.

## 2026-07-15 (19) / 직장 — 은경 저축: 표 왼쪽 만기 알림 공지판 신설
- 요청했던 "표 왼쪽 공지" = **만기 임박 알림판**. 각 계좌 만기일(`mat_date`)로 D-day 계산해 색으로 표기: 만기 지남=녹색(#16a34a), ≤1개월=빨강(#dc2626), ≤3개월=주황(#ea580c), ≤6개월=파랑(#2563eb). 6개월 초과·날짜 없음은 미표시. 긴급도(days 오름차순, 지난 것 먼저) 정렬.
- 문구: 미래는 "만기까지 N일 (약 M개월)"(1개월 이내는 일수만), 지난 건 "만기 지남 · N일 경과". 각 항목 색 좌측선+옅은 배경, 하단 범례.
- 레이아웃: `#sv-table`을 `.sv-layout`(flex)로 감싸 왼쪽 `.sv-notice`(240px, 2px 프레임) + 오른쪽 `.sv-main`(표, 기존 가로스크롤 유지). 900px 이하 세로 스택. 함수 `svMatInfo`/`svNoticeHTML`/`svRefreshNotice` 추가.
- 만기일·이름 편집 시 `svSet`에서 `svRefreshNotice()`로 공지판만 갱신(표 Tab 포커스 유지). 순수 프론트 계산 — 재배포 불필요.
- 검증: 로컬 — 4밴드 색/문구/정렬 정확, 6개월초과·무날짜 제외, 날짜 편집 시 공지 즉시 갱신·정렬삽입, 표 6행 유지, flex 레이아웃 240px, 콘솔 무에러.

## 2026-07-15 (18) / 직장 — 은경 저축: 이율 3자리·메모칸 확대·순서이동 행 강조
- **이율 표시 3자리 고정**: `svRateShow`가 `Number(v).toFixed(3)` — 4→4.000, 4.3→4.300, 2.205 그대로. 이율칸 폭 `.sv-in.rt` 48→64px(PEPER 2.205 잘림 해결).
- **메모칸 확대**: `.sv-in.memo` 폭 120→360px(3배), 글자 14→11px(-3pt).
- **순서 이동 행 추적**: ▲▼로 옮기면 데이터가 목적지 행으로 가는데 버튼·색이 다 같아 어느 행인지 놓침 → 옮겨간 행(`svLastMovedId`)을 렌더 후 강조(액센트 인셋 + 첫칸 3px 액센트 좌선 + 0.7s 플래시), `scrollIntoView`. 여러 번 올려도 계속 그 행이 강조돼 눈으로 추적. 추가/삭제 시 강조 해제.
- 표 폭이 메모 확대로 카드 초과 → `.sv-wrap overflow-x:auto`로 가로 스크롤(기존 설정).
- 검증: 로컬 — 이율 3.150/2.205/4.000, 이율칸 64·메모 360/11px, 2연속 이동해도 강조행이 이동 종목 따라감, 콘솔 무에러. 프론트만 — 재배포 불필요.
- **미완(대기)**: "표 왼쪽에 공지" 요청 — 넣을 내용/형태 불명확해 사용자에게 질문함.

## 2026-07-15 (17) / 직장 — 표 마지막 행 아래 테두리 복구 (전역 규칙 범위 축소)
- 전역 CSS `tr:last-child td { border-bottom:none; }`가 **모든 표** 마지막 행 바닥선을 지우고 있었음(연금플랜·보유종목·분배금 등) → `.sv-tbl tr:last-child td { … }`로 범위 축소. 은경 저축 표(자체 2px 프레임)만 규칙 유지, 나머지 표는 마지막 행에도 아래 테두리 살아남.
- 연금플랜 표의 테두리 없는 액션·빈 칸(inline `border:none`)은 그대로 유지 → 바닥선은 데이터 칸(계좌명~평가금액/개시년도)에만 생김(의도대로).
- 반복 지적 사항이라 **피드백 메모리(table-bottom-border) 저장**. 검증: 로컬 computed-style — ISA·연금계좌 표 마지막 행 데이터칸 border-bottom 1px solid, 액션칸 0px none, 콘솔 무에러. (스크린샷 서브시스템은 이 세션도 타임아웃 → 측정값으로 검증) 프론트만 — 재배포 불필요.

## 2026-07-15 (16) / 집 — 은경 저축 클릭 전체선택 + 추가버튼 표 우상단 이동
- **입력칸 클릭 시 전체선택**: 상수 `SV_SEL = 'onfocus="this.select()" onmouseup="return false"'`을 날짜·금액·이율 칸에 적용 — 클릭하면 값 전체 선택돼 바로 숫자 입력하면 덮어써짐(onmouseup return false로 클릭 후 해제 방지). 이름·비고는 부분수정 위해 제외.
- **추가 버튼 위치**: 카드 헤더에서 제거 → 표 바로 위 우측정렬 바(`.sv-topbar` flex justify-end)로 이동. 표+버튼을 `.sv-inner`(width:fit-content, margin auto)로 감싸 버튼 우측이 표 우측 끝과 정렬(간격 0px). 제목은 순수 중앙정렬만 남김.
- **표 폭 축소(넘침 해결)**: 글자↑+비고칸으로 표가 카드 폭 초과해 오른쪽 넘침 → 입력칸 폭 축소(이름110·금액94·이율48·날짜112·비고120). 표폭 약 1150으로 카드 안에 들어와 좌우 41px 중앙정렬, 버튼도 정상 우상단.
- 검증: 로컬 — onfocus 속성·focus시 10글자 전체선택·버튼우측=표우측(gap0)·버튼 표위·넘침없음·중앙정렬·콘솔 무에러. 프론트만 — 재배포 불필요.
- 참고: 백엔드에 사용자 실입력 12건 확인(SMART·JJ·유안와·금화·상상인·키움YES·웰컴·인천·OSB 등, 원금 수천만원대). 데이터 미변경.

## 2026-07-15 (15) / 집 — 은경 저축 글자↑·직각테두리·비고칸 추가
- **글자 +2px 전체**: 표 12→14, 그룹헤더 11→13, 컬럼헤더 10.5→12.5, 입력칸 12→14, ▲▼ 10→12, ✕ 12→14, 제목 18→20, 힌트 11→13. 입력칸 폭도 소폭 확대(이름118→128·금액92→104·이율46→54·날짜118→130).
- **직각 + 진한 외곽선**: `.sv-tbl` border-radius 12→0, 외곽 border 1px var(--border)→**2px var(--text)**(짙은색·테마안전). thead 하단·tfoot 상단도 2px var(--text)로 프레임. box-shadow 제거.
- **비고칸 추가(맨 오른쪽)**: 중도해지금액 뒤·액션 앞에 비고 컬럼(rowspan2 헤더, `.sv-in.memo` 좌측정렬 150px, 중립 구분선 `.bd-n` 2px var(--text2)). 컬럼수 9→10(본문·푸터·빈상태 colspan 갱신). memo가 svRow·svVals·svAdd blank·svMove 스왑·svSet(else 분기)에 모두 반영.
- **Code.gs 비고 컬럼**: 시트 11열에 비고 저장 — getSavingsSheet 헤더에 '비고', getSavings `memo:r[10]`, addSaving 11번째 값, updateSaving `getRange(i+1,11)`. created_at(10열)은 그대로. **⚠️ 비고 저장은 재배포 필요**(기존 시트는 헤더 라벨 미갱신이나 데이터엔 무관). 글자·테두리·비고 렌더는 프론트라 재배포 불필요.
- 검증: 로컬 computed-style — 헤더 '비고' 포함·본문/푸터 10칸·글자 14/13px·radius 0·외곽 2px 짙은색·비고 구분선 2px·memo 값 렌더·svSet 편집→updateSaving 페이로드에 memo 포함·콘솔 무에러. (memo 백엔드 persist는 재배포 후 실측 예정)

## 2026-07-15 (14) / 집 — 은경 저축 편의기능 5종 (Tab이동·날짜자동포맷·순서변경·메뉴맨끝)
- **Tab 옆칸 이동**: 편집 시 `renderSavings()` 전체 재렌더 제거가 핵심 — `svSet(id,k,el)`가 해당 칸만 재포맷(`el.value`)하고 합계는 `svUpdateTotals()`로 인플레이스 갱신(합계 셀에 `sv-sum-p/m/e` id). 재렌더 안 하니 네이티브 Tab이 그대로 옆 input으로 이동. ▲▼✕ 버튼은 `tabindex=-1`로 Tab에서 제외 → 데이터 칸끼리만 이동.
- **날짜 자동 포맷**: 날짜칸을 `type=date`→`type=text`(placeholder 20261101)로 교체. `svFmtDate`가 숫자만 추출해 8자리면 `YYYY-MM-DD`로 변환. 텍스트라 월/일 클릭 후 키보드 수정도 됨(수정 후 blur에 재포맷).
- **순서 변경 ▲▼**: `svMove(id,dir)` — **인접 두 행의 데이터를 맞바꿈(id·행위치 고정)** 방식이라 백엔드 `moveRows` 없이 기존 updateSaving 2번으로 처리 → **재배포 불필요**. 첫 행 ▲/끝 행 ▼ disabled. (created_at은 id에 묶여 유지되나 미표시라 무관.)
- **메뉴 맨 오른쪽**: 은경 저축 탭 버튼을 엑셀 뒤(제일 오른쪽)로 이동.
- 표 아래 안내문(날짜 8자리·Tab·▲▼) 추가. 검증: 로컬서버 — nav 마지막·날짜 20261101→2026-11-01(표시+백엔드 실측 persist)·편집후 동일 input 유지(Tab)·금액 재포맷+합계 인플레이스·▲로 순서변경+저장 페이로드 확인·첫끝행 버튼 disabled·콘솔 무에러. 프론트만 — **재배포 불필요.**
- ⚠️ 참고: 백엔드에 사용자 실입력 데이터 발견(`SMART` 원금 5,033만·만기 2026-11-01 + 빈 행 1) — 건드리지 않고 그대로 둠.

## 2026-07-15 (13) / 집 — 은경 저축 표 디자인 개편 (그룹 구분·컴팩트·중앙정렬)
- **3구역 그룹화 + 2단 헤더**: 계좌명 | **원금**(파랑) | **만기**(초록: 이율·날짜·금액) | **중도해지**(앰버: 이율·날짜·금액). 그룹 경계마다 3px 굵은 색 세로선(`.bd-b/.bd-g/.bd-a`), 각 구역 옅은 색 배경(body 5~7%, header 16~20%), 헤더 글자색도 구역별(파랑#2563eb/초록#0f766e/앰버#b45309). 색은 테마가 안 바꾸는 고정 시맨틱 색이라 전 테마 안전.
- **컴팩트·중앙정렬**: `width:fit-content;margin:0 auto`로 표를 내용 폭(약 930px)으로 줄이고 카드 중앙 배치(이전엔 카드폭 꽉 채워 늘어남). 입력칸 고정폭(이름118·금액92·이율46·날짜118), 숫자 오른쪽정렬이라 빈 공간 없음. 둥근 모서리12px+그림자. `.pp-plain` 대신 저축 전용 `.sv-in` 도입.
- **제목 가운데 정렬**: 카드 헤더를 position:relative 중앙정렬 제목(18px)+추가 버튼 우측 절대배치로 교체.
- 검증: 로컬 서버 computed-style 측정 — 그룹 테두리 색·배경·헤더색·2단 colspan·표너비 932(카드1232)·좌우여백 150=150 중앙정렬·셀 내용맞춤·콘솔 무에러 확인. (브라우저 스크린샷 서브시스템이 세션 내내 타임아웃이라 측정값으로 검증) 프론트만 — 재배포 불필요.

## 2026-07-15 (12) / 집 — 은경 저축 백엔드 재배포 완료 + CRUD 실측 검증
- Code.gs 재배포 완료(배포 관리에서 앱 URL과 배포 ID 일치하는 활성 배포를 "새 버전"으로 갱신 — "제목 없음" 활성 배포가 여러 개라 이전에 엉뚱한 걸 눌러 헛돌았던 것). 재배포 후 `/exec?action=getSavings`가 `Unknown action`→`[]`로 정상.
- 실제 앱 경로(브라우저 fetch/apiPost)로 add·update·delete 왕복 실측: 추가 후 전 필드 정확, 한글 이름 정상, 빈 날짜 `""` 보존, 수정 시 전 필드 갱신, 삭제 후 `[]`. 시트탭 `은경저축` 자동생성 확인. 테스트 행은 모두 삭제해 시트 비움.
- 참고: curl 직접 POST는 302 리다이렉트 재전송 때 411/한글깨짐 발생 — curl 아티팩트일 뿐 앱 fetch 경로는 정상. **이제 은경 저축 탭 실사용 가능.**

## 2026-07-15 (11) / 집 — "은경 저축" 탭 신설 (정기예금·적금 만기/중도해지 관리)
- 새 탭 **💵 은경 저축**: 계좌명·원금·만기이율·만기날짜·만기시금액·중도해지이율·중도해지일·중도해지금액 8컬럼 인라인 편집 표(`.sv-tbl` + `.pp-plain` 재사용). ➕추가로 빈 행 생성, 셀 onchange 자동 저장, 행마다 ✕삭제. 맨 아래 합계 행(원금·만기시금액·중도해지금액). 금액은 ₩ 없이 콤마 숫자, 이율은 %.
- 금액/이율은 **직접 입력**(요청). 나중에 원금·이율로 자동 계산 전환 여지 열어둠 — 컬럼 구조는 그대로.
- **저장은 구글 시트 백엔드**(두 PC 동기화). `Code.gs`에 시트탭 `은경저축` 자동생성 + `getSavings`/`addSaving`/`updateSaving`/`deleteSaving` 추가(holdings CRUD와 동일 패턴). 날짜는 `'` 프리픽스로 텍스트 저장(시트 날짜 자동변환 방지).
- **⚠️ 다음 할 일: Code.gs 수동 재배포 필요.** 재배포 전엔 저장·불러오기 안 됨(프론트는 빈 표로 안전하게 폴백). 재배포 후 추가/수정/삭제 실측 확인할 것.
- 검증: 로컬 서버로 띄워 탭 활성·헤더 8개·빈 상태·가짜데이터 렌더·합계(15,000,000/15,550,000/15,070,000)·입력 파싱(콤마·% 제거)·콘솔 무에러 확인. (백엔드는 미배포라 CRUD 왕복은 재배포 후 검증)
- 두 표 모두 [계좌명 20 | 월 배당금(평균) 21 | 연간 합계 21 | 평가금액 21 | 개시년도 9 | 액션 8%] 동일 그리드. 연금계좌 헤더 월 수령액→월 배당금(평균), 연 수령액→연간 합계. ISA 표의 개시년도·액션 칸은 테두리 없는 빈 칸(표가 평가금액에서 끝나 보임).
- 연금계좌에 평가금액 추가: loadPensionPlan에서 전 계좌 `valByAccount` 집계(현재가×수량, USD 환산) → 계좌 연결 행만 표시, 수동 행(우체국)은 빈칸.
- 액션(✏️ 수정=계좌명 포커스, ✕ 삭제)은 테두리 밖 무테두리 칸으로 이동. 안세공 배지 회색→앰버(#f59e0b) — **회색 금지 피드백 메모리 저장함**. ISA 표 값 색 text2→text.
- 검증: 콘솔에서 두 표 th 너비 완전 일치·헤더 통일·평가금액 표시·수동행 빈칸·✏️/✕ 무테두리 확인, 무에러.

## 2026-07-15 (9) / — ISA·일반 표 계좌명도 인라인 수정 가능
- ISA/일반계좌 라벨을 pp-plain 인라인 입력으로 교체. 표시 이름은 `ppCfg.baseNames[k]` 오버라이드(로컬 저장), 비우면 기본(ISA/일반계좌) 복귀 — `ppSetBaseName`. 집계 키(재남_ISA 등)는 그대로라 계산엔 영향 없음.
- 검증: 콘솔에서 기본 렌더·이름 변경·빈값 복귀 3단계 확인, 무에러.

## 2026-07-15 (8) / — 연금플랜 ISA·일반 표 헤더 '구분' → '계좌명'
- 한 단어 수정. 콘솔 렌더 확인, 무에러.

## 2026-07-15 (7) / — 연금계좌 구분 컬럼 → 계좌명 옆 배지
- 세공/안세공 구분 컬럼 삭제 → 계좌명 셀 안 오른쪽 배지(pill)로 이동(세공=accent 파랑, 안세공=회색 테두리, 클릭 토글 동일). 계좌명 셀은 flex(input min-width:0 + 배지 flex:none).
- 연금계좌 표 th 너비를 위 ISA·일반 표와 정렬: 계좌명 19% / 월 수령액 27% / 연 수령액 27%(= 구분·월배당·연간합계 열과 동일) / 개시년도 17% / 삭제 10%. 참고 문구 갱신.
- 검증: 콘솔에서 두 표 th 너비 [19,27,27,…] 일치·배지 렌더 확인, 무에러.

## 2026-07-15 (6) / — 연금플랜 표 개편 (ISA·일반 컬럼 교체 + 연금계좌 표 평문화)
- **ISA·일반 표**: 컬럼을 [구분 | 월 배당금(평균) | 연간 합계 | 평가금액]으로 교체. 직접입력(월)·적용(월) 삭제 → `ppCfg.base` 오버라이드·`ppSetBase` 제거, ppCalcAt은 자동 월평균만 사용. 평가금액은 getSheetData 현재가(없으면 평단가)×수량, USD는 환율 환산 — loadPensionPlan에서 계산해 `ppAuto.byBase[k].val`에 저장.
- **연금계좌 표**: 셀을 위 표처럼 평문 스타일로(`.pp-plain` — 평소 투명, hover/focus 때만 입력칸 표시). 🔄 버튼·'자동' 배지 삭제(연 수령액 비우면 자동 복귀, title 힌트). 구분(세공/안세공)은 select → 클릭 토글(`ppToggleTax`), ➕추가 시 confirm으로 설정. 계좌명은 계속 인라인 수정 가능.
- 두 표 모두 `table-layout:fixed` + th 너비 % 지정으로 컬럼 폭 통일. 참고 문구도 갱신. 검증: 콘솔에서 ppRenderOwnerCol 출력 검사(새 헤더/옛 컬럼 제거/토글/평가금액 표시) + 무에러. 프론트만 — 재배포 불필요.

## 2026-07-15 (5) / — 연금플랜 ISA 행 0원 버그 수정
- `ppAccClass`가 type `'일반'`도 표준값으로 신뢰해 계좌명 판별을 건너뜀 → DB의 계좌 type이 전부 `'일반'`(기본값)이라 ISA·연금저축 계좌까지 일반으로 분류, 연금플랜 탭 ISA 행이 0이 되고 그 몫이 일반계좌 행에 합산되던 버그. `'일반'`을 신뢰 목록에서 빼서 계좌명 판별로 넘어가게 수정 (portfolio.html 한 줄).
- 검증: 콘솔에서 `ppAccClass({type:'일반',name:'재남 키움 ISA'})` → `'ISA'`, 일반계좌·연금저축·명시적 type ISA 모두 정상, 콘솔 무에러. 프론트만 수정 — 재배포 불필요.

## 2026-07-15 (4) / — 공지 페이지: 종목 목록을 하단 별도 섹션으로 (앱과 동일 레이아웃)
- 공지 카드 안 접이식 "공지 종목 전체 N개"(stk-details) 제거 → 페이지 하단에 **📋 공지 종목 전체** 섹션 신설. 앱의 '내 보유 종목 분배 내역'과 동일한 운용사별 카드 3열 그리드(모바일 1열) + 주기/코드/종목명/분배금/분배율 표. 카드 본문 max-height 340px 스크롤.
- 표시 대상: 현재 공지 종목만(hist 이력·기준일 전월~익월 밖 제외). 앱 renderDistGrid에도 `it.hist` 제외 가드 추가(이력 쌓이면 지난 회차 섞임 방지).
- 프론트만 수정 — Code.gs 재배포 불필요. 실측: KODEX 45/TIGER 33/ACE 19/PLUS 16/RISE 26/SOL 6종목, 콘솔 무에러.

## 2026-07-15 (3) / — 지난 회차 이력 + NEW 7일 + force 타임아웃 90초
- **분배캐시 회차별 이력(Code.gs)**: `writeDistCache`를 source당 회차별 행(최근 3회차 유지)으로 변경, `attachDistHistory`가 이전 2회차 종목을 items에 병합(종목 sched 없으면 그 회차 대표 일정 주입, `hist:true` 표시). 모든 반환 경로를 `finishDist` 헬퍼로 통일 — 기존엔 kodex~plus 분기가 시트캐시를 아예 안 썼음(버그). → **지난 달 운용사별 일정·공지 종목이 계속 보임**(이력은 지금부터 쌓임 — 6월 월중 소급은 ACE 등 시트에 6월 행 남은 소스만 가능). **⚠️ 재배포 필요.**
- **NEW 배지 5일→7일**(양쪽 프론트): 월중 공시(10~13일) 흐름을 다 덮게. 7/10 등록 PLUS·RISE가 5일째에 꺼지던 문제.
- **force 새로고침 타임아웃 25→90초**(양쪽 프론트): KODEX OCR 재파싱이 25초 초과 → 새로고침 직후 달력·표에서 KODEX 통째 누락되던 원인.

## 2026-07-15 (2) / — ACE 파서 수정 (papi WAF 차단 대응)
- **원인 조사**: papi.aceetf.co.kr가 Apps Script 요청에만 HTML 차단 페이지 반환(로컬 PC에선 UA 무관 200, 타 사이트 Origin 헤더면 403). 구글 IP/봇 필터로 추정.
- **수정**: `fetchAceApi()` 헬퍼 신설 — 브라우저형 헤더(Chrome UA, Accept, Accept-Language, Referer)로 요청, HTML 응답이면 `ACE API 차단 (HTTP xxx)` 명시적 에러. `getEtfNotices(ace)`·`fetchDist_ace`(목록/본문/body 3곳) 모두 이 헬퍼로 교체.
- **재배포 완료 + 복구 확인**: 헤더 우회 성공(IP 차단 아니었음 — 봇 핑거프린트 필터). getEtfNotices(ace) 공지 5건, getDistribution(ace) 19종목·일정(공시 7/13·기준 7/15·지급 7/16) 정상. 공지 페이지에서 ACE 카드·달력·일정 표 복귀 실측.

## 2026-07-15 / — 분배금 공지 카드: 월중/월말 빈 슬롯 유지
- **공지 카드 월중/월말 슬롯 고정**: 해당 회차 공지가 없어도 자리를 비워둠(회색 라벨 + 날짜 `-`) — 카드 높이·순서가 달마다 안 흔들림. 특별/기타는 있을 때만. `dist_notice.html`·`portfolio.html` 둘 다 반영, jjk-dist/index.html 복사·푸시.
- **"6월 월중 없음" 원인 규명**: 📢 공지 패널의 6월 월중은 정상 표시됨(전 운용사, 브라우저 실측). 비는 건 🏢 **운용사별 일정 표** — getDistribution이 운용사 사이트의 "현재 공지"만 스크랩해 지난 회차(6월 월중) 일정 데이터 자체가 없음. 분배캐시도 source당 1행 덮어쓰기라 이력 없음. 근본 해결은 Code.gs에 회차별 이력 저장 필요(+수동 재배포) — 미착수.
- **발견(미수정)**: ACE 공지 API(papi.aceetf.co.kr)가 HTML 반환 → `getEtfNotices(ace)` 파싱 실패로 ACE 카드 "불러오기 실패". Code.gs 수정+재배포 필요.

## 2026-07-14 (3) / — 연금플랜 좌우 재남/은경 완전 분리 + ISA 자동추정 미표시 수정
- **레이아웃 전면 개편**: 좌 재남 / 우 은경 2컬럼(grid auto-fit 360px, 모바일은 세로 스택). 사람마다 한 카드에 ISA·일반 표 + 연금계좌 표(+ **인별 1,500만 한도 게이지** — 한도는 1인당이 세법상 맞음) + 기타 연금 표. 연도별 표도 재남(월)/은경(월)/합계/인별 연금계좌 연간으로 분리. 요약 4번째 카드도 인별 표시.
- **ISA 자동추정 0 원인 추정 수정**: 기존엔 계좌 `type` 필드가 정확히 ISA/연금저축/IRP/일반일 때만 집계 → 실계좌 type이 비었거나 다른 값이면 누락. `ppAccClass()` 추가: type이 표준값 아니면 **계좌명으로 판별**(ISA/IRP/연금 포함 여부, 대소문자 무시). 자동 행 생성도 동일 로직.
- **연간 합계 컬럼 추가**: ISA·일반 표가 연간합계 → 월평균(n개월) → 직접입력 → 적용값 순. 월평균 근거가 보이게.
- 데이터 cfg **v3**: pension/others 행에 `owner`(재남/은경) 필드. v1·v2 → v3 마이그레이션(이름에 은경 포함 시 은경, 아니면 재남). 사학 입력도 콤마 처리.
- 검증: mock(type 빈 값·'중개형' 포함)으로 이름 폴백, 인별 분리 합계(재남 1,729,000/은경 1,398,000), 2037 우체국 전환, 인별 한도 게이지 실측. 콘솔 무에러.

## 2026-07-14 (2) / — 연금플랜 피드백 반영 (재남/은경 분리 + 우체국 통합 + 콤마)
- **ISA·일반 → 재남/은경 4행 분리** (재남 ISA/재남 일반/은경 ISA/은경 일반). 자동추정 = 올해 분배금의 계좌유형×소유자(계좌명에 재남/은경 포함)별 월합산 평균, 행마다 (n개월 평균) 표기. 오버라이드 키도 `재남_ISA` 형식으로 변경.
- **우체국 연금 별도 섹션 삭제 → 연금계좌 표에 행으로 통합**. 기본 2행(재남 연 6,348,000 / 은경 연 4,896,000, 세공, **개시 2037년** — 실제 개시일 2037-10-25, 둘 다 동일). "1,500만 합산" 체크 제거 — 세공/안세공 구분만으로 한도 합산. 연금계좌 표에 **월 수령액 컬럼 추가**(월↔연 자동 환산).
- **금액 입력칸 전부 콤마 표시** — type=number → type=text(inputmode=numeric), `ppFmtIn`로 콤마 렌더, `ppNum`이 콤마 제거 파싱.
- 연도별 표에서 우체국 컬럼 제거(연금계좌에 포함). cfg **v2 마이그레이션**(`ppMigrateCfg`): 구 postal→pension 행 변환, 구 base 오버라이드는 폐기(자동 복귀).
- 검증: mock으로 4행 분리값(은경 ISA $100×1400=140,000), 2037년부터 연금계좌 월 1,937,000 전환, 콤마 입력("550,000") 파싱, v1→v2 마이그레이션 실측. 콘솔 무에러.

## 2026-07-14 / — 연금플랜 탭 신설 (월 수령액 시뮬레이션)
- **새 탭 🪙 연금플랜** (`tab-pension`, 대시보드 다음). 프론트만 수정 — Code.gs 변경 없음(재배포 불필요).
- 구성: ① **ISA·일반계좌** — 올해 분배금 데이터에서 계좌유형(`a.type`)별 월평균 자동 추정(채워진 달 기준), 직접입력 시 오버라이드(비우면 자동 복귀). ② **우체국 연금** — 직접입력(월↔연 자동 환산), 기본값 재남 52.9만/은경 40.8만(비율 추정, 채팅 근거 주석 포함), "1,500만 합산" 체크(기본 ON). ③ **연금계좌(연금저축·IRP)** — 연금저축/IRP 계좌마다 자동 행 생성(연금저축=분배금 연환산 자동, IRP=직접입력 0), 세공/안세공 분류, 섹션 상단에 **연 1,500만 종합과세 한도 게이지**(세공 행+우체국 포함분 합산, 초과 시 빨간 경고). 삭제한 자동 행은 `pensionRemoved`로 재생성 방지. ④ **기타 연금** — ➕추가로 제목/월액/개시년도 (국민연금·사학연금용), 사학연금 납입누계(참고) 필드. ⑤ **연도별 표** — 퇴직월(기본 2029-03, month input) 행 하이라이트, 각 행 개시년도 반영한 연도×소스별 월 수령액 + 한도체크 컬럼. 요약 stat 카드 4장(지금/퇴직시점/전체개시/한도합산).
- **저장은 localStorage(`jjk_pension_plan`)** — ⚠️ PC·폰 간 동기화 안 됨. 기기 간 공유가 필요해지면 Code.gs에 config 저장 액션 추가+재배포 필요(다음 할 일 후보).
- 검증: node 문법 체크 OK, 로컬 서버+mock api로 자동추정 산식(ISA 640k=KRW500k+$100×1400, 연금계좌 연환산), 한도 초과 경고(17,244,000 시 ⚠️), 개시년도별 연도표(2026/2033/2040), 기타 추가 모두 실측 확인. 콘솔 무에러.

## 2026-07-09 (2) / — 저장소 다시 public + 백엔드 토큰 인증 도입
- **private화가 GitHub Pages(jjk/portfolio.html URL)를 꺼뜨림** → 사용자가 URL로 못 열게 됨. 무료 Pages는 public 저장소만 가능. **jjk를 다시 public으로 되돌리고 Pages 재생성**(API POST, main/root). URL 복구 확인(200).
- 대신 **데이터는 백엔드 토큰으로 보호**하도록 변경:
  - `Code.gs`: `_authOk(action, token)` 추가. Script 속성 `APP_TOKEN`과 일치해야 통과. `PUBLIC_ACTIONS`(getDistribution·getEtfNotices)는 토큰 없이 허용(공개 분배 페이지 프록시용). **속성 미설정 시 전부 허용(하위호환)** → 재배포 전 앱 안 끊김. doGet/doPost 양쪽에 가드.
  - `portfolio.html`: 하드코딩 비번 제거. 게이트가 입력값을 `APP_TOKEN`으로 삼아 `getAccounts` 호출로 검증. `api`/`apiPost`/getAlerts/markAlertRead 모두 `token` 파라미터 전송. 세션 저장키 `jjk_token`. 프리뷰로 통과·부팅·계좌7개 로드 검증.
- ⚠️ **다음 할 일(사용자 수동)**: Apps Script 편집기 → 프로젝트 설정 → 스크립트 속성 `APP_TOKEN=1231` 추가 → Code.gs 붙여넣기 → **기존 배포 편집(새 버전)으로 재배포**(URL 유지). 이걸 해야 실제 강제됨. 그 전까지는 아무 비번이나 통과(하위호환).

## 2026-07-09 / — 보안(저장소 private화) + 진입 비밀번호 + ACE 표시 버그
- **`jjk` 저장소를 private으로 전환** (GitHub API). public일 때 portfolio.html·Code.gs 소스가 통째로 노출됐고, 그 안의 개인 GAS API URL로 외부에서 `getAccounts`/`getHoldings` 직접 호출해 실명 계좌·보유내역이 그대로 뽑히는 것 실증됨. 공개 분배금 페이지는 `jjk-dist`(별도 저장소)에서 서빙되므로 영향 없음. 확인: jjk API 404, github.io/jjk-dist 200.
- **portfolio.html 진입 비밀번호 잠금 추가**: body 최상단 #gate 오버레이 + 하단 스크립트 `APP_PASSWORD`(현재 '1231') / `submitGate` / sessionStorage `jjk_auth`. init()은 인증 후에만 호출. 프리뷰로 오답 차단·정답 통과·콘솔 무에러 검증 완료.
- **ACE 분배금 안 뜨는 버그 수정**: renderDistGrid의 "묵은 자료 숨김" 필터가 기준일 月을 당월/익월로만 비교 → 7월에 ACE 최신(6월, cycle 태그 없음) 항목이 전부 걸러짐. 전월도 허용하도록 수정.
- ⚠️ 한계: 비밀번호는 클라이언트 잠금일 뿐, 개인 API는 여전히 열려 있어 API 직접 호출은 차단 못 함. 다음 할 일(원하면): Code.gs doGet/doPost에 토큰 검사 추가 후 재배포.

## 2026-07-08 / — 시세 13초 지연 근본수정 (getSheetData 캐시)
- 사용자 캡처의 "…"는 직전 커밋에서 추가한 로딩 표시. 멈춘 게 아니라 getSheetData 응답(최대 13초) 대기 중 상태였음. 라이브 실측: 은경 미래에셋 연금저축 12종목 전부(494300=10,270/+0.08%) 결국 채워짐.
- **Code.gs `getSheetData`에 CacheService 3분 캐시 추가** → 첫 호출만 느리고 이후 로드/탭전환/일괄적용은 즉시. ⚠️ **Apps Script 수동 재배포 필요.**
- portfolio.html: 로딩 '…'를 빈 셀에만 적용(기존 값 유지) → 새로고침 시 깜빡임 제거.
- 남은 개선: 첫 호출도 빠르게 하려면 time-trigger(snapshotPrices 등)로 캐시 warm-up.

## 2026-07-08 / — 시세 로드 안정화 (getSheetData 간헐 지연 대응)
- 증상: 새로고침 시 종목관리 현재가/평가금액/손익이 "-"로 멈춤. 추종·배당주기는 정상.
- 원인: 외부 '주식상황' 시트의 실시간 수식 재계산으로 `getSheetData`가 warm 1.8초 → cold 13.5초/타임아웃. 로드 시 동시 호출 경합으로 실패하면 `.catch(()=>({items:[]}))`가 빈 배열로 삼켜 시세가 "-"로 굳음. 브라우저 실측 확인.
- 수정(A, 프론트): `loadHoldingPrices`에 getSheetData 빈 결과 시 1.5초 후 1회 재시도(`fetchSheet`), 로딩 중 셀을 '…'로 표시. 67개 개별조회 폴백 대신 재시도 경로 사용.
- 즉시 회피책: 종목관리 "📈 시세 불러오기" 단독 클릭 → 정상.
- 다음 할 일(B, 근본): Code.gs `getSheetData`가 '주식상황' 재계산 대신 시세로그/snapshot 캐시에서 현재가 읽게 변경 → 재배포 필요.

## 2026-07-07 / 직장 — 종목관리 괴리율(NAV) 컬럼 추가
**목적**: 리밸런싱 지표로 괴리율(현재가 vs 기준가NAV) — 님 요청, "추세" 다음 컬럼. (지표 논의 결과 스냅샷 괴리율부터, 월별 추이 로깅은 추후 확장 후보)
**데이터 소스 확인**: Naver realtime API(`polling.../stock/`)엔 nav **없음**(시세만). ETF 리스트 API(`finance.naver.com/api/sise/etfItemList.nhn`)엔 **nav 있음**(1142개 ETF, 예: 161510 nav 25449). → 이걸 씀.
**구현**:
- `Code.gs`: `getNavMap()` 추가 — etfItemList 1회 호출 → `{티커:nav}` 맵, 1h 캐시. 라우터 `getNavMap` case 추가. **⚠️ 비공개 GAS 재배포 필요**.
- `portfolio.html`(renderHoldings): "추세" 다음 `괴리율` th + `disc-<id>` td 추가. `loadHoldingPrices`가 navMap 병렬 로딩 → 종목별 `(현재가−NAV)/NAV×100` 렌더(+프리미엄 빨강/−디스카운트 파랑, ±0.05% 이하 회색). 국내 ETF만(navMap에 있을 때), USD·비ETF는 `-`.
- 재배포 전엔 getNavMap 없어 괴리율 전부 `-`(graceful), 재배포 후 값 표시.
**검증**: 재배포 → 종목관리 "시세 불러오기" → 괴리율 값·부호 확인(예 161510 ≈ −0.1%). 표 컬럼 정렬 확인.

---

## 2026-07-07 / 직장 — "운용사별 일정" 표 디자인 개선
**증상**: 표가 지저분·선이 이상함. 원인: 모든 셀 1px 격자테두리(#d4c2e8) + 한 셀에 월중/월말 두 값을 내부 border-bottom으로 우겨넣어 인접 셀 분할선이 안 맞음 + 행 테두리와 겹침.
**수정**: 회차(월중/월말)마다 **실제 `<tr>`로 분리**, 운용사명은 `rowspan`으로 묶음. 셀 내부 분할선 제거 → 가로선만. 회차는 알약(pill) 배지(월중 파랑/월말 핑크), 날짜는 종류별 색(공시 amber·분배락 green·기준 blue·지급 red), 빈 값은 muted `·`. 격자테두리 → 운용사 그룹 사이 1px solid var(--border)만. 하드코딩 색(#d4c2e8/#c9b3e0) 제거하고 CSS 변수 사용.
- `portfolio.html`(renderMasterCalendar) + `dist_notice.html`(공개, 보유X만 뺀 동일본) 둘 다 적용 → jjk-dist 재배포.
- **후속**: portfolio.html은 전역 CSS(`td{border:1px solid #64748b}`, `th{background:#e2e8f0;text-transform:uppercase}`, `tr:last-child td{border-bottom:none}`)가 표에 세로격자·회색헤더·맨아래선제거를 강제 → 각 셀에 인라인 `border:0`+필요한 밑줄만 박아 눌러씀(인라인이 전역 선택자 이김). "맨 아래 선 혼자 없음"도 이 `tr:last-child` 규칙 때문 → 해결. 공개페이지 룩에 맞춤.

---

## 2026-07-07 / 직장 — PLUS 월말 해결 ✅ (원인=결제 미연결, 코드 아님)
**결론**: PLUS 월말 누락은 **비공개 GAS 프로젝트에 Google Cloud 결제(billing)가 연결 안 돼 Vision OCR이 막혀 있던 것**. 코드 문제 아님.
- 진단 경로(`diagPlus`/`diagPlus2` GAS에서 실행): ① `VISION_API_KEY` 존재(길이39)하나 OCR 0 → ② Vision 직접호출 원본응답 확인: 처음 `API_KEY_INVALID`(키 무효, 다른 대화서 삭제→복구) → 키 교체 후 `BILLING_DISABLED`(403, 결제 미연결) → 프로젝트#610977749646에 결제계정 연결(무료체험 크레딧 있음) → Vision HTTP 200, OCR 정상.
- 결과: `getDistribution('plus', true)` 재파싱 → **items 15개(월중 3 + 월말 12)**, 월말 sched 정확(공시 6/25·분배락 6/29·기준 6/30·지급 7/2). PLUS는 분배내역을 이미지로 공지하므로 OCR 필수 — OCR 살아나니 `fetchDist_plus`가 정상 동작(코드 수정 0).
- **최종 상태**: 오늘 프론트 cycleOf 월-경계 수정(별도 커밋) + 이 결제 연결 = PLUS 월중·월말 둘 다 정상.
**선택 개선(미적용)**: `ocrImageText`가 Vision 에러를 조용히 `''`로 삼켜 진단이 어려웠음 → 실패 시 `console.log`로 에러 남기면 다음 OCR 장애 진단이 쉬움(백엔드 수정+재배포 필요, 지금은 안 함).

---

## 2026-07-07 / 직장 — CLAUDE.md 작업원칙 보강
**한 일**: karpathy-guidelines 스킬(github.com/multica-ai/andrej-karpathy-skills) 읽고 CLAUDE.md "Working principles" 보강. `.claude/`는 gitignore라 스킬 파일 설치는 두 PC 동기화 안 됨 → git 동기화되는 CLAUDE.md에 원칙을 박는 방식 채택.
- 추가: 원문 링크 + "속도보다 신중, 사소한 건 재량" 단서 / 원칙2 자가체크("시니어가 과하다 할까? 200줄→50줄이면 재작성") / 원칙3 "내 변경이 만든 고아만 제거, 기존 죽은 코드는 신고" + "바뀐 줄이 요청에 직결되는가" 테스트 / 원칙4 "재현 먼저(테스트러너 없으니 콘솔로 `_distData` 덤프 등) → 수정 → 재현 소멸 확인".

---

## 2026-07-07 / 직장 — PLUS 월중/월말 오분류 버그 수정 🐛
**증상**: portfolio.html "운용사별 일정" 표에서 PLUS가 월중 줄 비고 월말 줄에만 뜸(`플러스가 안나오네`).
**원인 확정**(브라우저 콘솔 `_distData` 덤프로): PLUS 데이터는 smarttoday 기사 출처라 아이템에 `cycle` 없음 + 대표 `schedule` 기준일 = **6월 15일**(명백한 월중, title도 "PLUS 월중 배당"). 그런데 `cycleOf`가 `base.m === month(=7월) ? 월중 : 월말` → 6≠7이라 **월말로 오분류**. **월 경계 버그**(7월에 6월 데이터 보면 전부 월말).
**수정**: 회차 판정을 월 비교 → **기준일의 '일(day)' 기준**(20일 이하=월중, 초과=월말)으로 변경. 월 경계 안전. 명시 `it.cycle` 있으면 그대로 우선.
- `portfolio.html` 2곳: `renderMasterCalendar`의 cycleOf(캘린더+일정표), `renderDistGrid`의 cycleOf(보유종목 그리드).
- `dist_notice.html` 1곳(공개 페이지) 동일 수정 → jjk-dist repo에도 재배포.
**검증**: PLUS 기준일 6/15 → 월중 ✓. **portfolio.html 열린 탭 하드새로고침(Ctrl+Shift+R)으로 육안 확인 필요.**
**남은 것(선택)**: 백엔드 `fetchDist_smarttoday`가 PLUS **월말 일정은 아예 안 가져옴**(왼쪽 공지엔 PLUS 월말배당 06.25 있음). 완전하려면 백엔드에서 월말 기사도 파싱 필요(GAS 수정+재배포). 분류 버그와는 별개.
**→ 월말 누락 근본원인 규명(같은 세션)**: PLUS는 분배내역을 **이미지로만** 공지(plusetf 상세 n=30821 = 텍스트표 아님, `hwadm.plusetf.co.kr/webapp/upload/*.PNG`). `fetchDist_plus`는 OCR(`ocrImageText`, Google Vision) 의존인데 `ocrImageText`는 `VISION_API_KEY` 스크립트 속성 없으면 즉시 `''` 반환 → PLUS 0건 → `fetchDist_smarttoday` 폴백. 근데 smarttoday는 **브랜드당 기사 1건**만 수집(브랜드당 첫 매칭에서 `found[brand]` 차단) → PLUS는 월중 기사 하나만 → **월말 구조적 누락**.
- **조치(님 몫)**: 비공개 GAS 편집기 → 프로젝트 설정 → 스크립트 속성에서 `VISION_API_KEY` 확인. 진단함수 `diagOcrPlus`(PLUS 이미지 OCR 테스트)로 키/OCR 상태 확인. 키 없으면 Google Vision 키 발급·등록 → `getDistribution('plus', true)`로 강제 새로고침. 키는 자격증명이라 Claude가 안 만짐.
- OCR 살아나면 fetchDist_plus가 월중·월말 둘 다 정상 파싱 예상. 그래도 안 되면 parsePlusNotice regex 조정 필요.

---

## 2026-07-07 / 직장 — 공개 페이지 호스팅 완료 ✅
**한 일**: 호스팅 결정 → **(a) 별도 공개 repo** 채택. 새 repo `jjk-dist` 생성 → `dist_notice.html`을 `index.html`로 복사해 push → GitHub Pages 켬.
- **공개 URL**: https://jaenamking1-collab.github.io/jjk-dist/ (HTTP 200 확인).
- 안전 재검증: 공개 페이지에 개인 GAS/`portfolio` 노출 **없음**, `getHoldings` → `Not allowed` 확인.
- 구조: `jjk-dist`는 **배포 전용**. 원본 수정 시 `jjk/dist_notice.html` → `jjk-dist/index.html` 재복사 후 push 필요(해당 repo README에 명시). 폴더: `C:\Users\azsxd\Documents\jjk-dist`.

**⚠️ 새 이슈 (다음 세션 백엔드에서 확인 필요)**: 07-07 현재 백엔드 데이터가 **전부 빈값**.
- `getDistribution` → `{"items":[],"error":"기사/사이트 모두 실패"}`
- `getEtfNotices`(kodex·ace·tiger·plus) → `{"success":true,"items":[]}` (전부 빈 배열)
- 07-06 "검증 완료"였는데 하루 뒤 빈값 → **캐시 만료 후 재-깨짐 / 비공개 GAS 배포 되돌아감 / 상류 사이트 차단** 중 하나 의심.
- 조치 후보: 비공개 GAS 편집기에서 실 fetch 로그로 재검증, 필요 시 재배포 + `clearNoticeCache` + `notices_*` 캐시 제거. 공개 프록시는 중계만 하므로 손댈 것 없음.

**남은 것**: 호스팅은 완료. 위 **데이터 빈값 이슈만 비공개 GAS에서 확인**.

---

## 2026-07-06 / 집 — 검증 완료 ✅
**한 일**: 비공개 GAS 재배포 + 공지 캐시 초기화(`clearNoticeCache`) 후 공개 프록시로 재검증.
- **6개 운용사 공지 전부 정상**(TIGER·PLUS 서버 파서도 실 HTML과 일치 확인).
- 캘린더(`getDistribution`)·운용사 일정·보안 차단(`getHoldings`→`Not allowed`) 모두 정상.
- 백엔드/페이지 기능 완료.

**남은 것 (다음 세션 재개 지점)**: `dist_notice.html` 호스팅만 남음.
- ⚠️ 이 repo로 GitHub Pages 켜면 `portfolio.html`도 `.../jjk/portfolio.html`로 공개돼 **개인 GAS URL 노출**(우리가 막으려던 문제). → **공개용 별도 repo에 `dist_notice.html`만 올리는 것 권장.**
- 결정 필요: (a) 공개 전용 새 repo 만들어 그 파일만 배포, (b) 이 repo Pages + 위험 감수.
- 공개 프록시 URL은 `dist_notice.html`의 `API`에 이미 박힘(안전 — 개인데이터 차단 검증됨).

---

## 2026-07-06 / 집 — (b) 공개 페이지 작성 완료
**한 일**:
- `dist_notice.html` 생성 — 반응형 공개 페이지(3패널: 공지·캘린더·운용사일정). 보유종목 강조·보유X 태그·하단 보유표 전부 제거. `API` = 공개 프록시 URL placeholder. 6개 운용사 공지를 모두 프록시 `getEtfNotices`로 통일(브라우저 CORS 제거). 모바일은 세로 스택 + 캘린더/일정 좌우 스크롤(min-width로 5열 유지).
- `Code.gs`의 `getEtfNotices`에 **tiger·plus 분기 추가**(서버측 파싱). → **비공개 GAS 재배포 필요**.

**진행 상황**:
- ✅ 공개 프록시 배포됨. URL을 `dist_notice.html` `API`에 반영(커밋).
- ✅ 보안 검증: 공개 URL `?action=getHoldings` → `{"error":"Not allowed"}` (개인데이터 차단).
- ✅ `getDistribution`(캘린더) + KODEX/ACE/RISE/SOL 공지 정상.
- ⚠️ TIGER·PLUS 공지 `items:[]`. 원인: 비공개 GAS 재배포 전이거나, `getEtfNotices` 6h 캐시에 옛 빈결과가 남음(getEtfNotices는 force 없음 → 빈값도 캐시).

**다음 할 일**:
1. 비공개 GAS(`Code.gs`, tiger/plus 분기 포함) **재배포**(기존 배포 "새 버전").
2. 비공개 GAS 편집기에서 공지 캐시 비우기: `CacheService.getScriptCache().removeAll(['notices_tiger','notices_plus'])` 한 줄 함수 실행.
3. 재검증 — tiger/plus 공지 뜨는지. 안 뜨면 서버 정규식 조정(실 HTML 확인).
4. `dist_notice.html` GitHub Pages 배포.

---

## 2026-07-06 / 집
**한 일**: 분배금공지 공개 페이지 착수 — 백엔드 (a) 단계. `public_dist_proxy.gs` 생성(얇은 프록시: `getDistribution`·`getEtfNotices`만 화이트리스트로 비공개 GAS에 중계, 개인 데이터 액션 차단, force 무시, 30분 캐시). 실제 비공개 URL은 파일에 넣지 않고 placeholder 유지(공개 repo 대비).
- 구조 결정: **얇은 프록시** 채택(파서 복붙 대신) → 단일 소스, 복제 0.
- 권한 프롬프트 감소 위해 `.claude/settings.local.json` allow/deny 정비(로컬 전용).

**다음 할 일 (b 단계)**:
1. `portfolio.html` 분배탭 렌더 로직(`loadDistributions`·`renderMasterCalendar`·`loadAllNotices`·`normalizeNoticeTitle`) 읽고, **보유종목 강조·보유X 태그 제거**한 반응형 공개 페이지 `dist_notice.html` 1개 작성. API 상수 = 공개 프록시 URL.
2. TIGER·PLUS 좌측 공지 서버화: 비공개 `Code.gs`의 `getEtfNotices`에 tiger/plus 분기 추가(실 HTML 구조 확인 후) → 재배포.
3. 폐기: `dist_notice_pc.html`/`dist_notice_mobile.html`(이전 세션 산출물, GAS URL 노출) — repo엔 없음.
**배포(님 몫)**: 공개 프록시 GAS 새 프로젝트에 붙여넣기 → PRIVATE_URL 교체 → 웹앱(모든 사용자) 배포 → 공개 URL을 페이지에 사용.

---

## 2026-07-06 / Windows (직장) — 후속
**한 일**: `Code.gs` 변경분(top-level 디버그 2줄 제거)을 Apps Script 편집기에서 **재배포 완료**. 실제 백엔드에 반영됨.

**다음 할 일**: (없음)

---

## 2026-07-06 / Windows (직장)
**한 일**:
- CLAUDE.md 정비: init 형식 재작성, 백엔드 API 계약 문서화, Karpathy 작업 원칙, 두 PC 루틴/세션 연속성 규칙 추가.
- `Code.gs`(Google Apps Script 백엔드)를 repo에 추가 + 매 호출마다 ACE 파싱을 돌리던 top-level 디버그 2줄 제거.
- `portfolio.html`: 분배금 그리드 소계/합계 폰트 크기를 `1em`으로 통일, 표시되는 ₩ 기호 전부 제거(파싱용 정규식 속 ₩는 유지).
- 로컬 설치 도구용 `.gitignore` 추가(`.agents/`, `.claude/`, `skills-lock.json`).
- WORKLOG.md 신설 + 두 PC 동기화 루틴 확립.

**다음 할 일**: `Code.gs` 변경분을 Apps Script 편집기에 붙여넣고 **새 배포**(재배포)해야 실제 백엔드에 반영됨.
